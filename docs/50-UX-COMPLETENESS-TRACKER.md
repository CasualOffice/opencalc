# 50 — UX Completeness Tracker

_Generated 2026-08-08 from a six-domain parallel audit of the editor against
Microsoft Excel + Google Sheets standard UX. Every "done" feature was
re-examined for actual completeness and standard behavior. Work items are
completed **one at a time to full Excel/Sheets parity** (implement → verify
in-browser → commit) before starting the next._

Status: 🔴 Todo · 🟡 Partial-in-progress · ✅ Done. Severity: **P0** broken /
data-loss / wrong-target · **P1** expected-daily · **P2** polish.

Sources: audits of selection/navigation, editing/clipboard, formatting,
structure/data, **formula-authoring**, and chrome/fidelity. Code citations are in
each audit; this file is the durable backlog.

---

## Already completed this session ✅

- Font pipeline (opendoc parity): substitution table (`casual-calc-layout`),
  bundled faces + skrifa glyph rendering (`casual-calc-render`), per-glyph
  coverage fallback, canvas `@font-face` + `font_css_stack`, per-workbook default
  font, canvas default size aligned to 11pt.
- Toolbar: menu bar (File/Edit/View/…); progressive group-collapse (no
  scrollbar/wrap); phantom collapsed-button fix; font/size auto-populate.
- Freeze: prominent **draggable** divider (change/remove by drag); header resize
  still works.
- Engine correctness: wildcard COUNTIF/SUMIF/AVERAGEIF(S); non-finite → #NUM!;
  missing builtin number-format IDs; Cmd+Shift+Z redo.
- Font picker: custom searchable combobox (UX-P07) — a native `datalist` is
  filtered by the input's own text, so the auto-populated box offered one font.
- **Import fidelity (round 2)**: shared formulas (`<f t="shared">`) expand from
  their master instead of importing as bare cached constants; theme + tint and
  legacy indexed colours resolve (the form every Excel built-in cell style
  uses); multi-area `sqref` keeps every area; comments bind through the OPC
  relationship graph rather than by part numbering.
- **Import fidelity**: `<col customWidth="true">` (LibreOffice / POI / ExcelJS
  spelling) no longer discards every column width; `customWidth` no longer gates
  whether a width is honored at all; `hidden`/`collapsed`/`wrapText` likewise;
  `<b val="0"/>` no longer reads as bold ON. The editor's auto row-height no
  longer overrides a height the workbook set, no longer un-hides hidden rows,
  and no longer inflates every styled row by 25%.
- **Text overflow control**: overflow / wrap / **clip** as one three-way choice
  on the toolbar and in Format ▸ Text overflow. Clip is an engine-side view
  setting (SpreadsheetML has no attribute for it — Excel always spills), noted
  in the fidelity ledger as not surviving a save.
- **Text overflow**: spill stays inside its own pane (a frozen cell can't borrow
  the scrolling body's columns, and vice versa), and a number too wide for its
  column renders `#######` instead of spilling or being clipped into a shorter,
  plausible-looking wrong number.
- **Frozen-pane painting**: selection spans are pane-relative (a body range no
  longer paints a sliding strip into the frozen band, and no longer vanishes
  when its start scrolls off); the selection outline / marching ants / fill
  preview clip per pane; a merge straddling a freeze line is painted as one
  slice per pane instead of one rect built from a pinned edge and a scrolling
  edge (which went negative-width and flipped back over the frozen band); merge
  geometry reads the drawn grid, so auto-row-height no longer makes merges drift
  from their own cells as you scroll. The in-cell editor follows its cell while
  scrolling and spans a merged block; a selection containing a merge unmerges it.

---

## P0 — broken / data-loss / wrong-target

| ID | Item | Domain | Status | Fix |
|----|------|--------|--------|-----|
| UX-F01 | **Formula bar is a dumb field** — no autocomplete, no ref-picking, no highlight; all formula intelligence is bound to the inline editor only | Formula | ✅ | One edit session keyed to `editSurface` (the in-cell overlay *or* the formula bar): autocomplete, click/drag reference picking, the invalid-formula outline, commit and revert all read the active surface. Text mirrors between the two, and focusing the bar mid-edit hands the same edit over. Reference highlighting (UX-F03) still to come |
| UX-M01 | **Merge silently hides data** — non-anchor values not cleared, no confirm; reappear on unmerge, round-trip keeps hidden values | Format | ✅ | `session_merge_hidden_count()` counts what a merge would bury; if any, a confirm names the number before anything happens. `session_merge_cells_discarding()` then merges and clears the covered values (keeping their styling) in one batch, so it is one undo step and unmerging no longer resurrects them |
| UX-N01 | **Scientific format renders garbage** (`0.00E+00` → `12345.00E+`) | Format | ✅ | Exponential branch in `numfmt.rs`: mantissa decimals from the pattern, exponent zero-padded to its placeholder count, `+` only when the pattern asks. Handles the carry when rounding pushes the mantissa to 10 (`9999` → `1.0E+04`), and skips an `E` inside a literal |
| UX-N02 | **`[Red]`/color codes in number formats dropped** (negatives not red) | Format | ✅ | `format_number_colored()` returns the chosen section's colour (Excel's eight named ones; `[Color n]` deliberately not guessed at), threaded through `display_color()` into `session_cells`' `fc`, where it overrides the style's font colour as in Excel. Verified by opening a file whose negatives carry `[Red]` |
| UX-N03 | **Text (`@`) format inert**; text values bypass number formatting | Format | ✅ | A cell formatted `@` keeps what was typed as text (`007` stays `007`, not `7`) — coercing it was a silent edit of the user's input. `format_text()` applies a code's text section to string values (`@" kg"`, and the 4th section of a multi-section code), wired into `display_text` |
| UX-D01 | **Sort includes the header row + single-key only** — corrupts tables | Data | ✅ | `session_sort_range_multi()` takes N key columns each with its own direction (later keys break ties in earlier ones); `looksLikeHeader()` defaults the "my data has a header row" checkbox by Excel's rule of thumb, and a header row is excluded by starting the sort one row lower. Custom-sort dialog (3 keys, columns named by their heading) on the toolbar, Data menu and cell menu; the one-click A→Z / Z→A keep working and now also spare the header |
| UX-C01 | **Right-click on row/col header** shows generic cell menu and acts on the *previous* selection (wrong target) | Chrome | ✅ | The contextmenu handler detects a header hit (cellAt returns null there, which is why it fell through), selects that band unless it is already inside a row/column selection, and opens a header menu whose verbs name the band: insert before/after, delete, clear, exact size, autofit, hide/unhide. Corner → select all |
| UX-E01 | **Alt+Enter multi-line entry impossible** (inline is `<input>`) | Editing | ✅ | `#inline-edit` is a `<textarea>` that grows past the cell while editing; Alt+Enter inserts a break (in-cell only — the formula bar is one line), Enter still commits. `wrapLines()` now treats `\n` as a hard break instead of letting `\s+` swallow it, and committing a multi-line value turns wrap on for that cell, as Excel does |

## P1 — expected daily

| ID | Item | Domain | Status | Fix |
|----|------|--------|--------|-----|
| UX-F02 | Arg/signature tooltip inside `()` with active arg bolded, persists | Formula | ✅ | `callAtCaret()` walks the text to the caret tracking a call stack, so nested calls resolve to the innermost and commas inside string literals or nested parens don't advance the argument. Yields to the name list while that is open; follows the caret on arrow/Home/End/click; a trailing `…` argument stays active past the named ones |
| UX-F03 | Colored reference highlighting (formula tokens + on-grid range-finder boxes) | Formula | ✅ | On-grid range-finder outlines landed earlier (UX-F03a); **token tinting** closes it. An `<input>` cannot colour a substring, so a mirror element sits exactly behind the editing surface rendering the same text with coloured spans, while the surface draws its glyphs transparent and keeps its caret (`caret-color`) and selection. The mirror is inert — no pointer events, `aria-hidden` — so typing, selection and IME are untouched; it copies the surface's metrics and follows its scroll, since a long formula scrolls inside the box. Works on both the formula bar and the in-cell editor, and the colours index the same palette as the grid outlines, so a token and its box always match |
| UX-F04 | Cross-sheet reference picking (click another sheet's cells mid-formula) | Formula | ✅ | A tab click mid-formula navigates without ending the edit (`switchSheet(i, keepEdit)`), picks there insert a qualified `Sheet!ref` (quoted when the name is not a bare word), and commit/cancel return to the cell the edit belongs to — tracked as `editHome`, since the write target can no longer be "wherever the selection is now" |
| UX-F05 | F4 anchor cycling ($A$1→A$1→$A1→A1) | Formula | ✅ | `cycleAnchors()` finds the reference under the caret from the UX-F03 spans and rewrites it A1 → $A$1 → A$1 → $A1 → A1; a range cycles both endpoints together and a sheet qualifier is left as written |
| UX-F06 | Keyboard point mode (arrows insert/extend a ref while editing) | Formula | ✅ | `pointStep()` — arrows build a reference when the caret sits where one may go (otherwise they move the text caret), Shift extends it into a range, the pointed cell is scrolled into view without disturbing the selection, and typing anything ends the mode leaving the reference in place |
| UX-F07 | Editing an existing formula re-highlights its refs | Formula | ✅ | `beginEdit` runs the range finder, so F2 on a stored formula outlines its inputs immediately — from either surface |
| UX-F08 | Error affordances: per-cell error corner marker + hover explanation + trace precedents/dependents | Formula | ✅ | Error corner markers and hover explanations landed earlier; **trace precedents/dependents** closes it. The eval crate already walked precedents to build its dirty set, so this exposes that walk rather than inventing a second one — a traced arrow can never point somewhere recalculation would not follow. Precedents come back as *blocks*, so a range is one arrow rather than one per cell. Dependents scan the workbook rather than keeping a reverse index: tracing is a deliberate, occasional action, and a persistent reverse map would have to be maintained on every edit for something asked once in a while. Arrows are colour-coded by direction and cross-sheet hits are counted in the status line rather than silently dropped, since they have nowhere on screen to point |
| UX-E02 | Formula bar second-class: no Esc-revert, no autocomplete, no ref insert, Enter doesn't advance | Editing | ✅ | All four closed by UX-F01: Escape restores the cell's text, autocomplete and reference insert work from the bar, Enter commits and moves down, Tab commits and moves right, an invalid formula outlines the bar and keeps focus there |
| UX-E03 | Find: no highlight-all, match-entire-cell, wildcards/regex, Values look-in, all-sheets scope | Editing | ✅ | Highlight-all, match-entire-cell, Values look-in and all-sheets scope landed earlier; **wildcards** close it. `*` and `?` reuse the matcher written for autofilter rather than a second implementation of the same language — it was made public and given a case-fold flag. A substring search wraps the pattern in `*`, since the matcher is whole-string. Regex is deliberately **not** offered: Excel's Find is wildcard-based and this is an `.xlsx` engine, so matching Excel's semantics is the point; Sheets-style regex would quietly mean something different for the same box |
| UX-E04 | Ctrl+D / Ctrl+R fill-down/right missing | Editing | ✅ | `fillWithin()` fills the selection from its own first row / first column via `session_fill`, so references shift as they do on a drag-fill |
| UX-E05 | Double-click fill handle to autofill to neighbor extent | Editing | ✅ | `autofillToNeighbour()` — the extent comes from the adjacent column's data edge (left column, else right), which is Excel's rule |
| UX-V01 | Data validation: list-only + **not enforced**; no number/date/text/custom rules, no input/error messages | Data | ✅ | **Only `type="list"` was parsed**, so a file's number, date, time, text-length and custom rules were dropped the moment it was saved — the cells looked constrained in Excel until you reopened and found they were not. All eight OOXML kinds are modelled now, with the operator, both operands, `allowBlank` and the author's own input/error message wording, round-trip tested. Enforcement lives in the model (`DataValidation::accepts`) so the rule and the check cannot drift; a `Custom` rule returns "cannot judge" rather than blocking input on something this layer does not evaluate, and an unparseable operand (a range reference) rejects nothing rather than making the cell uneditable. The panel offers every kind, and the author's message beats the generated one. **Also fixed**: `session_validation_at` returned an empty array for non-list rules, which every JS array makes truthy — so a whole-number cell grew a dropdown chevron that opened onto nothing |
| UX-V02 | Autofilter: single global column, checklist-only, fights manual hidden set; no per-header dropdowns/conditions/multi-column | Data | ✅ | Rebuilt on the engine. The filter is now a **model object** (`Sheet::auto_filter`) rather than editor state, so it saves to `.xlsx` (`<autoFilter>` with `<filters>`/`<customFilters>`), undoes as one step, and shifts correctly through row insert/delete. **Per-header dropdown buttons** on every column of the range, accented where a rule is active. **Any number of columns** filter at once, AND-ed. Checklists are **chained** — a column offers only the values left by the other columns' rules — and searchable. **Conditions**: two comparisons joined by AND/OR; contains / begins with / ends with are encoded as OOXML `equal` + wildcards, since that is how Excel stores them. Filter-hidden rows live in `Sheet::filter_hidden`, **separate from `hidden_rows`**, so clearing a filter releases exactly the rows it hid and leaves hand-hidden rows alone. Not yet: sort-from-dropdown, filter-by-colour, top-10 |
| UX-V03 | Row-height / column-width numeric dialog missing | Data | ✅ | `sizeDialog()` on the row/column header menu (landed with UX-C01), seeded with the current size and applied across the selected band |
| UX-V04 | Outline / grouping: model support, zero UX | Data | ✅ | The model carried outline levels and collapsed flags all along — import and export round-trip them — but nothing could create or toggle a group. New wasm surface: `session_group` / `session_ungroup` (± one level, capped at OOXML's 7), `session_toggle_outline`, `session_show_outline_level`, and `session_outline` for reading the gutter. `Sheet::outline_band` resolves which lines a summary controls: the contiguous run nesting deeper than it, above when `summaryBelow` (the OOXML default) and below otherwise. Editor gains an **outline gutter** left of the row headers / above the column headers, sized from the deepest level and zero-width otherwise, drawing a rail per group and a ± toggle at each summary line; `HW`/`HH` absorb the gutter so all existing geometry follows it. Data ▸ Group covers group/ungroup, expand/collapse all, and show-level. **A collapse hides through `hidden_rows`**, because that is what OOXML writes, so unhiding by hand can reveal a collapsed band — `hidden_edit` now clears any collapse flag whose band is no longer fully hidden, or the toggle would show `+` over visible rows. Not yet: numbered level buttons drawn in the gutter corner (the same action is on the menu) |
| UX-V05 | Remove duplicates — absent | Data | ✅ | `session_remove_duplicates` compares rows on their *displayed* values (two cells reading `1.50` are the same row even if one is a formula), keeps the first of each, deletes bottom-up so indices stay valid, and returns the count. Confirms first, excludes a detected header, reports how many went |
| UX-FM1 | Conditional formatting: fill-only cell rules; no color scales / data bars / top-bottom / above-avg / duplicates; no Manage Rules; no priority/stop-if-true; font/border effects | Format | ✅ | Complete. Colour scales and data bars landed earlier; this closes the rest. New kinds: **top/bottom N**, **top/bottom N%**, **above/below average**, **duplicate/unique values** — all decided from the whole range, so `session_cells` computes each rule's statistics once per call (`CfStats`: min, max, mean, a presorted top-N cutoff, and value counts built only when a duplicate rule needs them) rather than rescanning per cell. **Priority and stop-if-true** are modelled and honoured: rules evaluate in priority order, and a matching `stopIfTrue` ends the search. **Dxf font effects** (colour, bold) join the fill, and a CF font colour outranks the cell's own. **Manage Rules** dialog lists the rules in evaluation order with reorder / stop / delete. Round-trip tested for all six new kinds plus priority and stopIfTrue |
| UX-FM2 | Format painter — absent | Format | ✅ | Toolbar button picks up the active cell's *resolved* style (`session_copy_style`), so number format, font, fill, borders, alignment and wrap travel together; the next selection is painted. Single click paints once, double-click keeps painting, Escape cancels |
| UX-FM3 | Custom number-format dialog + currency locale/symbol; expose negative/zero/text sections (engine supports) | Format | ✅ | Custom-format dialog with live preview and the negative/zero/text sections landed earlier; the **currency builder** closes it. Picking a symbol, decimals and red-negatives assembles `[$SYM-lcid]#,##0.00` — the locale id is a hex LCID nobody remembers, and the symbol has to sit *inside* the bracket or it is a literal that behaves differently across sections. Eight currencies including the multi-character ones (CHF, A$, C$), all verified to render through the engine's own formatter rather than assumed |
| UX-FM4 | Align: no Justify / Fill / Center-Across; vertical Justify/Distributed folded away | Format | ✅ | `HAlign` and `VAlign` now cover the **full OOXML token set**. This was a fidelity bug, not just a missing menu: `centerContinuous` imported as `center` and vertical `justify` as `bottom`, so opening and saving a file quietly rewrote its alignment — a round-trip test now pins all twelve tokens. Rendering: **Fill** repeats the text across the cell, **Center across selection** centres over the run of cells that carry the mode (not over every empty neighbour — that flung a lone label into the middle of the viewport), **Justify/Distributed** stretch inter-word gaps per wrapped line with Distributed also stretching the last, and **vertical Justify/Distributed** spread wrapped lines down the cell. `session_set_valign` had its own hand-rolled token match that silently dropped the new values; it defers to `VAlign::from_ooxml` now so it cannot drift again |
| UX-FM5 | Merge variants: no Merge & Center, no Merge Across | Format | ✅ | The merge button is a four-verb menu: merge & centre, merge, merge across (each row separately), unmerge. Across counts what *all* the rows would bury and asks once before discarding, keeping UX-M01's rule intact |
| UX-FM6 | Color pickers: no theme colors, no RGB/HSL, no eyedropper; hex rejects 3-digit/rgb() | Format | 🟡 | Picker now offers a **Theme row sourced from the open workbook**, not a stock imitation: the theme palette was parsed at import and then thrown away, so it is now kept on `Workbook::theme_colors` and exposed as `theme_colors()`, falling back to the stock Office scheme for a workbook that never came from a package. Under it sits Excel's tint ladder (±) computed the way OOXML's `tint` shades. The custom field accepts `#abc`, `abc`, `#aabbcc`, `rgb()`/`rgba()` and `hsl()` — verified `#abc`→`AABBCC`, `rgb(255,87,34)`→`FF5722`, `hsl(210,100%,50%)`→`0080FF`; alpha is accepted and dropped, since the model has no alpha channel. Plus the native colour dialog for a full HS/V surface and an eyedropper, shown only where `EyeDropper` exists. **Still amber**: picking a theme colour stores the resolved RGB, so the *theme linkage* is not round-tripped — re-theming the file in Excel will not move these cells. That needs `Style` to hold a slot+tint reference, tracked on the [33](33-FIDELITY-LEDGER.md) styles row |
| UX-FM7 | Named cell styles (Good/Bad/Heading…) — absent | Format | ✅ | Named styles were **dropped entirely**, not merely missing from the UI: import never read `cellStyleXfs` or `cellStyles`, and the writer emitted one anonymous entry with every cell pointing at it — so a "Heading 1" cell came back as merely bold-and-blue and Excel's gallery no longer recognised it. Model gains `Workbook::cell_styles` (`NamedCellStyle` = name + `builtinId` + formatting) and `Style::style_ref` as the `xf/@xfId` link. Import parses both elements and remaps the link; export writes real `cellStyleXfs`/`cellStyles`, **emitting Normal in slot 0** since that is what every unlinked cell's `xfId="0"` resolves to, and remapping links accordingly. UI: Format ▸ Cell styles… gallery previewing each entry in its own look; the workbook's own definitions win over the stock ones, so a file that defines Heading 1 shows *its* Heading 1. Applying replaces rather than merges (keeping the number format), because a leftover fill from the previous style would leave the cell looking like neither |
| UX-FM8 | Indent increase/decrease — model field dead, no UI/API/render | Format | ✅ | `session_adjust_indent` (clamped to Excel's 0–250), two toolbar buttons, and the indent now reaches the editor at all — it was in the model but never emitted — shifting text off its leading edge in both the wrapped and single-line paths |
| UX-FM9 | Text rotation — not even in model | Format | ✅ | `Style::rotation` in OOXML's own `textRotation` encoding (0–90 CCW, 91–180 for value−90 CW, 255 stacked) so the round-trip is lossless and there is no conversion to get backwards; parsed, written (round-trip test), set from a toolbar menu, drawn under a canvas transform, and the row auto-grows to fit — without which the rotation is clipped to a 20 px row and achieves nothing |
| UX-G01 | **Drawn geometry vs engine geometry diverge for auto-grown rows** — `measure()` grows a row for wrapped text, but scroll anchoring (`session_row_at_px`), the frozen-band origin, the scrollbar extent, `ensureVisible` and the row-resize origin all read the engine's un-grown offsets. Symptoms: a hitch at row boundaries while wheel-scrolling, a short scrollbar extent, `ensureVisible` under-scrolling so the selection creeps below the viewport, and a resize drag offset by the accumulated growth | Nav | ✅ | Fixed by making the growth **document-wide** instead of per-frame. Measuring text is the host's job, so the engine cannot know a wrapped row's real height — but measuring only the *visible* rows left every offset past the first grown row wrong. New `session_autofit_candidates` reports every cell on the sheet that could grow its row (wrapped, rotated, or larger-than-default font, excluding rows the workbook sized itself, since an explicit height wins over auto-fit) plus the sheet's default height. The editor measures those once into a sparse row→growth map with prefix sums, and `rowOffsetPx`/`rowAtPx` fold it into every offset — screenY, the frozen-band origin, scroll anchoring, the scrollbar extent, `ensureVisible` and the resize origin all read through them now. The height rule itself was extracted into `neededRowHeight` so the per-frame measure and the growth map cannot diverge. Rebuilt on edit, zoom, column resize, sheet switch and load. The candidate list is capped and says so in the status bar rather than truncating silently |
| UX-A01 | Status bar Min/Max never shown (engine computes them); no separate numeric count | Chrome | ✅ | `session_range_stats` already returned min/max; the bar now folds them across multi-ranges and shows a separate Numbers count when it differs from Count, so a stray text cell in a numeric column is visible |
| UX-A02 | Cell-mode indicator (Ready/Enter/Edit/Point) + persistent bottom status; Count always-on | Chrome | ✅ | A mode chip in the bottom bar. The word tracks the *gesture* that started the edit, not whether the cell was empty — typing over a filled cell is Enter, F2 into it is Edit — and reference picking (mouse or arrows) shows Point |
| UX-A03 | Canvas invisible to AT: no `role=grid`, no `aria-live` active-cell announce | Chrome | 🟡 | `role=grid` + label on the canvas, and a visually-hidden `aria-live` region announcing the active cell, the selection size and the cell's content as it moves (de-duplicated, since re-announcing an identical string is silence in some readers and noise in others). **Remaining**: this is an announcer, not a real accessibility tree — a DOM mirror of the visible cells would be needed for structural navigation |
| UX-A04 | Canvas has no focus ring | Chrome | ✅ | Inset ring on the grid, gated on keyboard navigation: Chrome counts a focusable `<canvas>` as `:focus-visible` even for a mouse click, which would ring the whole grid every time you clicked a cell |
| UX-A05 | Formula bar `fx` decorative; no Insert-Function; no multi-line expand | Chrome | ✅ | `fx` was decoration; it is an **Insert function** button now, opening the searchable catalogue with each entry's signature and summary and dropping the call in with the caret between the parentheses. The catalogue is the eval crate's own dispatch table, so it can never offer a function the engine cannot evaluate. Added an expand caret that gives the formula bar several lines, for formulas that are unreadable on one |
| UX-A06 | Menu bar has no keyboard nav / mnemonics; items lack `role=menuitem` | Chrome | ✅ | `role=menuitem`/`role=menu`, roving tabindex and arrow navigation landed earlier; **Alt mnemonics** close it. Letters are assigned as the first character not already claimed, not blindly the first — File and Format both start with F, so a first-letter rule left Format unreachable *and* advertising File's shortcut in its `aria-keyshortcuts`. Format takes Alt+O, as it does on Windows. The underline appears only while Alt is held (a permanently underlined letter reads as a link) and is cleared on window blur too, since Alt+Tab leaves the key held as far as the document can tell |
| UX-A07 | Toolbar not a roving-tabindex composite (every control a tab stop) | Chrome | ✅ | One tab stop for the whole toolbar (was ~40), arrows/Home/End move within it. Text fields keep their own Left/Right so the caret still works, and every control — including those parked in a collapsed group's flyout — is reset, or opening a flyout would have added stops back |
| UX-A08 | Cell context menu missing Insert note / Format cells / Define name / Filter | Chrome | ✅ | Cell context menu gains Format cells…, Insert note, Define name… and Filter — the things you reach for *from a cell*, previously only on the toolbar or menu bar, which is a long way to go for something the right-click is already asking about |
| UX-NV1 | Zoom (Ctrl+wheel, control, 25–200%) — absent | Nav | ✅ | Applied to the **canvas transform**, not to the geometry: the grid keeps measuring in engine pixels and only the drawing and the viewport it must fill are scaled, so drawn and modelled geometry stay comparable (scaling widths/heights instead would re-create UX-G01). Pointer coordinates divide by zoom; the in-cell editor is a DOM element so it multiplies. Ctrl+wheel, Ctrl+0, View ▸ Zoom presets |
| UX-NV2 | Scrollbar track-click paging | Nav | ✅ | Clicking the scrollbar track pages a viewport toward the click. The track was inert before, so the only way to move a long way was to drag the thumb precisely. Shares the new `clampScroll` with the wheel path |
| UX-NV3 | Tab-run return on Enter (return to Tab-origin column, row+1) | Nav | ✅ | A run of Tabs remembers the column it began in, so Enter returns there and drops a row — what makes tabbing across a record and pressing Enter land at the start of the next one rather than wherever you stopped |
| UX-NV4 | Enter/Tab wrap inside a multi-cell selection | Nav | ✅ | Enter and Tab walk *within* a multi-cell selection and wrap at its edges. The earlier attempt was backed out because `state.sel` is the active cell everywhere else in the file while the block is derived from anchor+sel — so moving `sel` shrank the very selection being walked. `navBlock` holds the block still and `selRect` prefers it, which needed one change rather than renaming ~50 readers |

## P2 — polish

| ID | Item | Domain | Status | Fix |
|----|------|--------|--------|-----|
| UX-P01 | Toolbar file group (New/Open/Download) redundant with File menu — declutter | Chrome | ✅ | Removed group; Open now in header + File menu; Download/New in File menu; new-sheet is the bottom "+" |
| UX-P02 | Ctrl+click deselect toggle; Shift+wheel horizontal; Alt+PageUp/Dn; clamp scroll to extent; Ctrl+Arrow into blank; End-mode; Go-To dialog | Nav | ✅ | Ctrl+click on an already-selected block **removes** it (a mis-click into a multi-range selection could previously only be undone by starting over); Shift+wheel scrolls horizontally, the only way to pan sideways on a one-wheel mouse; Alt+PageUp/PageDown page sideways; and `clampScroll` holds the viewport inside the content extent — wheeling past the end used to park the grid in blank space with the thumb already bottomed out, giving no hint anything had moved |
| UX-P03 | Name Box: accept `A:A`/`1:5`/`Sheet!ref`/comma-multi; NRxNC on keyboard extend; defined-names dropdown | Nav/Chrome | ✅ | Name Box accepts a cell, a range, a whole-column band (`A:C`), a whole-row band (`2:5`), a sheet qualifier (`Sheet2!B7`, which switches sheets first) and a comma-separated list building a multi-range selection. The NRxNC size readout now appears for **keyboard** extends too — it was wired to the mouse only, so a Shift+arrow selection gave no idea how big it had got. A caret lists the workbook's defined names, which were otherwise only reachable by typing one exactly, i.e. by already knowing it exists |
| UX-P04 | Paste-special: arithmetic ops + full dialog (Ctrl+Alt+V) | Editing | ✅ | Paste Special dialog (Ctrl+Alt+V) with what-to-paste, transpose, and the four arithmetic operations. Arithmetic combines the copied number with what is already there; a non-numeric cell on either side is **left alone** rather than coerced to zero, which would quietly turn a label into a number. An empty target uses the operation's identity, so pasting onto blanks behaves like a plain paste, and dividing by zero yields Excel's `#DIV/0!` rather than an infinity the grid cannot render |
| UX-P05 | Undo/redo labels; F2 caret-at-end; inline arrows commit+move (enter mode); Shift+Tab-left while editing; column-value autocomplete | Editing | ✅ | Undo/redo buttons name what they will do, from the engine's own history, so the label always matches the operation on the stack. F2 opens with the caret **at the end** — it exists to amend a value, and selecting the text meant the next keystroke replaced it. Arrow keys commit and move while typing a fresh value (Excel's "enter mode"), gated on `editMode` and the absence of a leading `=` so they still move the caret when amending and still pick references mid-formula. Shift+Tab while editing moves left, which it previously could not |
| UX-P06 | Autofill: growth series, date stepping, Ctrl copy↔series toggle, fill-options popup | Editing | ✅ | Fill gains explicit modes: copy, linear series, **growth** (geometric — continues by ratio, and doubles from a single seed as Excel does), formatting-only and without-formatting. Ctrl while dragging the handle inverts the default, so a series-looking source copies and a plain one becomes a series. A fill-options button appears at the corner of the last fill — a fill has to *guess* between copying and continuing, and this is how you say it guessed wrong, without which the only recourse is undo and a different drag. The same modes are on Edit ▸ Fill: a mode reachable only by dragging is one most people never find. Verified: 2 → 2,4,8,16,32 under growth and 2,3,4,5,6 under series |
| UX-P07 | Font picker searchable + rendered in-face; grow/shrink ladder beyond 72 | Format | ✅ | Custom combobox replaces `<input list=datalist>` (a datalist is filtered by the input's own text, so an auto-populated box offered exactly one font). List comes from the engine (`font_families()` ← `PICKER_FAMILIES`); rows preview in-face, substitutes say so in the tooltip. A▲/A▼ already stepped past 72 |
| UX-P08 | Borders: 13 placements incl. composite bottoms + diagonals; line-color swatch reflects pick | Format | ✅ | Full 17-placement palette including the four diagonals and the composite bottoms; diagonal borders modelled and round-tripped (they were being dropped on save); `double` renders as two parallel lines rather than one thick one. The line-colour pick now survives closing the palette — it was built once, so its marks went stale — and is mirrored onto the toolbar button, so the current colour is visible without opening the menu |
| UX-P09 | Date `[h]/[m]/[s]` elapsed time dropped; English-only month/day | Format | ✅ | Elapsed-time formats landed earlier (and the `[$-409]` misclassification behind them). **Localized month and day names** close it. The language comes from the format code's own `[$-lcid]`, as it does in Excel — not from the reader's machine — so a workbook's dates read the same for everyone, which is the whole point of the locale being in the file. Sub-language is ignored (`[$-807]` Swiss German is still German), a currency symbol before the id still resolves (`[$€-40C]`), and an unrecognised language falls back to English rather than failing. Names are truncated **by character**: `&"décembre"[..3]` would panic mid code point, which is a risk a localized table introduces and an English-only one never had. Covered: fr, de, es, it, pt, nl — the ids that actually appear in `numFmt` codes; the mechanism is complete and adding a language is one table entry |
| UX-P10 | numfmt/align submenu checkmarks; %/comma toggle back to General | Format/Chrome | ✅ | Number and alignment submenus show a tick against what is already applied — a menu that never shows the current state makes you guess. The % and comma toolbar buttons are toggles now: pressing the one already applied returns the cell to General, which was otherwise only reachable by hunting through the number menu |
| UX-P11 | Header right-click selects line (dup of UX-C01); Insert/Delete cells w/ shift; move/cut rows-cols; Unhide All; resize drag tooltip; autofit all-selected + wrap/merge | Data | ✅ | Header right-click already selected the line when it was not covered. Added **Insert/Delete cells with shift** (down/right/up/left) and Unhide-all landed earlier. References are deliberately **not** rewritten for a cell-block shift — that needs the same range-aware rewriting the whole-row ops do, and doing it half-way would be worse than not doing it — so `session_shift_affects_formulas` checks first and the user is told plainly that references will keep pointing at the same addresses. Confirmed in-browser: the warning fires when formulas read the band, and the dependent totals visibly change afterwards, which is exactly what it warned about |
| UX-P12 | Threaded comments (author/time/replies); right-click Insert comment | Data | ✅ | A cell now carries a **thread**, not a note: an opening remark plus replies, each with an author and a timestamp, and a resolved flag. One model type covers both shapes — a plain note is simply a thread with no replies and no timestamp — so the editor never has to ask which kind a cell has, and a note that gets its first reply becomes a thread instead of migrating between representations. Round-trips through the real 2018 parts (`xl/threadedComments/*` + `xl/persons/*`), which are only written when a thread actually needs them, so a plain note still produces the legacy part alone. Two things that had to be got right: the schema is **flat** — replies are siblings of their root linked by `parentId`, not nested — and Excel writes a legacy note *alongside* each thread for older readers, so on import the threaded record replaces the legacy one rather than appearing beside it as a duplicate. GUIDs are derived from the thread's own coordinates rather than drawn at random, or two saves of one workbook would differ. Timestamps come from the host, keeping the engine a pure function of its inputs. UI: right-click says **Insert comment** or **Show comments** depending on what is there, the panel's primary verb switches Save → Reply ("Save" on someone else's thread is ambiguous), hovering shows the whole conversation rather than just the opening line, and the author is asked once and remembered. Fixed alongside: an unsigned note used to come back attributed to "OpenCalc" — an empty author slot is the schema's own way of saying anonymous. 5 round-trip/XML-shape tests; verified in-browser through post → reply → resolve |
| UX-P13 | Sheet tabs: overflow scroll + all-sheets menu; hide/unhide sheet; move-to; protect | Data | ✅ | Hide/unhide landed earlier (it was silent data loss — `<sheet state>` was unmodelled). This closes the rest. **Sheet protection was the same kind of loss**: `<sheetProtection>` was neither read nor written, so a protected sheet came back unprotected, and the xf `<protection>` flags went the same way, unlocking cells the author had locked. Both round-trip now. The element is preserved **attribute-for-attribute** rather than typed: it mixes permission flags with a password hash, salt and algorithm, and a regenerated hash locks the author out of their own sheet while a dropped flag grants a permission they withheld. Protecting from the UI sets only the master flag — this cannot invent a hash, and pretending otherwise would claim a security property it has not got. Tab strip: protected tabs are marked, an all-sheets menu lists every sheet including hidden ones (jumping to a hidden one reveals it, rather than landing somewhere with no tab and no way back), and the active tab scrolls into view once the strip overflows. Move-to is drag-reorder, which was already there |
| UX-P14 | Text-to-columns | Data | ✅ | Text to columns: splits the selected column on comma, tab, semicolon, space or a custom delimiter into the columns to its right. Works on values already in the sheet, so it needs no clipboard or import path. **Formula cells are skipped** — a formula's result is not the user's text, and overwriting it would lose the formula — and the status line says when nothing contained the delimiter rather than reporting a silent no-op as success |
| UX-P15 | Import/export UX: progress + friendly errors; CSV encoding (BOM/UTF-16) fallback; CSV active-sheet-only warning; date/leading-zero fidelity | Chrome/Fidelity | ✅ | Opening a file now says so and, on failure, says what went wrong in terms of the file rather than the exception. Text is decoded before parsing: UTF-16 LE/BE by BOM, UTF-8 BOM stripped, and BOM-less UTF-16 sniffed from its zero bytes — verified on a UTF-16LE CSV that previously imported as mojibake. Delimited export writes a UTF-8 BOM (without it Excel renders `Café` as `CafÃ©`) and warns before dropping the other sheets, since `.csv` holds one. **Field typing was the real find here**: `007` imported as `7` and `2024-03-05` was never a date at all, so the crate's own documented "parse → write → parse is a fixed point" invariant was false. Padding zeros now mark an identifier and stay text, and ISO 8601 dates/times become serials carrying a matching format. Locale-ambiguous `3/5/2024` deliberately stays text — nothing in the file says whether that is March or May, and guessing would make identical bytes import differently on two machines. The same parser backs typing into a cell, so a typed date and an imported one agree, and a date cell edits as `2024-03-05` rather than `45356`; an existing `dd/mm/yyyy` on the column survives a retype. Verified in-browser: `=B3-B2` on two imported dates gives 660 days. Also fixed here: opening hung whenever the tab was in the background, because the "let the status paint" await was on `requestAnimationFrame`, which does not fire off-screen. 6 io tests + 1 session test |
| UX-P16 | Format Cells dialog (Ctrl+1) consolidating number/font/border/fill/align | Chrome | ✅ | Format Cells (Ctrl+1) with Number, Font, Alignment, Fill and Border tabs. Each page collects a setter and they are applied together on OK, so the whole dialog is one visible change rather than a dozen. Needed `session_set_font_flags`: the toolbar's toggles are *relative*, which a dialog cannot use — it shows checkboxes with definite states and has to apply them as such |
| UX-P17 | `prefers-reduced-motion`; freeze-line theming; ~~collapsed-flyout self-dismiss on input click~~ (fixed with UX-P07) | Chrome | ✅ | `prefers-reduced-motion` honoured in one place for all CSS transitions, plus the marching-ants crawl, which is a canvas timer rather than a transition and had to be stopped in JS (the dashed outline still marks the copy source, standing still). `--freeze-line` is a real theme token now with a light and a dark value — the draw site already read `colors.freezeLine` but nothing ever populated it, so the hardcoded fallback was always what showed. Collapsed-flyout self-dismiss was fixed earlier under UX-P07 |

---

## Working order

1. **UX-P01** (toolbar declutter — user request, quick).
2. **UX-F01** formula-editor controller refactor → then the formula cluster UX-F02/03/06/07/E02 (highest user value; the flagged gap).
3. P0 correctness: UX-M01 merge, UX-N01/02/03 number formats, UX-D01 sort, UX-C01 header menu, UX-E01 multi-line.
4. P1 by domain value, then P2.

Each item: implement → verify in-browser → commit → flip status here before starting the next.
