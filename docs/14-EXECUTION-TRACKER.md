# 14 — Execution tracker

**The only tracker.** Every unit of work has one row here, whatever kind of work
it is. Closed rows move to [14a](14a-ARCHIVE-CLOSED-WORK.md); design reasoning
lives in the commit that made the change.

**Sweep the closed rows out when they crowd the open ones.** This has now been
done twice — 244 rows the first time, 245 the second — and both times they were
around 85% of the file. That is the normal rate, not a lapse: a row is closed by
the work that closes it, and nothing moves it afterwards. So this is a recurring
chore, and the useful signal is the ratio rather than the count. A tracker where
the open work is one row in seven is one people stop reading, and a tracker
people stop reading is how nine already-fixed rows sat open for weeks
(`DOC-047`).

Rows are moved **verbatim and never merged**. They are cited by id from `docs/12`,
from code comments and from each other, and `check-doc-references` fails on a
citation to an id no tracker defines — so merging two closed rows would turn
every citation of the loser into a dangling reference.

Four more were deleted on 2026-08-18: the support matrix (18), the competitive
parity analysis (46), the editor parity tracker (45) and the fidelity tracker
(52). An audit of 46 settled it — of roughly a hundred rows checked against the
code, **79 were done, 11 partial and 3 absent**, while the document marked most
of them "Todo". Merge cells was recorded as `Partial, Edit ✗ Render ✗` against
three engine operations, a toolbar menu and a browser test; the name box was
described as "a read-only `<span>` today" and is an `<input>` handling five
reference syntaxes; pivot tables were "Todo (preserve-only)" against a
1,386-line pivot engine.

The drift ran one way — pessimistic — so anyone reading it concluded the product
was far less finished than it is, including when somebody asked "do we have
X?". That is worse than no document, and it is why the rule below is a rule.

[53](53-FEATURE-CORRECTNESS-TRACKER.md) survives because it is **generated from
the code** by `tools/feature-audit/inventory.py` rather than maintained by hand,
so it cannot drift in the way these did.

There were thirteen overlapping tracking documents. They disagreed with each
other about what was done, what was severe, and in two cases about what an id
even meant — `FID-13` named both a sheet-quoting defect and a `sheetView`
construct, so `git log --grep FID-13` returned two unrelated changes. A tracker
nobody can trust is worse than no tracker, because work gets planned from it.

## How a row works

```
| ID | Title | St | Sev | Mechanism | Gate |
```

- **Mechanism** — the cause, in one line. Not the story of finding it.
- **Gate** — the named test that fails if it regresses. If you cannot name one,
  the row is not `Done`.
- **St** — `Open`, `WIP`, `Partial`, `Blocked`, `Designed`, `Done`, `Dropped`.
  Nothing else.
- **Sev** — by what reaches the *file*, not by what shows on screen. Silent
  corruption is P0 however small it looks; a missing menu item is P3 however
  loud.

Rows stay short on purpose. The long account of a defect — what made it
invisible, which two wrong turns preceded the fix — belongs in the commit
message, where `git blame` finds it beside the diff and where it costs nothing
to keep.

**Before adding a row, run `tools/check-tracker-ids.py`.** Ids are never reused;
it is a pre-commit hook and a CI gate because five rows were added without
reading the id space above them.

## P0 — silent corruption, or nothing ships until it is fixed

| ID | Title | St | Sev | Mechanism | Gate |
| --- | --- | --- | --- | --- | --- |

## P1 — wrong answers, lost work, or a promise the code does not keep

| ID | Title | St | Sev | Mechanism | Gate |
| --- | --- | --- | --- | --- | --- |
| DEP-08 | No Kubernetes manifests or Helm chart | Blocked | P1 | The design targets k8s ([59](59-COLLABORATION-SERVICE-STACK.md)) and ships nothing for it. DEP-04/05/06 are done, so it is unblocked by code and blocked by tooling: no cluster, no `helm`, no `kind` on any machine this has run on, and a chart that has never been rendered is unverified code. | A chart deploys and a pod restart loses nothing — needs a real cluster, not a dry run |

## P2 / P3

| ID | Title | St | Sev | Mechanism | Gate |
| --- | --- | --- | --- | --- | --- |
| PIV-02 | Pivots export as cells, not as a live pivot | Partial | P2 | **The row said "blocked on validating output against Excel"; it was also unimplemented.** Export only ever *retained* an imported pivot's part — a pivot created here reached the file as whatever values its last refresh wrote, so Excel opened a block of numbers with no field list, no layout to change and nothing to refresh. Now written properly: a `pivotCacheDefinition` with `saveData="0" refreshOnLoad="1"` — the decided route, and the honest one, because this engine recomputes a pivot from its source anyway and a records part would publish a second copy of the truth that nothing keeps up to date — plus a `pivotTableDefinition` carrying the layout, the table's own `.rels`, the workbook's `<pivotCaches>` entry and relationship, and both content-type overrides. Field names come from the source's header row, which is what `source_column` indexes into; a blank header falls back to a positional name, because Excel refuses an empty one and two fields sharing a name is a different refusal. **A real bug fell out of the wiring**: a sheet whose *only* attachment is a pivot had its `.rels` part skipped altogether — the emit condition listed comments, hyperlinks, tables, retained rels and charts — so the pivot was written and then unreachable, which is the one way to be worse than not writing it. **Still Partial, and deliberately so.** The acceptance criterion is *Excel* opening it as a pivot, and no oracle here can answer that: the round-trip below goes through this project's own importer, which was written against real Excel files, so a shape it accepts is a shape Excel writes — which is evidence, not proof. Opening one generated file in Excel closes this. **A foreign reader has now opened one, which the row had been waiting for without anybody checking what was available.** Excel is not on this machine; LibreOffice is, it reads OOXML pivots, and `--validate-package` already drives it. The pivot corpus had **no pivot in it**, so the writer had never been handed to any reader but our own. Added one, and LibreOffice does not merely accept the package — it **reads the pivot as a pivot and writes back a richer one**. `name="Sales"`, `rowFields`, `dataField`, `subtotal="sum"` all survive, and it materialises a `pivotCacheRecords1.xml` of **4 records** from our `saveData="0" refreshOnLoad="1"` cache — which it could only do by resolving the source range we named. A reader that had failed to understand the definition would have dropped the parts, as it drops anything it cannot read. **Still Partial, and the remaining gap is now precise rather than total.** The row's acceptance names *Excel*, and LibreOffice's OOXML reader is not Excel's — it is tolerant where Excel is strict, and Excel's repair prompt cannot be tested without Excel. What was unknown before was whether *any* foreign reader could open it; that is now answered. What is unknown is only whether Excel's stricter reader agrees. | `--validate-package` carries a pivot through a LibreOffice re-save; opening one generated file in Excel closes the row |
| PIV-11 | Two pivot shapes are unrepresentable in OOXML and are written anyway | Open | P2 | Measured by the slice-B worker, both pre-existing and neither a writer defect. An **ungrouped source column used on two axes** writes one cache field named by two axis lists — `<colFields><field x="1"/></colFields>` beside `<pageFields><pageField fld="1"/></pageFields>` with a single `<pivotField axis="axisCol">`. And **two ungrouped axis fields on one source column** write `<rowFields><field x="0"/><field x="0"/></rowFields>`, which LibreOffice answers by dropping the whole row axis. Neither is expressible, so neither wants a writer fix — they want a refusal at `session_set_pivot`, where the model is still in reach. | `session_set_pivot` refuses both, naming which shape and why |
| PIV-12 | A measure's number format never reaches the file | Done | P2 | `<cacheField @numFmtId>` and `<dataField @numFmtId>` are both written `0` or absent, because the pivot module has no allocator into `styles.xml`. Pre-existing — a plain measure has the same gap — and it becomes visible when `docs/85` §5.1's `0.00%` default lands: our report will show a percentage and Excel's rebuild will show a raw ratio, which is the two-answers-from-one-file failure the whole slice-B row exists to prevent. | **Two parts had to agree, and a third had to exist at all.** `pivot::number_formats(workbook)` is the single table: `styles_xml` seeds its `numFmts` with it before adding the codes cell styles need, and `table_xml` resolves a measure's id as a position in the same list — one function called twice, so the id a pivot writes and the id the styles part defines cannot drift. Seeded rather than appended, which keeps the position stable when a cell style happens to use the same code. **The part that only appeared once it was measured:** `styles.xml` is written when there are cell styles or dxfs, and a workbook that is nothing but a pivot has neither — so the first green-looking version named `numFmtId=164` in a package that defined no formats at all. That is a worse file than one with no format, because Excel repairs it silently, which is `IO-12`'s whole subject. A pivot format is now a third reason to write the part. The test asserts **both** halves — the id in the pivot and the code in `styles.xml` for that id — because checking only the first passes against exactly the broken package above. Red without the writer: *the measure was written with no numFmtId, so Excel shows General*. Found by the test rather than by reading: the missing `styles.xml` surfaced as `FileNotFound`, and an assertion listing the parts is what named it. |
| PIV-13 | Three obligations slice D inherits, recorded before they are forgotten | Open | P3 | Named by the slice-B worker so they do not have to be rediscovered. **`PivotGroup` cannot derive `Eq`** (it holds `f64`), while `PivotAxisField`, `PivotFilterField` and `PivotTable` all do — D must drop `Eq` from those three when it adds `group`; `Sheet` is only `PartialEq`, so it stops there. **`PivotBaseItem::PREVIOUS`/`NEXT` are unverified numbers** — `UNSET` is pinned by the schema and by an import fixture, those two by neither; they are unreachable in the first release and must be checked against a file Excel wrote before `runTotal` ships. **An explicit start/end on a date group is not written** — `@startDate`/`@endDate` are `xsd:dateTime` and `serial_to_ymd` lives in a crate the exporter does not depend on, so D either refuses explicit bounds or this becomes its own row. | Each is a test in slice D |
| PIV-07 | Calculated fields, Show Values As and date grouping — one design, one wire bump | Partial | P1 | **Design settled in [85](85-PIVOT-DERIVED-FIELDS-AND-GROUPING.md); slice A built.** **This row's central premise was wrong and the measurement says so:** it called the break the *quiet* kind, and all four pivot types carry `deny_unknown_fields` where `ChartView` does not — so an old peer answers `unknown field 'calculated'` and **refuses the whole message** rather than dropping it. Loud, not quiet. The conclusion (bump, now 8 -> 9 since `CHT-07` took 8) is unchanged; the mechanism is the opposite, which is better evidence the rule is real than "the same argument twice" would have been, and it means the three features can land in three releases with no quiet-loss window. The row's "field names bound to sibling measures" is also half wrong: Excel binds to sibling **fields, summed**. **Show Values As honours five of nine**, and the line is drawn by the schema rather than by taste — those five are exactly the ones whose base is a truncation of the cell's own key and therefore already in the accumulator map; four need `@baseField`/`@baseItem`, and six more menu entries are `x14:dataField/@pivotShowAs`, not in `<dataField>` at all. **Both open questions were answered by the product owner on 2026-08-30 and are recorded in [85](85-PIVOT-DERIVED-FIELDS-AND-GROUPING.md) §12**: a calculated field's operands bind **Excel's** way ("do what our competitors use" — and only Excel's binding survives the round trip, since `<cacheField @formula>` is what we write); and `C`/`D`/`E` ship as **three releases**, keeping each wire change independently revertible. | Slice A (`casual-calc-eval` only) fixes a live defect nobody had recorded — a pivot over a date column labelled its rows `45306`, the serial, as a string. Seen red: 7 tests, `left: ["45306", ...]` against `right: ["2024-01-15", ...]`. The worker said plainly that an 8th test passes either way and is a compatibility guard, not evidence. **Blocked on `B` before `C`/`D`/`E` may land**: the exporter writes no `showDataAs`, no `<fieldGroup>` and no `@formula` against a cache with `refreshOnLoad="1"`, so an authored pivot would reopen in Excel as raw sums under a caption reading `% of Grand Total` — `PIV-05`'s P0 again |
| PIV-09 | A pivot's column-width estimate ignores `date1904` | Open | P3 | Found by the `PIV-07` worker and named in `docs/85` rather than fixed silently. `plan`'s width estimate calls `format_number` unconditionally, so a 1904-epoch workbook's date column is measured against the wrong text. Cosmetic — it is a *width*, not a value — and pre-existing. | A width assertion on a 1904 workbook |
| P1C-003 | Text shaping is wired but not drawn | Partial | P2 | The shaper (`harfrust` since `DEP-14`) is behind a feature and correct; `draw_glyphs` still walks per `char`. Bundled fonts cover Latin and Hebrew only — other scripts need a font decision, not a shaper. **Re-measured 2026-08-30: `draw_glyphs` no longer walks per `char` unconditionally.** It has a shaped arm (`crates/casual-calc-render/src/lib.rs`, `#[cfg(feature = "shaping")]`) taken whenever `single_face_for` finds one face covering the whole run; `shape::run` returns glyphs in visual order and `hebrew_comes_back_in_visual_order_which_is_the_reverse_of_memory_order` asserts it against a naive `cmap` lookup rather than fixed ids. The per-`char` loop is now the deliberate fall-through for a run spanning two faces — shaping is defined against a single face, and shaping each part separately would place them by two different sets of rules. **So the row's original mechanism no longer reproduces, and what keeps it `Partial` is its second sentence, not its first**: the bundled families cover Latin and Hebrew and not Arabic, Devanagari, Thai or CJK, which needs a font decision. Shaping is also off in the WebAssembly build by decision (ADR-018) because the browser shapes the text itself, so the browser editor is unaffected either way. | A Hebrew run draws right-to-left |
| TAURI-006 | Shipping the desktop app: a design before six pieces of it are built separately | Designed | P1 | [86](86-DESKTOP-RELEASE-IDENTITY-SETTINGS-AND-UPDATES.md). **My brief's central premise was wrong and the note says so: a profile is *not* the prerequisite for desktop collaboration — the token is.** `docs/72` states "Identity is the host's, never the client's"; `presence.rs:48` says the display name comes from the token; `collab.js:543` says presence "carries no identity — the server takes that from the token, because presence is the one surface where a claimed name would be believed". A desktop profile name cannot reach `who.name` without deleting a rule three files independently keep. What is actually broken is narrower: `MODE_PRESETS.desktop` has `canShare: true` while a desktop user has no issuer. Two more corrections: **file associations today would open a blank window** (`main.rs` reads no `env::args()` and handles no `RunEvent::Opened`), and **a release must not be marked prerelease** because `releases/latest/download` skips prereleases — copying `release-sdk.yml`'s `--prerelease` would 404 the update manifest and kill the updater silently. **Both open questions answered by the product owner**: the profile is local user information only — name, timezone, preferences, no account; and the desktop application **works offline**, so nothing may wait on the network and an absent network is a normal state rather than an error. | The note's own strongest argument is that **the updater is what rescues an unsigned distribution**: updater-installed bytes are written by our own process, so no quarantine and no Mark-of-the-Web — the *damaged* dialog is a first-install cost, not a per-release one. Eight rows in build order, 1 and 7 sharing nothing and unblocking the rest |
| CI-029 | A worker launched before its predecessor merges reports confident, stale corrections | Open | P2 | **My error, twice in one round.** An agent's worktree branches at launch, so merging its predecessor mid-flight leaves its base behind — and the reports that come back state the stale view as a finding, in the same voice as a real one. The design worker reported that `TAURI-006`/`-007`/`-008` "do not exist" and rewrote two paragraphs around their absence; the touch worker reported that touch range selection "is still open" and declined to build on it. Both had branched one merge earlier, and both were verified wrong in a single command on `main`. **What makes it expensive:** a stale correction is indistinguishable from a real one — this round produced several genuine premise corrections, which is exactly what makes the false ones costly. The fix is the orchestrator's: state the base commit in the brief, or re-base a worker when its predecessor lands. | A brief carries the commit it was written against, and a report states the base it measured |
| CI-031 | A merge result is the one tree no gate ever checks before it becomes main | Partial | P1 | **Main was red and both parents were green, which is the whole finding.** `UX-CHR-01` and `UX-CHR-02` each ended up with two rows, one Open and one Done: #222 marked them Done, #223 had branched before that and inserted `UX-CHR-03`..`09` beside them, and the merge kept both copies because a *moved* row and an *edited* row are not the same line to git. Neither branch was wrong and neither could have been told so. This is the third id clash on main and the first that no existing gate could have caught — `check-tracker-ids.py` runs as a pre-commit hook and on every pull request, and **both parents passed it individually**. Every gate here runs on a *commit somebody wrote*; the merge commit is written by GitHub, and nothing inspects it until the next push, by which time it is already main. The same shape as the recurring failure this project keeps meeting: the instrument agrees with the code because it enters through the same door. Note the interaction with `CI-030` — main's own CI would have reported this, about two hours late. Candidate fix: run the gates on `refs/heads/main` after merge as a required status, or have the merge queue test the *merge result* rather than the branch. | **Detection landed; prevention is a repository setting.** First, a correction to the diagnosis above: `ci.yml` pins no `ref`, so `actions/checkout` already resolves `refs/pull/N/merge` and pull requests *do* test a merge result. The gap is narrower and worse — that merge is against main **as it was when the run started**, and nothing re-tests when main moves underneath, which is precisely how two independently-green branches produced the clash. `post-merge.yml` runs all 24 gates on `refs/heads/main` after every merge; they take **2.3s** and need no cargo build, so the answer arrives in under a minute where `CI-030` measured `ci.yml` taking about two hours under queue saturation. Proved on the artefact this row is about: duplicating a tracker row the way a merge does gives `tracker ids are not unique: TAURI-020 defined at …:105, …:106`, `exit=1`, where each parent passes. **The first draft of that workflow was wrong and a gate caught it**: it used one concurrency group for all of main with `cancel-in-progress: true`, on the reasoning that a newer merge makes an older answer irrelevant. `check-workflow-concurrency.py` refused it — each merge's result is the record of whether main was releasable *at that commit*, so cancelling discards the very answer the job exists to give. **Still Partial:** this is detection, not prevention. `ci.yml` now accepts `merge_group`, so enabling a merge queue in the repository settings makes the gates run on the queued merge result *before* it becomes main. That switch is not a file in this repository and the row stays open until it is thrown. |
| DOC-051 | `docs/12` states several things that are no longer true | Open | P2 | Found by [87](87-THE-DESKTOP-DIMENSION.md) while checking what it should not duplicate. `docs/12:498` says "Absent: PDF export … grep returns only comments" — `IO-03` is `Partial` and `pdf.rs` is a real paginator and writer verified against poppler; the live gap is that nothing wires it to the shell. `UX-DESK-01` is cited "(Open, P1)" and is Done; `UX-DLG-02` is cited as open three times and is Done; `SAVE-03` is described as open and is Done. **This is the drift `docs/14`'s own preamble was written about**, in the document that ranks what to build next — a stale competitive analysis is worse than none, because it is what a decision gets made against. | A citation gate that reads a cited row's status, or a pass that re-checks every claim against the tracker |
| DOC-052 | Architectural invariants live in commit messages, where nobody finds them | Open | P2 | Raised by external review and confirmed before being agreed with ([90](90-EXTERNAL-REVIEW-RESPONSE.md)). `AGENTS.md` puts long reasoning beside the diff, which is right for *why this code is like this* and wrong for *what may never be done* — the second is needed by somebody who is not reading that diff. Measured: the rule "an interned id is replica-local" appeared in **zero of twenty-four ADRs** and had been rediscovered as a defect three times (`COL-12`, `COL-62`, and designed around in `HIST-02`). `ADR-025` closes that one. Two more are still only in commits: **the host owns time and randomness** — the engine reads no clock, which is why `CALC-VOL-01` went unnoticed as long as it did — and **when a wire change forces `PROTOCOL_VERSION` up**, which `ADR-024` states for the op set but not for the `deny_unknown_fields` rule that decided `COL-62` and `HIST-02`. The working split adopted from the review: a commit carries discovery, evidence and implementation detail; an ADR carries invariant, decision, tradeoff and future constraint. | Not yet fixed. Whether a reader who has never seen the commit log can find an invariant is not something a gate can assert, so the measure is the count: two named invariants with no ADR |
| DOC-053 | The README understates the repository, and 1.0 has no written precondition | Open | P2 | From the same review. Two stars and no forks against hundreds of tests, a Rust engine, collaboration, WOPI, three published SDK packages and desktop builds for three platforms — discoverability lags engineering. Wanted: a capability list a reader takes in within fifteen seconds, and the architecture diagram `ADR-022` and `ADR-023` already decide but nobody has drawn. **The diagram is a drawing, not a new architectural exercise** — writing those decisions a second time in prose would create two statements of one fact, which is what `REL-04`, `REL-05` and `check-theme-tokens` exist to prevent. Second half: the 1.0 preconditions are policy and unwritten. Writing them down — external integrations, corpus families, stable command/event/serialization/access-control APIs, worker-ready architecture — makes "not yet" a measurable position rather than a feeling. | Not yet fixed |
| IO-12 | A file Excel silently repairs passes every check here | Open | P1 | The useful half of the review's compatibility proposal. The outcome vocabulary — `PASS`, `VISUAL_DIFF`, `SEMANTIC_DIFF`, `REPAIRED_BY_EXCEL`, `FEATURE_DROPPED`, `CORRUPTED` — is better than the boolean in use, and **`REPAIRED_BY_EXCEL` is the one that matters**: a file Excel opens *after silently repairing it* passes everything here today and is not a pass, and nothing in this repository can say so. **The proposed `compat/excel-2016/`…`excel-365/` tree is refused**, with the reasoning in [90](90-EXTERNAL-REVIEW-RESPONSE.md): running Excel needs a licence per version, a Windows runner with Office installed, and automation of an application with no supported headless mode — so such a directory would be filled by hand and then trusted *more* than an honest "not tried in Excel", because it has a folder. That is the exact failure this repository keeps catching in itself: a green check answering a narrower question than it prints. Built instead: the six classes applied to the LibreOffice oracle, which already runs unattended in three modes, plus a manual Excel checklist **named as manual**, carrying the version and the date it was last exercised so that it goes stale visibly. | Not yet fixed. The first case to classify is a pivot: `PIV-*` already refuses to call itself complete until Excel accepts one without repair, which is `REPAIRED_BY_EXCEL` waiting for a name |
| TAURI-016 | Six native accelerators shadow the chords that mean something else mid-edit | Done | P1 | **Measured, not guessed**: the native menu claims **twenty** accelerators, listed out of the live `menuModel()`, and each is consumed *before the webview sees the key*. Six of them mean something different inside a cell editor, which is where Excel and this diverge: mid-edit `Cmd+C` copies the **selected text**, `Cmd+Z` undoes **typing**, `Cmd+A` selects the text in the cell. On the desktop each ran the *document* command instead — copying cells while somebody was copying part of a formula, undoing a cell edit while they were undoing a keystroke. `edit.undo`, `edit.redo`, `edit.cut`, `edit.copy`, `edit.paste` and `edit.select-all` join `insert.table` and `formula.autosum` in the released set. **A test asserted the opposite for `edit.undo` and I wrote it**: the reasoning was "a person still expects undo to work", which is true and is not the question — the document undo firing mid-edit throws away a keystroke they wanted and a cell edit they did not. The chords that mean the *same* thing mid-edit keep theirs, and that is asserted too: releasing the whole menu during an edit is a different bug, and Save is the one people press *because* they have been typing. | `the_chords_that_mean_something_else_mid_edit_are_released` and `a_chord_that_means_the_same_thing_mid_edit_keeps_its_accelerator`, the second so a later widening cannot quietly take Save away. 58 desktop tests, clippy and fmt pass. **Not verified in a running desktop window** — the same limit `TAURI-012` had, and the reason a proposal to remove the mechanism was raised and then closed once running the app showed it was not the cause of anything; what is established is that the released set is now the right set |
| UX-EDIT-04 | ~~Ctrl+A during a cell edit selects neither the text nor the cells~~ — it was the probe | Dropped | P2 | **Withdrawn: there was no defect, and the row was mine.** The report said `Ctrl+A` during a cell edit selected 0 characters and left `selKind` at `cells`. It does — **and so does a plain `<textarea>` on a blank page**, which is the control I should have run before filing anything. On macOS the select-all chord is `Cmd+A`; Playwright sends `Control+a` literally, so the browser correctly did nothing with a chord that means nothing there. With the platform's own modifier the editor is exactly right: `Cmd+A` mid-edit selects all six characters, and `Cmd+Z` takes `abcdef` to `abcde` while the document is untouched — Excel's behaviour in both cases. Kept as a `Dropped` row rather than deleted, because the mistake is the reusable part: **a probe that reports a platform's own key handling as an application defect will do it again**, and the control that catches it costs one line. It also strengthens `TAURI-016` rather than weakening it — that row releases these accelerators on the desktop so the webview receives them, and this establishes that the webview then does the right thing, which was assumed there and is measured here. | The control: `Meta+A` in a plain `<textarea>` selects 6 of 6, `Control+a` selects 0. Then in the editor, mid-edit `Meta+A` selects 6 and `Meta+Z` undoes one character with the document unchanged
| REL-06 | The download page made every visitor work out which row was theirs | Done | P2 | `REL-02` built three platform cards and `REL-03` gave each the step its operating system needs, which is the right *reference* and the wrong first thing to meet: somebody who came to download does not want to identify their own platform, and on a phone the three cards stack so the answer is below the fold. The visitor's platform is resolved and promoted above the cards, with its own button, its own note, and a link to its own bypass section. **The three cards stay** — detection is a guess, and a guess that hides the alternatives is worse than no guess. It is also a progressive enhancement in the strict sense: the cards are real links that work with scripting off, and the script only *reorders* what is already on the page. A phone or tablet gets nothing rather than a desktop installer, whatever its user-agent claims. Apple Silicon cannot be told from Intel in a browser, so the macOS note says what the **build** is rather than pretending to know the machine. The landing page gained a download button beside "Open the editor", since the nav link was the only route. | Driven with four user-agent strings: macOS resolves to the `.dmg` and `#mac`, Windows to the installer and `#windows`, Linux to the **AppImage** and `#linux` — the AppImage rather than the `.deb` because that is the one that works on 22.04 — and an iPhone shows nothing at all
| REL-05 | `@opencalc/react` pinned a sibling at the old version, and nothing checked | Done | P1 | **Found one edit before it would have happened.** Three packages publish together from one tag, and `release-sdk.yml` compares that tag against **one** of them — `sheet`. Nothing checked the other two, and nothing checked the version `@opencalc/react` pins for `@opencalc/sheet`. That pin is the dangerous one: bumping the three `version` fields to `0.0.1` and leaving the dependency at `0.0.0` publishes `react@0.0.1` depending on `sheet@0.0.0` — **npm resolves that rather than refusing it**, so nothing fails, no job goes red, and a consumer installs two copies of one package at two versions. It would have shipped in the `0.0.1` I was preparing when I read the manifests to bump them. The same shape as `REL-04` one component over: a fact written down four times with a check on one of them. | `tools/check-sdk-versions.py`, red in both directions: "the SDK packages do not agree on a version: engine 0.0.2, react 0.0.1, sheet 0.0.1", and "an SDK package pins a sibling at a different version: react dependencies.@opencalc/sheet = 0.0.0, not 0.0.1"
| REL-04 | The desktop version is stated twice and nothing compared the two | Done | P2 | Found while preparing `0.0.1`. `desktop/tauri.conf.json` names the version the installer filename and the release notes carry; `desktop/Cargo.toml` names the one the binary reports about itself. They are **one fact written down twice**, which is the shape this repository keeps being bitten by — `UX-CHR-03` found a hand-written `v0.0.0` in the About dialog that was right only by coincidence, and `check-theme-tokens` exists because a published list could drift from the stylesheet it describes. A mismatch here stays invisible until somebody compares an installer's name with what the application says about itself, which is exactly the moment a bug report becomes impossible to place. `check-desktop-build.py` reads both and refuses a disagreement. | Seen red by bumping one and not the other: "the desktop version is stated twice and the two disagree: tauri.conf.json 0.0.1 / Cargo.toml 0.0.2", exit 1. The success line was **moved below the new check** in the same change — it had printed `ok` and then failed, which is a gate saying two things at once
| PERF-15 | The profiler's own self-check carried the flaky wall-clock assertion it forbids | Done | P1 | **`browser-smoke` went red on main: `a draw of a 60x12 sheet measured 4.10ms`, against a 4ms bound.** The header of `frame-profile.selfcheck.spec.mjs` promises the opposite in as many words — *"Neither can fail because a runner was slow"* — and cites `editor.frame-budget.spec.mjs` on why a wall-clock assertion on a shared machine is a flaky test that gets deleted. That reasoning is right, and this file had stopped keeping it. The bound was `HALF`, which is `BURN_MS / 2`, and `BURN_MS` is the injected busy-wait — a number with nothing to do with vsync. It was sound for the `idle` probe, which is empty and costs nothing on any machine. It was applied unchanged to the `draw` probe, which renders 720 cells, so its cost **is** a property of the machine. The file's own comment asserted that nothing lands between 4ms and 8ms; a contended runner landed at 4.10ms, which falsifies it. **What made it invisible:** the two probes are one line apart and read as a pair, so a constant that is a self-consistency threshold for one looks like a self-consistency threshold for the other. `PERF-13`'s actual signature is the report *equalling* the display period, and that is what a draw has to be distinguishable from. | Draw bound derived from the display period measured on the machine (`max(period * 0.75, HALF)`), not from `BURN_MS`. Proved by mutating `measureCalls` to `PERF-13`'s exact behaviour — `Math.max(elapsed, 16.7)`, the compositor absorbing anything under vsync — and then neutralising the `idle` assertion so the draw bound was the only thing under test: `a draw … measured 16.70ms against a display period of 16.70ms, at or past 12.52ms`. Real draws now have 3x headroom (4.10 against 12.52) and `PERF-13` still fails by 4ms. |
| REL-07 | The signature check I added failed the release it was meant to protect | Done | P1 | **`desktop-v0.0.1` failed on macOS, and the build was fine.** `REL-02` added a step asserting the app carries a signature Gatekeeper can validate — because `codesign --force` exits 0 whether or not the result verifies, so "it signed" was not the question. The step looked for the `.app` on disk and reported `no .app was produced`. The log directly above it reads `Signing …OpenCalc.app`, `replacing existing signature`, then **`Cleaning …OpenCalc.app`**: the dmg bundler removes the `.app` once it has been wrapped. So the check ran after its subject had been tidied away, and reported a missing *bundle* as a missing *build*. **It had never run before the release.** The dry run that verified this pipeline was executed before `REL-02` merged, so the step went straight from being written to gating a tag — which is the same mistake `TAURI-012` made in a different place, shipping a native path no test had exercised. Fixed by mounting the `.dmg` and verifying the app inside it, which is also the more honest check: the `.app` on disk is an intermediate, the copy inside the image is what somebody downloads, and nothing had ever confirmed the two were the same. | `hdiutil attach` into a temporary mountpoint with a `trap` detach, so a failing `codesign` cannot leave the image mounted for the rest of the job. Proved by the next tagged build rather than by argument — the previous one is the red
| TAURI-020 | The desktop bundle was built on three platforms and started on none | Done | P1 | **Three defects shipped from this shell in one session, and no test in the repository could have caught any of them.** `TAURI-017` was a desktop entry the launcher would not use, `TAURI-018` a bridge method the editor never called for any field but a cell, `TAURI-019` a clipboard the webview is refused. All three live in the gap between the Rust side and the page: unreachable by a browser test, because there is no shell; unreachable by a Rust test, because there is no page. And the `desktop` job builds the bundle on macOS, Windows and Linux without ever **starting** it — `A bundle was actually produced` looks for the artefact on disk, which is exactly the check that cannot notice a binary that opens to nothing. The pattern is `TAURI-001` one level up: there, nothing built the shell; here, nothing runs it. | `--self-check`: the binary loads its own page, asserts the bridge exists with all five methods, round-trips the system clipboard through it, and exits. Run under `xvfb` in the existing Linux desktop job, so no CI job is added. Linux only, and said so in the step rather than left to be inferred — the macOS and Windows runners have no session a GUI process can attach to, and a check that quietly does nothing on two platforms of three is worse than one that runs on one. **The first version was itself the bug it gates against**: it used `AppHandle::exit(1)`, which unwinds the event loop but leaves the process status at 0, so it printed `SELF-CHECK FAILED …` and the caller saw success. Found by regressing the clipboard on purpose and reading the *exit code* rather than the message — `CI-032` again, in a check written to prevent exactly this. Now `std::process::exit`, proved both ways: clipboard bridge reverted to `navigator.clipboard` gives `RED exit=1` with the platform's refusal quoted, restored gives `GREEN exit=0`. |
| TAURI-019 | The system clipboard was refused to the desktop build, in both directions | Done | P1 | **Copy out of the desktop application reached no other application, and paste in from one silently pasted something else.** Probed in the running shell rather than reasoned about, and the first hypothesis — a missing user gesture, since the native menu consumes the keystroke before the webview sees it — was **wrong**: `activation=true` and both calls still failed with `NotAllowedError`. WKWebView and WebKitGTK decline the async Clipboard API to embedded content whatever the activation state, so no arrangement of the call site could have fixed it. **The paste half was the worse of the two.** `doPaste` falls back to `navigator.clipboard.readText()` and, on failure, leaves `osText` as `""` — which the next line reads as *the OS clipboard is unreadable, use our own snapshot*. So a copy in Excel followed by a paste in OpenCalc pasted whatever OpenCalc had copied last. Wrong data, no error. **What made it invisible:** the fallback was written for a browser that *cannot* read the clipboard without permission, where "use our snapshot" is the right answer; in the shell the same branch turns a refusal into silently wrong content. The copy half was not even silent — `doCopy` has always been able to say *"copied here — the system clipboard was refused"*, and on the desktop it always did. A message nobody read is the same as no message. | `tauri-plugin-clipboard-manager`, reached through two commands on `window.__opencalcNative`; the editor prefers them when present and keeps the browser path when absent, so a tab is unchanged. Proved on the real shell as a pair: with the native path the system clipboard went from `SENTINEL-NOT-TOUCHED` to `PROBE-VALUE-42`, and the earlier probe of the same binary showed `write=FAILED NotAllowedError` with the clipboard untouched. |
| IO-14 | PDF export was finished, tested, and reachable by nobody | Done | P1 | **`IO-03` and `IO-10` built the paginator and the writer — print area, print titles, headers and footers, the field-code language, repeated rows — and none of it reached a user.** `casual-calc-sdk` is `publish = false`, `server/` has no PDF route, and `casual-calc-wasm` *already depends on that crate*, so every wasm build has carried `export_pdf` compiled in and never exposed it. Work that is done and unreachable is indistinguishable from work never done, from where somebody stands who wants a PDF. **The gap was two bindings, not an engine.** `session_export_pdf(sheet)` and `session_export_pdf_loss(sheet)`, plus `File ▸ Export as PDF…` — which reaches all three products at once, because the desktop shell runs this same editor and `@opencalc/engine` is these same bindings. `download()` already routes through the shell's native save panel, so it is a Save dialog on the desktop and a download in a tab without either being written twice. **The loss report is shown, not discarded**: the PDF backend draws no pictures, so a sheet with images is the ordinary case for it saying something, and a printout quietly missing a logo is the silent loss this repository refuses. | `editor.pdf-export.spec.mjs` asserts the *bytes*: a `%PDF-` header, a `%%EOF` trailer, and — the one that matters — that A4 and Letter produce different `MediaBox` values, because a writer emitting one hardcoded page size would pass every other check. Proved red by removing the binding and rebuilding the wasm: `TypeError: a.session_export_pdf is not a function`, which is exactly the state the repository was in. Command id `file.export-as-pdf`, read from `listCommands()` rather than assumed. **Not done here:** a first-class `exportPdf()` on `<opencalc-sheet>` — hosts reach it through `wasmApi()` today, which works and is not as good (`IO-15`). |
| IO-15 | The SDK reaches PDF export only through `wasmApi()` | Open | P3 | `IO-14` exposed `session_export_pdf` on the wasm bindings, which `<opencalc-sheet>` hands out through `wasmApi(): unknown`. So an embedder can export a PDF today, untyped, by reaching past the component's own surface — every other capability it offers has a real method and a declaration. Recorded rather than left as a gap somebody discovers: `wasmApi()` is an escape hatch, and a feature whose only route is the escape hatch is not really in the SDK. | `exportPdf(sheet?)` on the element and in `embed.d.ts`, returning bytes and the loss report, with `sdk/types/` compiling against it |
| IO-13 | A paste from another application loses its formatting on the desktop | Open | P3 | `TAURI-019` routes the clipboard through the platform because the webview refuses it. The plugin can **write** HTML but cannot **read** it, so `clipboardHtml()` has no native path: a paste from Excel or a web page arrives as text where a browser tab would have kept bold, fills and column structure. Copy *out* is unaffected — HTML is written alongside the text — so this is one direction only. Named here rather than left to be found, because a flavour that disappears without being counted is the failure this repository refuses. | Needs either a plugin that reads HTML or a small platform read of `public.html` / `text/html` in the shell. Not urgent: the TSV path carries values and structure, which is what most pastes are for. |
| UX-CHR-13 | Context menu rows were sized for a finger and shown to a mouse | Done | P2 | **Reported: the spacing between context menu options takes far too much room.** Measured rather than estimated: the cell context menu has **41 rows at 33px**, which is a **638px** panel — taller than the window on a laptop, and over half of each row is padding. Excel's own context menu row is about 24px and macOS's about 22, so 33px was not a house style. It is the *touch* metric: `editor.touch-targets.spec.mjs` pins these same rows to `min-height: 44px` under `pointer: coarse`, for reasons that are real — seventeen rows in the Format menu and a mis-tap puts "Clear formatting" two away from "Merge cells". The base rule had been grown toward that floor, so the mouse inherited a size only a finger needs. **What made it invisible:** the touch gate asserts a *floor* and nothing asserted a *ceiling*, so every change could only ever push these rows one way. A constraint with a test on one side is a ratchet. | `padding: 8px 10px` to `4px 10px`, `gap` 2px to 1px, panel padding 5px to 4px, separator margin 4px to 3px. Rows 33px to **25px**, panel 638px to **475px**, and the `pointer: coarse` block is untouched so a finger still gets 44px — `editor.touch-targets.spec.mjs` passes unchanged, which is the half of this that had to not move. `editor.menu-density.spec.mjs` is the missing ceiling, seen red at the old padding: *the tallest context menu row is 33.0px, and the panel is 611.0px for 41 rows*. Its ceiling is 28px rather than 25 so a font rounding a pixel differently does not fail it, while the 33px it was written against does. **Not addressed here:** 41 items in one context menu is a lot on its own, and trimming or grouping them is a design decision rather than a metric. |
| TAURI-018 | Copy and paste did nothing in every text field except a cell | Done | P1 | **Reported from the desktop build: copy/paste does not work in notes, in the command palette, or by keyboard generally.** A native menu accelerator is consumed before the webview sees the key, so the shell releases the chords that collide with typing — `Cmd/Ctrl+C`, `V`, `X`, `A`, `Z` — while an edit is open, and the editor is what says an edit is open. `TAURI-016` wired that signal to `beginEdit`/`endEdit`, which is the **cell** editor, because a cell editor was the case in hand. Every other text field in the application was therefore invisible to the shell: Find, Replace, the name box, rename, notes, the command palette, every dialog input. In each of those the shell believed nothing was being typed, so the menu ate the chord and ran the *document* command — copy inside a note copied the selected **cells**, and paste pasted them over the grid. **What made it invisible:** it cannot happen in a browser, where there is no native menu to consume anything, so the entire browser suite passed and the bug existed only in the shipped shell. The signal was also named for its implementation rather than its question — `state.editing` answers *is a cell open*, where the shell is asking *is the user typing*, and those stopped being the same thing the moment a second text field existed. | A document-level `focusin`/`focusout` pair ORed with the cell-edit flag. `takesTypedText` deliberately is not "is an `<input>`": `find-case` and `find-whole` are checkboxes and `zoom-slider` is a range, and releasing the clipboard chords over one of those would put the bug back the other way round. An `<input>` with no `type` counts, because the name box is one. `focusout` recomputes from `document.activeElement` after a timeout, so a move between two text fields never reports its way through `false`. Proved by `editor.native-editing-signal.spec.mjs`, red before the fix with `Received array: []` — the shell told nothing at all when the name box took focus — and by a second case asserting a range input still reports nothing. |
| TAURI-017 | Linux file associations were declared, installed, and unusable | Done | P1 | **Reported: right-click offers no "Open with OpenCalc" on Linux, while the same build works on macOS.** The `.deb` ships `usr/share/applications/OpenCalc.desktop` and it carries the right `MimeType=` line — all five types, matching `bundle.fileAssociations`. The line above it is `Exec=opencalc-desktop`, with **no field code**. A desktop entry that declares `MimeType` and whose `Exec` takes no `%f`/`%F`/`%u`/`%U` cannot be handed a file; the freedesktop specification says so, and Nautilus, Dolphin and Thunar filter such an entry out of "Open With" entirely. So the association was present and inert. **This is tauri-bundler's default, not our configuration**: 2.9.4's `main.desktop` writes `Exec={{exec}}` unconditionally, directly above the `MimeType` line it has just written, so every Tauri v2 application declaring `fileAssociations` ships the same dead association. **Why it survived:** `launch.rs` is entirely correct and even documents the two spellings — *"a `.desktop` file's `Exec` line may spell it `%F` (a path) or `%U` (a `file://` URL)"* — and `path_from_args` handles both. The shell was ready for an argument the desktop entry could never deliver, so every test of the Rust side passed. And macOS takes `CFBundleDocumentTypes` from the same config and needs no field code, so the one platform that could be checked by hand was the one platform that worked. | `desktop/desktop-entry.desktop`, wired through `bundle.linux.deb.desktopTemplate` — `%F` rather than `%U` because `path_from_args` wants a path, and `%U` would deliver `smb://` URLs the engine cannot open where `%F` makes the file manager mount through gvfs first. Gated in the PR build, which already produces a `.deb`: the entry is extracted from the built package and an `Exec` without a field code fails the job when `MimeType` is present. Proved red against the entry `v0.0.1` actually shipped, green against the same entry with `%F`, and green again with `%F` but no `MimeType` so the gate does not fire on an application that claims no types. |
| REL-09 | The Linux download could not start on the release it was advertised for | Done | P1 | **Reported from a real download: the AppImage did nothing on Ubuntu 22.04.** The first hypothesis was FUSE — the runtime carries the string `No suitable fusermount binary found on the $PATH`, stock 22.04 ships `fusermount3` and no FUSE 2, and the symptom matches exactly. **It was wrong.** Moving `/usr/bin/fusermount` aside on a runner and leaving `fusermount3` — a stock desktop, precisely — the shipped AppImage mounted and blocked (`exit=124`). Refuted, and worth recording as the reason for testing a diagnosis that already explains the symptom. The real cause is `glibc`: the bundle's 176 ELF files require up to **2.39**, and 22.04 LTS has **2.35**. An AppImage carries its libraries but never libc, so it runs only on distros at least as new as the one that built it — and the Linux leg said `ubuntu-latest`, which became 24.04. Every 22.04 user got a file that does nothing, while `webapp/download.html` told them it worked on 22.04 and later. **Two things made it invisible.** CI is not a user's machine: the runner has `fusermount` *and* glibc 2.39, so no amount of running the app in CI could ever have caught it. And the page's `.deb` note — 22.04 cannot take the `.deb`, use the AppImage — pointed at the one download that was equally broken, for an unrelated reason. | Linux leg moved to `ubuntu-22.04`, and a step added that reads the requirement out of the binaries with `objdump -T` and fails above 2.35. Read rather than executed, because `runs-on` is one token and raising the floor produces no error anywhere — it builds, publishes, and fails only on the user's machine. Proved red on the published 0.0.1 AppImage (`glibc 2.39`) before the fix, then green on a 22.04 rebuild. |
| REL-08 | Nothing checked that the download page's links point at files a release has | Done | P2 | **The marketing site hard-codes eleven asset names and seven tag references, and no gate knew they existed.** `desktop-v0.0.1` published `OpenCalc_0.0.1_aarch64.dmg`, `_amd64.deb`, `_amd64.AppImage` and `_x64-setup.exe`; all five URLs on `webapp/download.html` were confirmed `200` against the real release, so today the page is correct. The exposure is the *next* version: the tag and the file name both carry the version, Tauri derives the file name from `desktop/tauri.conf.json`, and bumping that file is an edit that touches nothing near the page. The result would render identically and 404 on every button. No existing gate covers it — `check-doc-references.py` resolves repository paths and these are absolute URLs to a host it never consults, `check-sdk-versions.py` compares the three npm packages, and the build is what produces the *correct* names while the page keeps the old ones. **The first version of this gate was itself too narrow**, which is the part worth recording: it parsed `href` attributes and passed, missing the `BASE` constant and three bare file names inside the OS-detection script — the links a visitor on the matching platform is actually offered. Widened to scan the whole file. | `tools/check-download-links.py`, auto-discovered by `check-all.py` (24 gates). Proved red three ways before green: a version bump in `tauri.conf.json`; the JS `BASE` alone left stale; a single JS `file:` string alone left stale. The last two are exactly what the href-only version passed. Deliberately does not resolve the URLs — that needs a published release, so it would fail on every branch cut before a tag and turn a gate into a flake; the names are checked against their generator instead. |
| TAURI-008 | Alpha builds ship unsigned, and say so | Open | P1 | **Decided by the product owner on 2026-08-30: no OS code signing for now.** The builds go out as development alpha, the site carries the bypass steps, and the release says plainly why there is no signature. That is an honest position for alpha software and it removes the only dependency on a purchase. What it costs, stated so nobody is surprised: a downloaded `.app` is quarantined and Gatekeeper reports it as *damaged* rather than *unsigned* — the wording is Apple's and it misleads, so the site's instructions have to name that exact sentence — and Windows SmartScreen blocks the installer behind **More info -> Run anyway**. Linux needs nothing. **Auto-update is not blocked by this, and an earlier note in this session said it was — that was wrong.** Tauri's updater verifies its *own* minisign signature from `tauri signer generate`, kept as a repository secret; it is unrelated to a Developer ID or an Authenticode certificate. So `TAURI-006`'s update channel can ship on unsigned builds, and the updater's key is the one signature that **is** required. | The download page states the bypass for each OS, and a release names itself alpha |
| TAURI-001 | Tauri desktop shell | Partial | P3 | The **backend half of [44](44-TAURI-DESKTOP-SHELL-DESIGN.md)'s diagram is built and tested**: `desktop/` is a crate that opens a workbook, lays out a viewport, edits a cell, undoes, saves, and reopens what it wrote — proved by a test that needs no window. Its own Cargo workspace, per `ADR-023`, with a `check-rust.py` gate **from the day it exists** rather than after it breaks; `CI-014` is what a separate workspace without a gate costs, and repeating that would be the same mistake twice. **Tauri itself is deliberately not a dependency yet.** Every function is already the shape a `#[tauri::command]` takes — bytes and plain values in, JSON out — so adding the attribute is an attribute, not a redesign, and until a window is needed this half is provable by `cargo test` rather than by launching an application. What remains is the webview and the three decisions that need a *running* shell to measure: the Tauri IPC transport cost, the native GPU backend, and parallel-recalc partitioning. **The webview half is larger than the design implies, and the number is now measured rather than assumed.** The browser editor makes **229 distinct engine calls** — 101 that mutate, ~129 that query. *The UI shell renders a display list* is true of drawing, which `RND-10` reduced to one call; it is not true of being an editor. Everything a toolbar, dialog, context menu and shortcut does is in that 229. Which leaves three options at very different costs — bundle WebAssembly in the webview and give up native calc, reimplement 229 commands and keep a second surface in step, or make the editor's engine binding swappable so the calls are written once and dispatched twice. The third keeps both properties and carries the risk a diagram cannot show: two implementations that must agree. Filed as `TAURI-003` rather than chosen in passing. **Re-measured 2026-08-30: "Tauri itself is deliberately not a dependency yet" is no longer true.** `desktop/Cargo.toml` depends on `tauri = { version = "2" }`, `tauri-plugin-dialog` and `tauri-build`, and there is a `tauri.conf.json`, a `build.rs` and an icon set. The OS menu bar landed in `TAURI-005` and in-place save in `SAVE-02`. What still holds is the paragraph that matters — the **229 engine calls** the browser editor makes, and the three options for the webview half, filed as `TAURI-003`. The row stays `Partial` for that reason and for `UX-DESK-01`, not because the shell is unstarted. | A desktop build that opens an `.xlsx`, edits it and saves it |
| IO-03 | No PDF export, in a spreadsheet that already lays out pages | Partial | P1 | **A paginator plus a writer**, as the re-scope said — not a writer over existing layout. The brief was still understated: `IO-05` gave paper, printable box and scale, but **no notion of what prints at all** — print area, print titles and the used region existed only inside `casual-calc-wasm`'s HTML path, where a paginator cannot reach them, so `print::scope()` had to be built first. **No new dependency**: `skrifa` was already there for the raster backend and supplies every metric and glyph id the font embedding needs; the PDF operators involved are about thirty. The writer lives in `casual-calc-render` rather than a seventeenth crate, which `ADR-023` already settles — *"a crate earns its place by having more than one consumer"*, and extracting one later from working code is mechanical where withdrawing a published one is not. **Status is `Partial`, deliberately**: what is missing is listed in the row below and in the module header, not implied by silence. | **Verified by the orchestrator with tools outside our own writer** (`qpdf`/`mutool` absent; poppler present): `pdfinfo` reads it, `pdftotext` returns `Region/Revenue/North/S\u00fcd \u2014 \u03a9/Total` with the formula's **recalculated** 1912.75, `pdffonts` reports `emb yes / uni yes` so the face is embedded and the `ToUnicode` map is present, and a 300x12 sheet is **14 pages** ending at `r299c11`. Shifting every xref offset by one byte is caught by *both* the crate's own test (naming the byte) and poppler ("xref num 1 not found... try to reconstruct"). Workspace tests, clippy, fmt, `cargo doc -D warnings` and `check-host-seams` all exit 0 |
| IO-10 | What PDF export does not do yet, named rather than implied | Partial | P1 | **The two that mattered are closed.** Headers and footers are drawn — the field-code language (`&L &C &R &P &N &A &F &Z &D &T &B &I &"Name,Style" &nn`, `firstPageNumber`, `differentFirst`, `differentOddEven`, `alignWithMargins`, `scaleWithDoc`), with unsupported codes **refused by name** into the compatibility report rather than printed as letters. Nothing was being dropped at read time — the importer already kept all six strings and every attribute; they had simply never been used. Reservation is the **max over the odd/even/first variants**, because pages are cut before their numbers are known and a per-page reservation would make page three's rows depend on page one's header. **Shaping now goes through the raster backend's own two calls**, so itemization and glyph ids are one decision rather than two that happen to agree. | 23 tests. **Verified by the orchestrator with poppler**: a 3-page document numbers itself `Page 1 of 3`, `Page 2 of 3`, `Page 3 of 3`; `&D` resolves from the host clock; a second face is embedded for `&B`; and `-bbox` puts the header at `yMin=21.60` — exactly the 0.3in header margin — the footer at `yMax=770.59` and the left section at `xMin=50.4`, the 0.7in page margin. Seen red six ways, including the footer anchored at its top instead of its bottom, which is the natural mistake. **Still open**: pictures, compression and subsetting, row/column headings, centring, multi-rectangle print areas, one sheet per call, and no wiring to the bridge, editor, desktop shell or server |
| IO-11 | The field-code language now has two implementations | Partial | P1 | **The row's premise was half wrong, and testing it is what showed that.** A test comparing what the two parsers produce for `&LSales&C&BQ3 Summary&B page &P of &N&R&I&A` — the codes `hf_sections` was said to drop — finds they **agree on every section**. Both consume the styling codes; the layout one keeps them as per-run style and this one has nowhere to put them, because its output is a CSS `content:` string that carries no markup. So the duplication is a **drift risk**, not a present divergence, and the test now guards it. **The two divergences that are real are architectural rather than parser bugs.** `differentFirst`/`differentOddEven` are ignored here because the browser paginates (`IO-06`), so this path never learns which page is first; and the header cannot be scaled with the sheet because it lives in a CSS page-margin box, outside the element `zoom` applies to. **The comment claiming "Excel does not shrink the header and footer with the sheet" was simply false** — `scaleWithDoc` defaults true and the PDF has honoured it since `IO-10` — so a fit-to-page workbook prints its header at two sizes from one file. Corrected to say what is true and what this path cannot do, rather than corrected into agreement. | `both_paths_read_the_same_header_the_same_way`, comparing the **text a reader sees** rather than the internal shape — the two representations differ on purpose, and demanding a structural match would force one to become the other. 110 wasm tests, the workspace, clippy, fmt and `cargo doc -D warnings` all pass. **What is left**: delegating this parser to `casual_calc_layout::print` so there is one implementation, and reporting the two divergences the HTML path cannot close |
| IO-04 | `.xls` and `.xlsm` are refused, and `.xlsm` loses a user's file | Partial | P1 | Raised by the user. `SessionFormat::for_extension("xls")` returns `None`, and there is a test asserting it does — the refusal is deliberate and recorded, not an oversight. Two very different cases hide under one complaint and they should not share a row's fate. **`.xls`** is the legacy BIFF8 binary format: a large, self-contained reader, worth doing only if real users still have such files, and the honest thing is to measure that before building it. **`.xlsm`** is the *same* OOXML package as `.xlsx` with a `vbaProject.bin` part and a different content type — so the engine can already read the sheets, and what stops it is a name check. That makes it the more urgent half and the more dangerous one: opening an `.xlsm` and saving it as `.xlsx` **silently discards the macros**, which is exactly the `no silent data loss` bound. Whatever is done, an `.xlsm` that round-trips must either keep `vbaProject.bin` byte-for-byte or name the loss in the compatibility report. **Half closed 2026-08-30.** The `.xlsm` half — the urgent and dangerous one — is `IO-08`, Done: `SessionFormat::for_extension("xlsm")` resolves, and the macro bytes survive because the *container* is written as the macro-enabled flavour (`PackageKind`, `docs/36`). Keeping `vbaProject.bin` was not sufficient on its own; a plain content-type declaration over a VBA project makes Excel report the file as damaged and repair it by deleting the project. **What is left in this row is `.xls` alone**, and its own reasoning is unchanged: BIFF8 is a large self-contained reader, worth building only if real users still have such files, and nobody has taken that measurement. Two `.xlsm` residuals live in their own rows rather than here: `IO-09` (detection by bytes) and `FID-37` (no fixture from a real producer). | Open |
| HIST-04 | A restore reports attribution as unexpressed rather than carrying it | Open | P3 | Split out of `HIST-02` when building it, rather than folded in silently. A restore emits **cell** operations and attribution lives on `Sheet::attribution`, so nothing re-stamps the restored cells with whoever pressed the button — that was the dangerous half and the side-map shape makes it free. Carrying the *snapshot's* authorship across is the other half and is not free: it needs an operation that sets attribution, which is a new `Operation` variant and therefore `CHT-07`'s **loud** wire break, inside a P2 about showing a name on hover. So the restored cells keep whatever attribution they had and the restore **reports it**, in the same `unexpressed` list where every other thing a restore cannot carry is already named — stale rather than wrong-and-confident, and said out loud instead of discovered later. P3 because the visible consequence is a name that is out of date on cells a restore rewrote, in a session that has a collaboration server, after a restore. | Not yet fixed. `every_sheet_field_is_carried_by_an_operation_or_counted_as_unexpressed` already enforces the choice — it is the exhaustive `Sheet` pattern that failed at the compiler the moment the field was added, which is how this got split out rather than forgotten |
| SIGN-01 | No digital signatures, in a format whose enterprise users require them | Open | P2 | Raised by the user. Nothing in the tree signs or verifies a document — the `signature` matches in `crates/` are all function signatures. OOXML digital signatures are an XAdES package part; the relevant bound for an engine that *reads* files is the more urgent half: a signed workbook opened and re-saved here has its signature **silently invalidated**, which is a `no silent data loss` case even though no cell is lost, because what is lost is the file's claim about itself. That half — detect a signature part, preserve it or name its loss in the compatibility report — is small and should not wait for the ability to *create* signatures, which needs a certificate story this project does not have. | Open |
| CHT-12 | A cross-sheet chart series broken by a column delete is not restored by undo | Done | P2 | Raised by the `PIV-08` work against the neighbouring path, same mechanism: `delete()` snapshots only its own sheet while `rewrite_chart_series` rewrites series across the **whole workbook**, and `sheets_charting` exists for the `RemoveSheet` path with no counterpart here. A separate producer with its own convention question — `CHT-08` chose `#REF!` for a series, and whether the *restore* should follow is not settled by that. | **The machinery already existed; what was missing was the call.** `sheets_charting` had been sitting in `structural.rs` for the `RemoveSheet` path, and `sheets_sourcing` — the pivot counterpart `PIV-06` added — sits one line above the gap. `delete` now snapshots both, as one deduplicated list so a sheet carrying both a pivot and a chart on this data is not captured twice. Red first, and the failure is the whole row: after undo the series read `#REF!` where it should read `Other!$B$1:$B$3`, while `undoing_a_column_delete_restores_a_pivot_that_lives_on_another_sheet` passed in the same run — the asymmetry printed by the harness. **The convention question the row raised answered itself**: the restore is a *snapshot* of the pre-delete text, so it puts back what was there and takes no view on `#REF!`; `CHT-08`'s choice governs what breaking looks like, not what undoing does. **`move_lines` was probed and is sound**, which is why it did not change: it snapshots its own sheet only and calls the same workbook-wide rewrite, so by shape it should have been broken too — but a move is a permutation and the reverse move shifts a cross-sheet reference back exactly, where a delete collapses one to `#REF!` and nothing can un-shift that. The probe is kept as a guard, because the day `move_lines` gains a lossy case it is the test that says the snapshot must come with it. |
| COL-57 | The editor has no `desynced` branch, so the transport's own status never reaches the screen | Done | P2 | `editor.core.js:9179-9203` writes `status.textContent` for `lost`, `live`, `reconnecting`, `refused` and `stopped` and **not** for `desynced`. `COL-56` makes the delivered behaviour correct anyway, because `rejoin()` routes through the sticky `lost` path — but between the desync and the welcome, a few hundred ms, the bar reads "reconnecting…" and a host reading the transport's status gets nothing. | Fixed with the missing branch: the desync window now reads *recovering the shared history…* instead of leaving *reconnecting…* standing, which is not what is happening. Asserted inside the window rather than only after it, in `collab.spec.mjs`. |
| COL-58 | The loss notice states a cause it does not know | Done | P2 | `editor.core.js:9191-9192` hardcodes "reconnected to a **different server** — edits you made while disconnected were not saved". For a desync the first clause is false: the client reconnected to the same server and abandoned its own state. `COL-56` now carries the cause in both `event.detail` and `onDocument`'s `why`, so the line can read one; it was deliberately not fixed in `editor.presence.js` instead, because two files writing one status line is how a status line decays. | **The cause is not on the status event, and the row said it was.** `collab.js` is deliberate about the split — `status("lost", "unsentEdits")` answers *what* went, which is the same either way, and the cause rides on `onDocument`'s `why`. Tests assert that detail verbatim, so it could not simply be widened. Fixed by composing the sentence in **one** helper, `lostNotice(why)`, called from the `lost` status branch with `null` — the half true of both — and again from `onDocument` with the real cause, in the same tick, so no frame shows two sentences. Both call sites are in `editor.core.js`; nothing moved to `editor.presence.js`. Proved red: reverting to the hardcoded line gives *the loss notice never took the cause the document event carried*, quoting back `reconnected to a different server …` for a desync. |
| COL-59 | `ClientSession::is_desynced()` is not exposed, so the transport cannot tell a latch from a bad frame | Open | P3 | `session.rs:503` has it; no wasm binding. `collab.js` therefore cannot distinguish a latched session from any other throw out of `receive()` — bad JSON, an unreadable snapshot — and after `COL-56` rejoins for all of them. Correct for the latch and merely wasteful otherwise, under backoff, which is why this is P3 and not a defect. | A binding, and a test that a non-latch throw does not rotate the key |
| COL-60 | `redirect()` may leave the old socket's `onclose` running against the new connection | Done | P2 | `collab.js:588-590` does `socket?.close(); socket = null; open();`. `close()` is immune because it sets `closed = true` first; `redirect()` sets nothing, so the **old** socket's `onclose` closure fires later and runs against the new connection — `stopTimers()` killing the new socket's timers, `joined = false`, then `status("reconnecting")` and a `setTimeout(open, wait)` that opens another. **Unverified: from reading, not measurement.** `collab.redirect.spec.mjs` only exercises `redirectTarget()` and never the socket path; reaching it needs a full node answering `stopped` with `elsewhere`. | **Measured, and it was worse than the reading suggested.** A stubbed WebSocket in a real page, driving the real module: one connection, then a `stopped` naming `elsewhere`. Without a guard the run opens **three** sockets — `full.example`, then `spare.example` *twice* — and leaves **two** live, because the superseded `onclose` ran `stopTimers()` against the replacement's timers and then scheduled its own reconnect. Fixed with a per-socket generation: `open()` takes `++generation`, and `onclose` returns before touching anything if it is no longer the current connection. Per socket rather than per session for the same reason `rejoining` is — the question is whether this is still the connection anybody is listening to. The guard returns *before* `stopTimers()`, because the timers running by then belong to the new socket. `close()` never had the defect: it sets `closed` first. Proved red by removing only the guard, keeping the counter, so the test is answering about the check rather than about the refactor. The WebSocket is stubbed deliberately — a real reproduction needs a second live node answering `stopped`, and what is under test is this module's bookkeeping, not a server's. |
| DOC-049 | `structural.rs` describes behaviour the code does not have | Open | P3 | Found by the `PIV-08` worker against the comment beside its own change: the prose says a delete *"leaves the definition in place with an empty rectangle"*; `map_range_delete` returning `None` leaves the rectangle **unchanged**, not empty. The sentence's point — keep the pivot — holds; the detail does not. Left as a row rather than a doc edit per `CLAUDE.md`, and the actual behaviour pinned with a test instead. | Open |
| CALC-VOL-01 | `TODAY()` returned 1899-12-30 and `RAND()` one fixed sequence, in every host | Done | P1 | **Found by using the editor rather than by reading it**: a realistic sheet — a few products, a `SUM`, a `VLOOKUP`, a `ROUND`, an `IF` — came out right in every cell but one, and `=TODAY()` was `0`. The engine is **correct and has a test proving it**: `volatile_functions_read_the_supplied_clock_and_seed` sets `Workbook::volatile_now` and gets the right answer, because a calc engine that reaches for the wall clock cannot be tested or replayed and `AGENTS.md` puts time in the host. **No host ever supplied it.** There is no `volatile_now` anywhere in `casual-calc-wasm` or `webapp/`, so the field sat at its default and every date formula in every session read the epoch — `YEAR(TODAY())` was 1899, and `RAND()` returned one fixed sequence from seed 0. The engine's test passed throughout, because it supplies the value the product never did. `session_set_volatile` is the binding and the editor calls it **before each edit and each explicit recalculation**, which is what makes `NOW()` current and `RAND()` reroll the way Excel does. The serial is **local** time: a spreadsheet that rolls over at midnight UTC shows yesterday to most of the world for part of every day. | `TODAY is today, and NOW carries the time of day`, which computes the expected date from the browser's own clock rather than hard-coding one, and asserts `NOW() > TODAY()` by less than a day so an equal pair cannot pass. Plus `a workbook created after boot still knows what day it is` — **a second bug the first fix exposed**: `volatile_now` is `#[serde(skip)]`, correctly, so every new session starts at zero and `File ▸ New` returned `TODAY()` to 1899 until some unrelated edit refreshed it; the wasm layer remembers the host's last value and re-applies it on session creation. And `RAND rerolls on recalculation`, through `recalculateNow` because that is the route a user takes — an earlier version created sessions directly, bypassed the refresh, and reported a failure that was its own doing |
| COL-50 | An insert meeting a delete does not converge for a formula **range**, and both answers are Excel's | Open | P1 | The one the repaired gate found that could not be fixed, and the most interesting result of the round. `=SUM(A1:A8)` with a concurrent insert and delete at row 8 settles as `A1:A8` in one order and `A1:A7` in the other — and **each is the answer Excel gives for the sequence that produced it**: `apply` grows a range an insert lands inside and clamps one a delete overlaps, and those two rules do not commute. 68 pairs, all one shape. **It needs no formula in flight at all** — a document holding a range formula plus two ordinary concurrent structural edits is enough, so it is reachable by two people doing unremarkable things. The shape was established rather than guessed: replacing every range in the seed with a single reference, changing nothing else, takes the count from 68 to 0. Not narrowed away — the property asserts the shape *per pair* and the count exactly, so 69 or 67 both fail and the hole stays pinned and visible in code. Resolving it means choosing a rule that commutes, which is a semantics decision about what a range *means* across a concurrent edit, not a transform bug to patch. | Open |
| COL-52 | `ServerSession::submit` builds its sheet-name snapshot from a workbook the history is already in | Open | P2 | Raised by the `COL-46` worker against its own change. `ClientSession::receive` builds the snapshot **before** applying the arrival, which is correct; `submit` builds it from a workbook the committed history has already been applied to, so for a rename inside that history it reads the **new** name. The worker closed the case it introduced — `rebase_across_rename` refuses when `old == new`, since a stale snapshot is indistinguishable from a no-op rename — but the same staleness applies to the band path's `target_name` and to `shift_bundle_references`, which is pre-existing (`FID-28`) and untouched. **Citation corrected 2026-08-30**: the method is `ServerSession::commit`, not `submit` (`crates/casual-calc-transaction/src/session.rs:584`). The mechanism reproduces exactly as described — `let sheets = sheet_names(workbook);` is taken inside `commit`, from a workbook the committed history has already been applied to, while `ClientSession::receive` takes its snapshot before applying the arrival. Worth fixing because a wrong method name is how a `file:line` stops being evidence, which is the failure `DOC-045` and `DOC-040` were both instances of. | Open |
| UX-MAP-04 | The zoom sweep row is satisfied by static markup | Open | P3 | Found by the `UX-CHROME-05` worker against its own acceptance criterion, which is the right instinct. `"the zoom level is visible without opening a menu"` probes `/\d{2,3}\s*%/` over `.bottom-bar` — literally what the row's wording asks, and satisfied by a hard-coded `100%` that never changes. The row should set a zoom first and assert the readout followed. Small, and it is the same class as the two false negatives closed in `UX-MAP-03`: a probe that cannot observe the thing it asks about. | Open |
| IO-09 | `for_bytes` cannot tell a macro workbook from a plain one | Open | P2 | Found by the `IO-08` work against its own change. `casual_calc_io::detect` reads only the zip's first local file header, and `.xlsx` and `.xlsm` both start with `[Content_Types].xml`; telling them apart needs that file's contents, which would put a zip reader inside a crate deliberately free of the format stack (`ADR-022`). **Consequence:** `.xlsm` bytes arriving with no filename open as `Xlsx`, and the first edit then legitimately drops the macros. The loss report is correct in that case, so nothing is silent — but the *detection* is lossy, and it wants a decision about where dispatch-by-content belongs rather than a patch. | Open |
| CHT-05 | Charts are drawn as a lie, and editing one destroys what it was | Partial | P1 | **The data-loss half is done, which is the half that was destroying files.** The writer hardcoded `clustered`, so dragging two columns — an edit with nothing to do with the chart — rewrote a stacked chart as clustered in the saved file. Now `<c:grouping val="stacked"/>` survives, with `percentStacked`, a combo chart's two groups and a secondary axis's second `<c:valAx>` alongside it, each asserted to **read back** as the chart it was. The drawing half: stacked, 100%-stacked, combo and secondary-axis each produced a display list **identical to the clustered control** and `compatibility entries mentioning "chart": 0`, so the user saw a picture that was not their chart and was told nothing. Now the stacked axis covers the sum (220) where the control covers the tallest single value (140), bands abut exactly, and 100%-stacking puts every column's top at the plot ceiling. **`docs/84` §10 is not the order of work — §7 is**, and their letters disagree; the worker said so rather than silently substituting a plan. §7's step B was already closed by `CHT-06`/`CHT-09`/`CHT-11`. | Four independent reverts, each the defect itself: the hardcoded `clustered` -> "the written part is still a stacked chart"; the importer's `stamp_groups` -> 6 read tests and 4 write tests red; the plotter -> **two byte-identical rectangle lists**, which is `docs/84`'s finding printed by the test harness; an absent wire field read as "off" -> "labels were silently turned off". **The worker's own first stacking implementation was wrong and its tests did not catch it — printing the geometry did**: it measured the band between the bucket's min and max instead of between base and value, making every stacked bar one pixel tall |
| CHT-16 | `retune_series` still detaches on a grouping change | Done | P2 | `CHT-05` fixed what detaching *loses* — the model now carries grouping, per-series kind, secondary axis and data labels, so rebuilding no longer discards them. It did not stop the detach. A chart whose grouping changes is rebuilt from the model, so the hundreds of formatting elements in the retained part that the model does not describe are still dropped. Editing grouping **in place** inside the retained part is the stronger form and the next item in `docs/84` §7 D. | **The detach was unconditional, which is wider than this row assumed.** `session_set_chart` called `ChartView::detach()` on every call — so a drag, a resize or a retitle threw the retained part away just as a grouping change did, and an imported chart came back from a save plainer than it went in, for an edit that had nothing to do with its appearance. Two halves. `retune_grouping` rewrites the grouping value where it sits, changing only the characters between the quotes so every other byte survives; it walks each group element, because a combo chart states grouping more than once, and passes the value through `ChartGrouping::from_val` against the enclosing chart-group element, so a `clustered` arriving for a line chart leaves the file alone rather than writing a token the schema forbids — the same rule `group_head` already applies when generating. And `retunable_in_place` narrows the detach to what the part states and nothing can rewrite: kind, title, legend and the two axis titles. Series, frame and grouping are retunable, so they no longer cost the formatting. |
| CI-028 | `metrics_count_a_save_that_really_happened` fails intermittently | Done | P2 | Failed once in a full `--workspace` run, then passed in isolation, on `main`, and in a second full run — so it is a flake, not the change under test. A metrics test that counts a save is timing-sensitive by construction, and the cost of leaving it is that CI goes red at random and the next person learns to re-run rather than to read. Recorded rather than shrugged at: CLAUDE.md's rule is to stop retrying after the second unexplained failure, which is what stopping here means. | **It was waiting for the wrong signal.** The test waits for the save sink to fill and then asserts on a *counter*, and those are not simultaneous: `Deliver::put` fills the sink from **inside** the save callback, and `service_lifecycle` increments `saves_accepted` **after** that callback returns (`net.rs`). So the wait can finish, and the scrape can run, in the window between them. This is the same shape as the rest of this section — an instrument agreeing through a door that is not the one under test. **Reproduced deterministically** by widening that window: `tokio::time::sleep(300ms).await` between the callback and the `fetch_add` gives `the save moved the counter (0 -> 0)` on every run. The first attempt at that probe used `std::thread::sleep` and the test kept passing — it blocks the current-thread runtime, so the test never reached the scrape, and the probe proved nothing. Worth recording: a probe that cannot fail is as useless as a gate that cannot. Fixed by polling the counter itself, bounded the same way the existing wait is; a counter that never moves still fails. Verified with the 300ms window still in place, then with it removed, three runs. |
| CHT-15 | Chart code left behind by `RND-10` and `CHT-13` has no callers and nothing warns | Open | P3 | `valueExtent` (`webapp/editor.geometry.js:67`) is imported at `editor.core.js:176` and `:570` and `editor.paint.js:64` and called nowhere — left from `RND-10`, when the canvas stopped drawing its own axes. `chart_data::ref_numbers` and `ref_cells` lost their last non-test callers in `CHT-13` (`chart.rs:63` uses `ref_strip`/`ref_text`/`strip_numbers`); they are `pub`, so no warning fires. | A gate, or removal |
| PERF-14 | A browser measurement that reloads after an edit measures a different viewport | Open | P2 | Found by the `PERF-13` worker against its own harness. The draft-recovery bar (`SAVE-03`) costs 60 px of viewport on the *second* load, so the same seed gave a 30-row window in one run and 27 in another — `session_cells` payloads of 14,753 against 13,259 characters, **purely on run order**. Fixed inside `frame-profile.mjs` with a fresh context per case and a pinned row height, and the report now prints `[colsxrows]` so a reader can see whether two lines are about the same sheet. **Any other browser measurement in this repo that reloads after an edit has the same exposure**, and a number that moves with run order reads as a regression. | A measurement whose window is asserted, not assumed |
| CI-026 | Only one tool checks that it is measuring this checkout's bytes | Open | P2 | `frame-profile.mjs` gained `assertServingThisCheckout`, which SHA-compares `editor.core.js` **and** the wasm on the wire against disk and exits 2 on mismatch — it printed the hashes on every run and the hash visibly changed under a mutation, which is how that work knew it measured its own bytes. That is a mitigation for **one tool**; `reuseExistingServer` in `playwright.config.mjs` is untouched, which is `CI-025`'s second half. Filed separately because the shape is reusable: every harness could assert its origin, and doing it once in the config would cover them all. | The check, applied where it is missing |
| FID-42 | Recalculation changes saved values without moving the dirty counter | Open | P3 | Split off `FID-39` rather than folded into it, because it is a judgement call and the rest was not. `recalculate` changes formula results, and those results *are* written to the file — so by the letter of `edits_applied`'s promise ("is there unsaved work?") it should count. Against that: a recalc follows nearly every edit, which has already counted, so counting it too mostly adds nothing; and a recalc triggered by opening a file or by a volatile function would mark a document dirty that the user never touched, which is a close warning for nothing. Deciding needs the host's view of when it recalculates, which is why this is a row and not a line of code. | Whichever answer is chosen, a test that pins it — the current behaviour is untested either way |
| CHT-14 | Nothing in the engine can key a chart cache soundly | Open | P2 | The prerequisite `CHT-10` established rather than assumed, and the reason it fixed the cost instead of caching it: **there is no invalidation key**. `edits_applied` is not one (`FID-39`), and every `&mut Workbook` exit that is not `edit` changes values silently. A cache keyed on the counter serves stale numbers after an undo or a manual recalculation — `CHT-05`'s silent lie, bought at the price of the row. What it needs is a session-owned data epoch bumped at every `&mut Workbook` exit. Filed so the cache is not attempted again without it. | Open |
| FID-40 | The collaborative receive path bypasses `resequence_views` | Open | P2 | The fourth arm of `FID-38`, found by establishing the blast radius rather than fixing the case reported. `collab.rs` applies remote operations through `session.workbook_mut()`, so a **remote peer** inserting, removing or moving a sheet leaves this participant's personal views misaligned, with nothing to explain it. Outside the crate the other three were fixed in. | Open |
| FID-37 | The `.xlsm` path has no fixture from a real producer | Open | P2 | Named by the `IO-08` worker about its own evidence, which is the right instinct. Its macro package is one **this engine wrote**, so the tests prove the reader and writer agree and nothing about whether either matches Excel. `ods_sessions` deliberately uses a LibreOffice-authored fixture for exactly this reason and the macro path deserves the same — otherwise a shared misunderstanding between our reader and our writer is invisible, which is the `assert_round_trip` shape in a different place. | Open |
| SAVE-05 | Save, autosave, recovery and version history, designed as one thing | Designed | P1 | `docs/12` ranks *"there is no save"* **second** on the switching-blocker list, behind only a crash that is now fixed. `Ctrl+S` produces a download; there is no autosave, no crash recovery, no in-place save and no history. Designed rather than built first, at the user's direction, because save means something different in each of four hosts and three save models that disagree would be worse than one that is late. **The single rule:** a document has one *save target* — `file`, `handle`, `host`, `server` or `download` — resolved at open from the capability set that already exists. `Ctrl+S` commits to that target and never creates a second document; where the target is `download` it *acquires* a target instead, which is the whole of the `opencalc (1).xlsx` problem. Downloading moves to `File ▸ Download`, which is what it always was. **Version history is snapshot-based and restore is a new operation** — not a preference. Revisions are positional and every client, resume key and `oldest_rebasable()` is defined against that numbering, so under `ADR-011` rewriting history is not available. And because `COL-50` and `TransformError::Unsupported` mean a log cannot be replayed reproducibly, snapshots are not the expensive option, they are the only one — so history is **not** blocked on `COL-50`. See `docs/83`. Phases: desktop in-place → browser draft and recovery → history. | Design note; the phases are `SAVE-02`, `SAVE-03`, `SAVE-08` |
| UX-KEY-05 | `Alt+F1` is an unbound feature, not a missing one | Open | P3 | Raised by the `UX-KEY-04` worker against the brief and against `docs/12` §8 item 14, which files it beside Flash Fill as absent. Charts **exist** — seven types, `session_charts`, Insert ▸ Chart — and Excel's `Alt+F1` is *insert a default chart from the selection*, plausibly a few lines against the existing insert path. `F11` (a chart **sheet**) genuinely has no model behind it, and `Ctrl+E` (Flash Fill) has no engine support. Three things filed as one, and they are not alike. | Open |
| FID-41 | The build tag the editor records is always `dev` | Open | P3 | `editor.js` sets `globalThis.__opencalcBuild` **after** its `export *` has already evaluated `editor.core.js`, so the tag the module captures is never the real one. Cosmetic for drafts, where the build is informational and only used to mark a draft from a newer build — but anything else reading it gets `dev` in production. | Open |
| COL-64 | One operation too large for a frame still cannot be sent | Open | P3 | `COL-63` splits a chunk *between* operations, which is where the 4 MiB frame cap is reached in practice — a paste or a restore is many operations. It cannot split *within* one. `take_within_budget` therefore always takes at least one operation, even when that one is over the budget by itself, because returning an empty chunk would leave `flush` looping on a submission it can never make. Such an operation goes out and the transport reports what the server says, which is at least an answer rather than a silent stall. **Not currently reachable by any known path**: the largest single operation the editor produces is a `Batch`, and the batch is what `COL-63` splits. Recorded because the code takes a deliberate decision at that branch and a reader should find the reasoning attached to a row rather than only to a comment. | A single operation over the cap is refused by name rather than sent, or the wire form gains a way to fragment one |
| COL-63 | A submission larger than the frame cap closes the connection instead of refusing | Done | P2 | `net.rs:207` sets `max_message_bytes: 4 MiB` as both message and frame size, while `ClientSession::flush` takes all pending operations into one chunk with no byte bound. At the 84 B per changed cell `SAVE-08` measured, a restore of more than **~50,000 changed cells** exceeds it and **the connection closes rather than a `Refusal` naming the reason**. Not new to restore — a large paste is also one `Batch` — but a restore makes it easy to reach, and neither `docs/83` nor `docs/62` names the ceiling. | **Measured: one unbounded chunk came to 24,194,500 bytes against a 4,194,304-byte cap** — 5.8x over, and a frame over that cap is not refused, it closes the connection, so the work was neither delivered nor kept. Fixed by bounding the chunk in the bytes it will actually occupy: `take_within_budget` measures each operation once on its wire form, spends a budget in order, and leaves the rest pending for the next flush. Chunking, sequencing and `MAX_OUTSTANDING` already existed, so a large paste becomes several round trips instead of one closed socket — no new concept, and nothing refused. Measured on the wire rather than counted in operations, because a count would restate the constant instead of checking it and would keep passing if the wire form grew. **The cap is now stated once**: `MAX_FRAME_BYTES` in `session.rs`, which the server reads for its `max_message_size` instead of repeating `4 * 1024 * 1024`. The two disagreeing is not a warning here — it is a dropped session with nothing said. `serde_json` moved from dev-dependencies to dependencies to do the measuring; not new third-party code, since `casual-calc-model` already carries it. Proved red by removing only the bound: `frame 0 is 24194500 bytes, over the 4194304-byte cap`. |
| SAVE-04 | The File System Access API as a save target | Open | P2 | `docs/83` §3. A browser tab has no file path, so `Ctrl+S` cannot write back — unless the user grants a handle, which the File System Access API provides and which then behaves exactly like the desktop's `file` target under the one save rule. **Chromium only**: Safari and Firefox do not implement it and fall back to `download`. Filed separately from `SAVE-03` because a draft in IndexedDB and a real file handle solve different halves — the draft stops a crash losing work, the handle stops the downloads folder filling with copies. | Open |
| SAVE-07 | No gate asserts which browsers have the File System Access API | Open | P3 | `docs/83` §5. The save target resolves on a capability the browser either has or does not, and nothing checks that our belief about which browsers those are is still true. A feature-detect that silently starts failing degrades every Chromium user to `download` with no signal — the failure mode is quiet, which is what makes it worth a gate rather than a comment. | Open |
| SAVE-06 | `session_save()` blocks the main thread for 424 ms at 300k cells | Open | P3 | Measured for `docs/83`: 424–436 ms serializing, against an **8 ms** IndexedDB write, and a 196 ms worst frame gap versus 18 ms idle — twelve times the 60 fps budget `PERF-D-01` fought for. Serialization is the cost; storage is not, which is the opposite of the intuition and it decided the autosave cadence. Autosave fires at quiesce, copying the collaboration server's existing `SavePolicy` rather than inventing a second one, and the accepted cost is one ~0.4 s hitch per minute under sustained editing, named rather than hidden. The fix is serializing off the main thread; deferred with a stated trigger in `docs/83` §7. | Open |
| FID-43 | The style table leaks an undone edit the same way, and nothing prunes it | Open | P2 | Found by the `FID-36` worker while measuring its own blast radius, and correctly left alone. `styles_xml` walks every interned style, referenced or not, over a `StyleTable::intern` that is append-only and never reclaimed. An undone formatting edit leaves an orphan `cellXf` carrying a **font name** and a **custom number-format code** — and a format code can hold literal text the user typed: the probe round-tripped `"MARKERFMT"0.00` through an undo into `styles.xml`. Lower disclosure value than cell text, but also unbounded growth under autosave against Excel's 64k `cellXfs` ceiling. The same watermark shape fixes it; it was deferred because pruning styles also means remapping `cellStyleXfs`, `dxfs` and the conditional-format references, which is a second change with its own risk surface rather than a bigger version of the first. | The `FID-36` tests, extended to a formatting edit — the probe already exists |
| DOC-039 | Four ADRs are still "pending", and three of their deadlines shipped years of work ago | Partial | P1 | **The op-set ADR is written and Accepted as `ADR-024`** — the one the row said to do first, because five Accepted decisions already rested on it. **Measuring changed what it says**, exactly as it did for `ADR-019`: `Operation`'s own doc comment claimed "a closed set", and the history refutes that — `MoveColumns`, `MoveRows` and `MoveRange` joined on 2026-08-29, three weeks after the rest and long after Phase 5 shipped on the set. So what is decided is the weaker and true property, which is also the one transform soundness needs: the set is total **at a given `PROTOCOL_VERSION`**, and adding a variant is a *hard* break where an additive `SheetMetadata` field is the *quiet* one. The enum's comment was corrected to match rather than left stating a contract the code does not keep. **Still pending: the dependency-graph representation and the MSRV/toolchain pin**, both also past their triggers, plus the recalculation-ordering and cell-store-constants entries which never had deadlines. | `check-adr-status` (24 decisions, none left proposed, no design note disagreeing with the register). The gate this row asks for — failing when a *pending* entry names a phase that is `Done` — still cannot be written: it needs `docs/06`'s phase table to be machine-readable, and it would fail on the two entries above the moment it existed, so it lands with them and not before |
| CI-019 | `formula_text` keeps a narrower version of the blind spot `CI-018` closed | Open | P2 | Raised by the `CI-018` worker against its own fix rather than left in a report. `formula_text` prints at the cell's **own** origin, which is the right origin — but it is still a rendering, so two distinct offsets that both resolve off-sheet *from that cell* both read `#REF!` and compare equal: in `A1`, column offset `-1` and `-2` are indistinguishable to it. Roughly 25 assertions across `tests.rs` are spelled out with it. Much narrower than the absolute case — it needs the reference to be genuinely off-sheet from its own cell, not merely to the left of it — and `observable()` now carries the real check in every round-trip test, so the exposure is small. It is the same class, which is why it is a row and not a comment. Note also that the comment at `tests.rs:3334` justified those spelled-out assertions by the absolute-rendering blind spot; that justification is now false and was rewritten rather than left telling a future reader the gate is weaker than it is. | Open |
| UX-MOB-06 | Touch has no route to range selection, resize, or the sheet-tab menu | Partial | P1 | **Range selection and the 390px status bar are done; resize, the sheet-tab menu and the head tags are not.** Range selection follows the Sheets/Excel-mobile idiom rather than inventing one — tap selects, two corner handles extend — and the active cell is the corner **not** being held, so typing lands where the user expects. **Two of this row's own claims were wrong.** The tabs do not *overdraw* the mode label: `.sheet-tabs` is `overflow-x: auto`, so 140 px of content was **clipped to a 4 px sliver** — there was nothing on screen at all, which is worse than overdraw, and no way to see which sheet you were on, switch, or add one. And the bar never overflowed the *page*: `docW == winW == 390` before and after; it overflowed *internally*, with one flex item absorbing all of it. | 17 tests driving real touch through CDP `Input.dispatchTouchEvent`, not synthesised events. **Verified by the orchestrator**: implementation reverted -> 6 red, the block refusing to grow and the active cell refusing to move. **399 browser tests pass including the collaboration specs, which the worker could not run** — the disk was full and the config builds two collab servers. Three defects were found by *looking*: at A1 the top-left handle was drawn at `(HW-7, HH-7)` inside the header corner where the body clip erased it — **invisible and still live**, since hit-testing went on answering for a finger there; the 6 px mouse fill square peeked out beside the 14 px handle, a second smaller target on a device that could never grab it; and at 559 px the tabs pill stretched to an empty 368 px. A test that passed with the fix reverted was strengthened rather than kept: at +5 px it was landing in the mouse fill handle's hit zone and **silently arming a drag-fill a touch device cannot finish** |
| UX-MOB-07 | The selection summary is 595px wide inside a 390px window | Open | P2 | Found by the `UX-MOB-06` worker against its own change, and it undercuts what that change delivered. `.sel-stats` (`webapp/editor.css:956`) is `position: absolute; right: 24px; white-space: nowrap`, so it grows leftward without limit: with A1:E13 selected it measures 595 px at `left: -229` — "Sum: 113.45 Avg: 11.345 Min: 2" runs off the left edge of the screen. On a phone the selection summary is **the payoff of touch range selection**, and it is half unreadable. | A width assertion at 390px |
| UX-MOB-08 | Mouse and touch disagree about where the active cell ends up | Open | P2 | Found by the `UX-MOB-06` worker. `extend()`'s own doc comment says the active cell must stay where the selection began, and explains why — typing goes to `state.sel`. The **mouse** drag-select branch (`webapp/editor.core.js:7017`) moves `state.sel` anyway, so dragging A1->C3 with a mouse leaves the heavy border on C3 where Excel leaves it on A1. The new touch handles follow the documented model, so the two input paths now disagree with each other. One of them should move, and the comment says which. Also at `:7011`: that branch computes `px` in grid units but clamps against `rect.width - 2` in CSS pixels, so it is wrong at any zoom but 100% — the same fault `editor.geometry.js:272` says "every pointer path in this file already divides" for. | A drag-select test asserting the active cell, at two zoom levels |
| UX-DESK-04 | `wirePresence` re-homes `#presence` after the mode has decided the chrome | Open | P2 | Found by the `UX-DESK-01` worker against its own change. `webapp/editor.presence.js:377` ends with `bar.insertBefore(box, byId("hdr-collapse"))`, unconditionally putting `#presence` back into `#menubar` *after* boot has placed it — which is why `#tb-status` relocated at boot and the roster did not, and why the fix has to re-assert `applyModeChrome()` after `wireEvents()`. Placement belongs to whatever decides the chrome, not to the widget. Related: `editor.css:411`'s `.presence { margin-left: auto }` is a menu-bar-shaped margin on a node three chromes now relocate, and the note at `:416` already predicted the failure — the split should be a rule of the bar, not of the item. | A mode switch leaves every relocated node where the mode put it |

## The Excel parity backlog

[73](73-EXCEL-UX-PARITY-AUDIT.md) holds roughly thirty-five findings that have no
row here yet — the largest untracked surface in the repository. Its
`[verified]`/`[unverified]` split is the point of it: three verified P1s became
`FID-23`, `UX-NAV-01` and `UX-NAV-02` and are closed, and the rest must be
reproduced before they are scheduled. Rows arrive here as they are verified;
the audit is worked down, not merged in wholesale.

## Where a finding goes

One question, in order:

1. **Does the code do something wrong?** A row here.
2. **Does a document describe code that does not exist?** Fix the document. No
   row — a doc edit is not tracked work.
3. **Does a document state a contract the code does not keep?** A row here, and
   **the document keeps its text.** Never resolve this by deleting the promise:
   that is how a security bound disappears without anyone deciding to drop it.
   [23](23-CELL-STORE-REPRESENTATION.md) says it plainly — *a documented limit
   nothing in the code enforces is not a limit*.

A new tracking document is never the answer. That is how there came to be
thirteen.
