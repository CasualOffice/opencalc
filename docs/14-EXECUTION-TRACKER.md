# 14 — Execution tracker

**The only tracker.** Every unit of work has one row here, whatever kind of work
it is. Closed rows move to [14a](14a-ARCHIVE-CLOSED-WORK.md); design reasoning
lives in the commit that made the change.

Four more were deleted on 2026-08-18: the support matrix (18), the competitive
parity analysis (46), the editor parity tracker (45) and the fidelity tracker
(52). An audit of 46 settled it — of roughly a hundred rows checked against the
code, **79 were done, 11 partial and 3 absent**, while the document marked most
of them "Todo". Merge cells was recorded as `Partial, Edit ✗ Render ✗` against
three engine operations, a toolbar menu and a browser test; the name box was
described as "a read-only `<span>` today" and is an `<input>` handling five
reference syntaxes; pivot tables were "Todo (preserve-only)" against a
1,386-line pivot engine.

The drift ran one way — pessimistic — so anyone reading it concluded the product
was far less finished than it is, including when somebody asked "do we have
X?". That is worse than no document, and it is why the rule below is a rule.

[53](53-FEATURE-CORRECTNESS-TRACKER.md) survives because it is **generated from
the code** by `tools/feature-audit/inventory.py` rather than maintained by hand,
so it cannot drift in the way these did.

There were thirteen overlapping tracking documents. They disagreed with each
other about what was done, what was severe, and in two cases about what an id
even meant — `FID-13` named both a sheet-quoting defect and a `sheetView`
construct, so `git log --grep FID-13` returned two unrelated changes. A tracker
nobody can trust is worse than no tracker, because work gets planned from it.

## How a row works

```
| ID | Title | St | Sev | Mechanism | Gate |
```

- **Mechanism** — the cause, in one line. Not the story of finding it.
- **Gate** — the named test that fails if it regresses. If you cannot name one,
  the row is not `Done`.
- **St** — `Open`, `WIP`, `Partial`, `Blocked`, `Designed`, `Done`, `Dropped`.
  Nothing else.
- **Sev** — by what reaches the *file*, not by what shows on screen. Silent
  corruption is P0 however small it looks; a missing menu item is P3 however
  loud.

Rows stay short on purpose. The long account of a defect — what made it
invisible, which two wrong turns preceded the fix — belongs in the commit
message, where `git blame` finds it beside the diff and where it costs nothing
to keep.

**Before adding a row, run `tools/check-tracker-ids.py`.** Ids are never reused;
it is a pre-commit hook and a CI gate because five rows were added without
reading the id space above them.

## P0 — silent corruption, or nothing ships until it is fixed

| ID | Title | St | Sev | Mechanism | Gate |
| --- | --- | --- | --- | --- | --- |
| COL-28 | Collaborative undo does not converge | Done | P0 | Undo applied its stored inverse untransformed. For a cell that is last-writer-wins and acceptable (docs/69); for a structural edit it deletes a band somebody has since filled, destroying work that was never there when the undo was recorded and that **no undo stack anywhere holds** — the peer's history says "typed into row 10", not what row 10 contained. Structural undo is now refused before it applies, and refused loudly. No authorship tracking was needed: the stack is LIFO, so by the time an insert is on top, everything this session did after it has already been undone and anything left in the band is somebody else's. | `undoing_an_insert_a_peer_has_filled_is_refused` + 4 siblings; `collab.spec.mjs` — two browsers, refused with a message, both documents unchanged |
| COL-30 | An imported chart's retained bytes bypassed the operation wire | Done | P0 | The row also named `sheet.tables`; that half was **stale** — tables are in the `SheetMetadata` bundle and have crossed the wire since. Retained parts were real: deleting or detaching an imported chart called `drop_chart_part`, which wrote through `workbook_mut` and so was in no operation. A replica kept the part, and whichever node saved decided what the file contained; undo restored the chart's entry with its bytes already destroyed. `apply` now **derives** which parts died from the bundle every replica already receives — no wire data forward — and carries them on the inverse in a `skip_serializing_if`-empty field, so no `PROTOCOL_VERSION` bump. | `deleting_an_imported_chart_reaches_a_replica`, `undoing_a_chart_deletion_restores_its_retained_bytes`, `the_retained_bytes_field_is_not_a_wire_break` |
| SEC-002 | No end-to-end admission budget | Done | P0 | Every limit was per part and every one was passing; nothing added them up. A package may hold 50,000 parts, so the only real ceiling on the total was the 4 GiB uncompressed cap — and 4 GiB of `<c r="A1"><v>1</v></c>` is on the order of a hundred million cells, from an upload of a few megabytes. `SpreadsheetLimits` now counts populated cells, shared strings, defined names and merged ranges **once for the document**, across every part, and fails closed. | `cells_are_counted_across_the_whole_workbook` — two sheets of 40 refused by a budget of 79, which a per-sheet check admits |
| SEC-003 | Docker secret hygiene | Done | P0 | Three holes: `.env` was in neither `.gitignore` nor `.dockerignore` while the README tells everyone to create one holding the signing secret; and compose defaulted to `dev-secret-change-me`, so a deployment that forgot got a **published** key — and a shared secret lets its holder *mint* tokens, not merely check them. Compose now has no default (missing fails loudly) and the server **refuses** every placeholder this repo publishes, plus anything under 16 bytes. | `secret_tests` reads the values out of `.env.example` and both compose files, so a new placeholder cannot slip in |
| PROD-12 | Docker collaboration endpoint is unreachable | Done | P0 | The default was `ws://127.0.0.1:8443/collab` — the *browser's* loopback, on a different port from the page — so a share link worked only on the Docker host. Now derived from the request the browser just made (same origin, `X-Forwarded-Proto` picks `wss`), and standalone gains an nginx so one origin is true. Explicit config still wins. | `endpoint_tests` (4); CI brings the stack up and upgrades `/collab` on the page's origin |
| PROD-13 | Upload → store → open → co-edit | Done | P0 | Limits landed; Docker reachability, error UX and the end-to-end gate remain. **Done.** `tools/e2e-document-lifecycle.mjs` runs the whole chain against the deployed stack in `docker-build`: upload a corpus `.xlsx` (somebody else's file, not one this project's own writer produced), follow the `303` to the new document, open the share link, take two tokens *from the host* so its own signing is under test, have one participant edit and the other receive it, then download and check what comes back is still an OOXML package. Speaks the wire protocol directly rather than through Playwright — the browser suite already proves two browsers converge; what was unproven is the same thing through the **deployed** stack, and the job that has the stack has no browser. Reads `PROTOCOL_VERSION` from the engine rather than keeping a copy, because `COL-42` was exactly that drifting. Running it found `PROD-14`. Also excluded `.claude/` from the build context: a finished agent worktree keeps its own full `target/` tree, so the directory had reached 4.4 GB and every `docker build` was shipping all of it — the existing `target/` rule does not cover a *nested* copy under a different prefix, and it is working state that has no business in a layer either way. | Upload a real `.xlsx`, share it, edit from two browsers |
| PROD-14 | A configured collaboration endpoint with no path is a dead socket | Done | P1 | The derived endpoint has always ended in `/collab`, the only path the collaboration server serves. A **configured** `OPENCALC_COLLAB_WS` was returned verbatim — so `ws://collab:8443`, which is exactly what the variable's name invites, produced an address nothing answers. It fails silently on both sides: the host did as it was told, the server was never asked, and the only symptom is an editor that never connects, with nothing in either log. **Found by running the stack, not reading it** — it is the first thing that happened when `PROD-13`'s gate was pointed at a hand-configured deployment. Fixed by giving a pathless configured endpoint the `/collab` path; one that names a real path is left alone, since a deployment behind a proxy may mount the socket anywhere and rewriting *that* would break the setup it was configured for. Gated through `collab_endpoint` rather than the helper — asserting on the helper alone passes with the call site reverted, which is the defect itself, and the first version of the test did exactly that. | The endpoint reaches the socket whether or not the path was written |
| PROD-15 | The quick start shipped a collaboration endpoint nothing listens on | Done | P0 | **Found by `PROD-13`'s gate on its first real run**, which is what it was built for. `.env.example` set `OPENCALC_COLLAB_WS=ws://127.0.0.1:8443/collab`, and the collaboration service is `expose`d to the compose network and **not published to the host** — so the browser dialled a port nothing was listening on. `cp .env.example .env && docker compose up`, the documented quick start, produced a stack whose one distinguishing feature was broken. `PROD-12` had already fixed exactly this in the code and `docs/65` records why the default was removed; the example file was left carrying the value the fix deleted, and the example file is what people copy. **Now empty**, so the host derives the endpoint from the request — same origin, through the proxy, which is what the proxy routes. `OPENCALC_COLLAB_PORT` went with it: neither compose file ever read it. Gated by `tools/check-env-example.py` — every `127.0.0.1:PORT` the file points a browser at must be published by some compose file. Seen red on the exact value that broke main, and on an unpublished port; a third mutation shows that blurring `expose` and `publish` hides it, which is why the check distinguishes them. | The quick start co-edits |
| PROD-16 | The proxy told the host a hostname with no port | Done | P1 | Caught while fixing `PROD-15`, before it cost a second CI round rather than after. With `OPENCALC_COLLAB_WS` empty the host derives the endpoint from the request — and nginx's `$host` **strips the port**, so a demo reached on `127.0.0.1:8080` would be told it was reached on `127.0.0.1` and would derive a socket on port 80, where nothing listens. The fix for `PROD-15` would have failed the same gate for a different reason. All three nginx configs now send `X-Forwarded-Host: $http_host`, which the host already prefers over `Host`. Caddy sets it by default and needed no change. | A stack behind the proxy derives an endpoint with its port |

## P1 — wrong answers, lost work, or a promise the code does not keep

| ID | Title | St | Sev | Mechanism | Gate |
| --- | --- | --- | --- | --- | --- |
| SEC-014 | No default recalc budget | Done | P2 | Cancellation existed but nothing bounded a recalculation for an embedder who passed no token. Now bounded by a **work** budget, counted across every pass including the iterative ones. Work rather than wall-clock deliberately: `Instant::now` panics on wasm32, and a clock would make the same workbook finish on one machine and stop half-way on a slower one — determinism ranks above security bounds here. docs/21 says so now rather than continuing to say "time". | `the_budget_spans_every_pass` — a per-pass counter lets a workbook ask for 30,000 passes |
| SEC-015 | The iterative recalc phase ignored the cancellation token | Done | P1 | Found one merge after SEC-012 shipped, by reading what the new code called rather than what it claimed. `recalculate_cancellable` checked the token inside a pass and then handed off to `iterate_to_convergence`, which took no token at all — and the pass count comes from the *workbook*, where Excel allows 32,767. So a document could ask for tens of thousands of uncancellable whole-workbook passes through an entry point named for being cancellable. | `the_iterative_phase_stops_too` — one formula cell, so only a check *between* passes can stop it |
| SEC-016 | The SEC-001 sink gate matched one sink in seven, and skipped a whole host | Done | P1 | Two independent blind spots in the same check -- the one that enforces the XSS boundary a workbook can reach (`SEC-001`). **(1) The regex never worked.** It used `\s`, and `git grep -E` is POSIX ERE, where `\s` is undefined; so the `innerHTML`/`outerHTML` half matched *nothing*. The job still looked alive because `document\.write\(` -- the one alternative with no `\s` in it -- kept matching, so the gate reported success having examined one sink out of seven. **(2) The path list named one host.** `server/casual-calc-host/src/*.html` was listed while `casual-calc-wopi/src/editor.html`, served to a browser exactly the same way, was not. **Neither was exploitable**: every existing sink turned out to be correctly marked, so the hole was latent -- but a *new* sink in either dimension would have passed the check written to stop it. Found while reviewing a WOPI change that touched that html. Fixed with `[[:space:]]` and a path *shape* rather than a list of crates. Proven by planting an unmarked sink in each dimension: the old gate is GREEN on both, the new gate is RED on both, and both return GREEN when the plant is removed. This is the third hand-maintained host list to go stale here, after the wasm excludes and the ADR-012 boundary. | A planted sink in any host's markup fails CI |
| SEC-013 | No bound on snapshot size | Done | P2 | The last row of docs/21's scale table, and the one admission path that accepted whatever it was handed — reached from a resumed collaborative session and from any host calling the model directly. The byte ceiling is checked **before** `serde_json` sees the input, because a limit applied after parsing has already paid for the allocation it exists to prevent; the cell count is checked after, at the same ceiling admission uses, so a workbook cannot enter by one door at a size the other refuses. | `an_oversized_snapshot_is_refused_before_parsing` — `OC-MDL-0005`, and not the malformed-snapshot code |
| CI-010 | Diagnostic codes and their registry could drift | Done | P1 | docs/20 is the promise a log-reader relies on, and nothing checked the code agreed with it. Both drifts had already happened: `OC-IMP-0003` sat registered for a limit nothing enforced, and `OC-RND-0001` was emitted by the renderer and registered nowhere — found by the new gate on its first run, not by review. Checked one way only: an emitted code must be registered; a registered code may be reserved for planned work. | The gate passes clean and fails on an unregistered code, verified both ways |
| SEC-011 | No populated-cell admission cap | Done | P1 | docs/21's spreadsheet-scale table listed seven limits; the grid bound and the formula depth/length bounds existed, four did not. Cells, shared strings, defined names and merged ranges are now admitted against a document-wide budget, emitting `OC-IMP-0003` — the code docs/20 reserved for exactly this and which nothing had ever emitted. Snapshot size is the one row still open, split out as SEC-013 rather than implied. | `the_refusal_is_diagnosable` — over-cap refused with `OC-IMP-0003` |
| SEC-012 | Nothing is cancellable | Done | P1 | docs/07 and docs/21 both promised admission and full recalc were "bounded *and* cancellable". They were bounded; nothing in any crate could stop one, so a workbook inside every limit and merely enormous held the browser's only thread until it finished. A cooperative `Cancel` token now exists in the model crate — cooperative because a browser offers no pre-emption, so a job can only stop by asking — and both loops ask periodically. Reachable from the SDK via `open_cancellable` and `recalculate_cancellable`. An import fails closed (`OC-IMP-0007`); a recalc keeps what it computed and *reports* that it stopped, because the document was already holding stale values and discarding fresh ones would leave it worse. | `an_import_stops_after_it_has_begun`, `a_recalculation_stops_after_it_has_begun` — both survive a check at the top of the loop and fail on it |
| FID-24 | Defined names are not shifted by an insert or delete | Done | P1 | Found by auditing docs/46 (CP-032). `structural.rs` shifted merges, sizing, hidden sets, the freeze boundary, outline levels, tables and autofilters — everything position-indexed except `defined_names`. A name pointing at `A10` still pointed at `A10` after five rows were inserted above it, so every formula using it silently read different cells: nothing marked `#REF!`, nothing refused, the number simply changed. Names now go through the same `rewrite_expr` the cells do. A workbook-scoped name is treated as having no home sheet, so an unqualified reference is left alone rather than rewritten against a sheet chosen arbitrarily — Excel writes those fully qualified anyway. | `a_name_below_an_insert_follows_its_target` + 3; the control catches a rewrite that shifts regardless of position |
| COL-41 | No version history or restore | Done | P2 | Asked for directly. There is none, and it is deliberate at the server: docs/57 says the collaboration server "is not a file store, a version history" — storage belongs to the integrator, so a WOPI host such as Nextcloud or SharePoint already provides versioning for that deployment. The gap is real for the SDK-embedded path and the demo host. Snapshot I/O exists in the model, so the missing part is retention and a restore path, not serialisation. **Built, at the host, where the row said it belongs.** A version is kept **before** each save overwrites the document, so history holds what it *was* rather than what it became — a version taken afterwards is a copy of the thing somebody is trying to undo. The timestamp is the filename, so there is no second file to keep in step and a directory listing is already in order. Bounded by `OPENCALC_MAX_VERSIONS` (default 20), because a version per save would otherwise fill the volume. Three endpoints: list, fetch one, restore. **Restoring keeps the current document first**, so the one button whose purpose is undoing a mistake is not the only one that cannot be undone; and a version that will not open is refused rather than made live, the same argument the upload path makes. A history panel on the demo document page makes it reachable rather than an API nobody can call. **Known limit, stated in the deployment page rather than hidden:** a restore lands on the stored file, not on open sessions — anyone still editing overwrites it on their next save. Reaching live sessions means the collaboration server evicting the document, which is a change to a boundary ADR-012 draws deliberately. Two flaws in my own tests were the interesting part. Millisecond-named versions collide on rapid saves and silently lose an entry; and the callback-ordering test passed with the ordering swapped because `blank_xlsx` ignores its title, so its two "different" documents were byte-identical. Seven mutations red once both were fixed. | A named revision can be listed and restored |
| COL-42 | The browser suite's protocol constant drifted from the engine's | Done | P1 | `PROTOCOL_VERSION` went to 6; `tests/browser/collab.spec.mjs` kept `const PROTOCOL = 5`. The suite speaks the wire protocol over a raw socket, so it carries its own copy, and the server refused every join at the old version -- three collaboration tests red **on main**, after the merge. The test that exists for exactly this even says `bump PROTOCOL in this file when the protocol changes`, and it still went unnoticed, because the only thing that could notice was a ten-minute `browser-smoke` job that does not run locally. **Fixed.** Constant bumped, and a grep gate added to the fast `ci` job so the same drift fails in a second instead of after a merge. The gate was exercised three ways: the drift that actually broke main, the engine moving ahead next time, and the constant being renamed so the grep matches nothing -- that last one is what stops a gate like this quietly passing forever. | Engine and suite agree, checked in `ci` rather than only in the browser |
| UX-PAGE-01 | Alt+PgUp/PgDn does not page horizontally | Done | P3 | Found by auditing docs/46 (CP-005). The horizontal-paging branch sits one nesting level too deep, inside the modifier block, so it answers to Ctrl+Alt+PgUp/Dn and Excel's plain Alt+PgDn falls through to vertical paging. A placement bug that reads as a missing feature. | Alt+PgDn pages right |
| UX-PROT-01 | Column and row resizing ignores sheet protection | Done | P1 | `edit_axis` took the sheet index as `_sheet` and dropped it, so resizing a column on a protected sheet succeeded while every other write went through a guard. The unused parameter is what made it invisible — the signature looked as though it cared. A resize is not a cell edit, so the question is not whether the cells are locked: Excel gates it behind protection's own `formatColumns`/`formatRows`, and `SheetProtection::permits` now reads them, refusing when the file is silent (the OOXML default, and the safer reading). | `resizing_obeys_sheet_protection` — asserts the *edit path* consults the rule, after a mutation showed that asserting the rule alone stays green |
| UX-SHIFT-01 | The destructive-shift confirmation fails open | Done | P1 | `session_shift_affects_formulas` is probed inside a swallowing catch whose fallback is `false` — so a probe that throws asserts the *safe* answer and the warning is skipped for a shift about to break formulas. The fix is one line (start `risky` at `true`) and was written and reverted: three attempts at a browser gate failed on locator resolution in the grid's context menu, and landing an ungated behaviour change is not how this repository works. Next session: gate it, then land it. **Done, and the gate is what took the thinking.** The three failed attempts all drove the grid's context menu and died on locator resolution in the submenu — they were testing Playwright, not the editor. The decision is now a named export, `shiftIsRisky(probe)`, taking the probe as a callback, so the browser gate imports the editor module and calls the **real** function with a probe it controls: no menu, no locators, and the failing case is reachable without a wasm module that can be made to throw on demand. Four cases: a throwing probe warns, a `true` probe warns, a `false` probe does not, and `undefined` is coerced rather than reaching the branch as a truthy-ish nothing. Seen red both ways — restoring the `false` fallback fails exactly one test, the one that owns the defect; making it always warn fails both controls, which is what stops the fix being "warn about everything" and getting trained away in a day. Full browser suite re-run: 51 passed. | A failing probe warns rather than proceeding |
| UX-END-01 | Excel's End mode does not exist | Done | P1 | `End` jumped to the last used column — which is Excel's *`End` then `Right`* — so the destination existed while the mode it belongs to did not, and the two-key sequence a spreadsheet user has in their fingers did nothing at all. It is how a keyboard user crosses a large sheet without holding a modifier. Found by auditing `docs/48` against the code rather than reading it: of 35 P0/P1 pipeline items, this was one of only two genuine gaps. **`End` now arms the mode and moves nothing**, an arrow spends it on a block jump, `End`+`Home` is the last used cell, and any other key cancels it — an armed mode that survives an unrelated keystroke fires on an arrow the user has forgotten arming. Announced in the status bar, because an invisible armed mode makes the next keystroke behave differently for no visible reason. Six tests, four mutations red: `End` back to jumping, the mode never spent, every arrow jumping, and nothing cancelling it. | End then an arrow jumps by block |
| UX-TOKEN-01 | No elevation or surface layer in the design tokens | Done | P2 | The other gap the `docs/48` audit found (`M10-1`). 24 semantic colour tokens exist — accent, border, text, selection, popover — and the chrome reads them, so the canvas and the CSS already agree about colour. What is absent is the *elevation and surface* layer the design system asks for: shadows, raised/sunken surfaces and state layers are still written inline per component, so a popover, a menu and a dialog each decide their own depth. Cosmetic rather than behavioural, which is why it is P2 — but it is the reason the chrome looks assembled rather than designed in places. **Done, and the interesting half was not cosmetic.** Chasing the elevation layer turned up two things a user can see: the focus ring and the text selection were written as `rgba(47, 109, 246, …)` — the *default accent*, spelled out — so a host that set a brand accent got brand-coloured buttons and **blue focus rings**. Invisible to us because our own accent is that blue, which is how this class always hides. And `dangerColor` sat in the **published** `THEME_TOKENS` list while `--oc-danger-color` appeared nowhere in any stylesheet: a host set it through the documented API and nothing happened, with no error to say why. Two different reds were hand-written instead. Both now derive from their tokens with `color-mix`, which already had precedent here. Elevation is two steps — `raised` is attached to the page, `overlay` floats above it — aliasing the component-named shadows rather than renaming them, because `controlShadow` and `popoverShadow` are in the published contract. Four hand-written overlay shadows became one, and are theme-aware for the first time; a fixed `rgba(0,0,0,…)` reads wrong on a dark surface. Five new tokens published and typed. Gated twice: `tools/check-theme-tokens.py` in CI asserts every advertised token is defined or referenced — red on the defect exactly as it was — and four browser tests assert the *computed* values, so `color-mix` and the token chain are exercised rather than assumed. Six mutations red in all. | Elevation and surface are tokens, and every advertised token does something |
| RND-08 | The canvas still draws its own data bar | Done | P2 | RND-07 gave the headless renderer a `DataBar` item; the editor canvas still draws its own from a per-cell `bar`/`barc` payload. They agree only because the geometry and alpha were copied across — which is the two-renderers-can-disagree shape `conditional.rs`'s own module doc argues against, reintroduced one layer up. **The stated acceptance was wrong about the size of the job, and is changed here deliberately rather than quietly.** "The canvas consumes `PaintItem::DataBar`" needs a display list across the WebAssembly boundary, and there is none — building one is an architectural change, not the P2 tidy-up this row reads as. What the row is *about* is the two renderers being able to disagree, and that is removable completely and cheaply: the inset, the alpha and the default colour were written out twice, in `casual-calc-render` and in `editor.js`, agreeing only because somebody had copied them across. `data_bar_style()` now exports them and the canvas reads them. **The canvas still paints; it no longer decides.** `default_data_bar()` parses the same exported constant, so the two blues cannot drift either. Gated where it counts: a browser test measures the *painted pixels* and compares them against what the engine exports — comparing the export against itself would pass with the canvas hardcoding whatever it liked. Proven by moving the engine to `padX = 4` while reverting the canvas to its old literal `1`: `the bar starts 1px into the cell; the engine says 4px`. Plus two Rust tests, one of which (the exported colour parsing to the drawn colour) is the one place the export cannot prevent drift by construction. **Still open, and now the honest version of the original idea:** a display list on the JS side would let the canvas and the PNG share the *drawing* as well as the numbers. That is `RND-10`. | The canvas draws from the engine's geometry, checked in pixels |
| RND-10 | No display list across the WebAssembly boundary | Done | P2 | The canvas painted charts from its own JavaScript while the PNG renderer painted them from `casual_calc_layout::chart::push_chart` — two implementations of one picture, so every fix landed twice and a divergence between them was invisible until somebody compared a screen against an export. The canvas now draws from the engine's display list, and **224 lines of duplicate renderer are gone** (`drawChartFrame`, `drawBarChart`, `drawLineChart`, `drawPie`, `drawAxes`, `drawLegend`, `drawCategoryLabels`, `legendBox`, `seriesColors`). `editor.paintlist.js` is the reusable half: a `PaintItem` painter, so the next primitive moves across without a second implementation. The frame is passed *in* rather than derived in the engine — a chart is anchored in cells, and the scroll offset, frozen panes and zoom that turn those into pixels are resolved on the client every frame; deriving them again inside would be a second thing to keep in step, which is the fault being removed. **Two unit bugs, both of which returned a plausible answer.** The display list is in **twips** (`PX = 15.0`, i.e. 1440/96) and the first version passed CSS pixels: the plot area computed negative, so `push_chart` drew its background and border and returned — two items, no error, an empty box where a chart had been. And geometry converts by `dpi/1440` while a font size converts by `dpi/72`, so one `ctx.scale` for both would have drawn every label at a fifteenth of its size. The painter does the same two sums `casual-calc-render` does, which is what makes *the same list* true rather than approximately true. **The first regression test passed with the fix reverted.** It built its own twips frame and called the export directly, so it never exercised the caller that was broken — a test that supplies the conversion under test proves only that the export works on correct input. It now renders through `drawCharts` and counts bar-coloured pixels in the chart's own rect; reverted, that reads `0 of 153600`. | `editor.chart-from-display-list.spec.mjs`; 151 browser tests and 13/13 repository gates pass |
| RND-09 | The lowest value in a data-bar range draws no bar | Done | P2 | `t = (n - lo) / (hi - lo)` gives the range minimum a fraction of zero, so it renders nothing. Excel draws a short non-zero bar for the lowest value under automatic `cfvo` min/max. Needs checking against Excel and LibreOffice rather than deciding from memory. **Fixed, and the check did not need either of them.** ECMA-376 gives `dataBar` the attributes `minLength` and `maxLength`, defaulting to 10% and 90% of the cell width, and the position in the range is interpolated between them -- so the spec answers the question the row was going to answer by observation, and answers it better. `bar_length` in `conditional.rs` applies it, which puts the fix in the one place both renderers read, so the canvas and the headless PNG cannot disagree about it (the argument `RND-08` is still open on). Verified neither consumer rescales: both multiply the fraction by an inset width. Gated by two tests, seen red under three mutations -- the raw fraction (the original defect, minimum at 0.0), a constant bar, and min/max swapped. **The swap is the one worth recording**: the first version of the gate took `min` and `max` of the fractions, which a reversed scale passes unchanged, because reversing moves which *cell* gets which fraction and not the set of them. It now asserts by position. | The minimum draws a bar, at ECMA-376's `minLength` |
| RND-06 | Headless render omits charts and images | Done | P1 | Split, because the two halves cost nothing alike. **Images are done.** `PaintItem::Image` carries the media part's *package path*, not its bytes — a display list is rebuilt every frame and would otherwise copy every picture into every one — and the frame comes from the anchor cells **plus** the EMU offsets, measured from `offset(end + 1)` because `<xdr:to>` is exclusive. Virtualized by frame **intersection**, not anchor containment: a picture is routinely larger than the window. **No new dependency** — `tiny_skia::Pixmap::decode_png` is already in the graph via tiny-skia's default `png-format`, so this trips no ADR and adds nothing to the wasm bundle. Bounded before allocating: the IHDR is read and anything over 16 MP refused, because a few hundred bytes of PNG can legally declare 65535x65535. **The row's premise about charts was wrong, and checking changed the answer**: the plot backend already exists — ~300 lines in `webapp/editor.js` draw all seven chart kinds. What the headless path lacks is series *resolution*, stranded in `casual-calc-wasm` (a host crate the renderer may not depend on), plus polyline/polygon/wedge primitives. That is `RND-05`'s shape, needs an ADR, and is `RND-11`; design in `76`. **Worth recording:** the first clip test was worthless — it asserted white outside the frame and did *not* fail when the fill covered the whole surface, because tiny-skia's pattern is transparent beyond its own extent. The transform bounds the picture, not the `fill_rect`. It was replaced with a scale test that a missing scale reds. **The chart half is now `RND-11`, and it is built** — the plotter was ported down rather than replaced by a crate, so the canvas and the PNG come from one source. This row closes with images done and charts delegated to the row that names them. | Pixels at four known coordinates from a four-quadrant source; seen red under a mirrored blit (red where green belongs) and under a missing scale. 14 mutations caught |
| RND-11 | Charts still are not drawn headlessly, and cannot even be reported | Done | P1 | Built to the owner's decision: **port the plotter down**, not add a charting crate. Series resolution (`ref_cells`/`ref_numbers`/`ref_text`) moved out of `casual-calc-wasm` — a *host* crate the render path must not depend on — into `casual-calc-layout`, the way `conditional.rs` moved for `RND-05`; `PaintItem` gained `Polyline`, `Polygon` and `Wedge` (`ADR-021`, `80`, Accepted once it had shipped — it said `Proposed` in this very sentence for weeks, which is why `check-adr-status.py` exists); and the editor's ~300 lines of plot construction were ported line-for-line, with every canvas pixel constant written as `n * PX` so the two diff by eye. **All seven `ChartKind`s draw**, model to PNG. **No new dependency anywhere** — one path edge, `casual-calc-layout` on `casual-calc-formula`. **What is not drawn is named rather than missing**, which is the whole reason geometry had to reach the display list: the **legend**, because the canvas sizes its box from `measureText` and layout has no text advances at all, so a guessed width would move the *plot rectangle* — a chart with a legend therefore renders with a wider plot in the PNG than on screen; and the **rotated y-axis title**, whose space *is* still reserved so the plot rectangle agrees either way. **A test that could not fail, caught by mutating:** breaking `Align::Center` in only the unshaped arm **passed on a default build**, because text is placed twice and the shaped path is the one that runs — the same trap as `DOC-031`, in a different file. Both arms are guarded now, checked under both feature configurations. **The legend now draws too, which closes this.** Built to the owner's decision: a **font-metrics crate below layout**, not a capability trait (ADR-019 rejected that). New `casual-calc-text` holds the bundled faces (moved from `casual-calc-render`), the substitution table (moved *down* from `casual-calc-layout`, because the faces need it and it needs nothing — it is still re-exported from layout, so `casual_calc_layout::substitute` is unchanged for every caller), and `advance_width`. Layout measures directly; no seam, no callback, one answer shared by both. `legend_box` and `draw_legend` are line-for-line ports of `webapp/editor.js`, including the order they take the legend off the frame — before the axis titles, or the two disagree about what is left. **The bundle did not grow**: `all-fonts` moved with the faces and is *off by default* in the new crate, because Cargo unifies features and a default-on flag reached through layout would have put every family back into WebAssembly, undoing the 72% this was once trimmed by. Measured: 7,547,464 → 7,553,213 bytes, +0.08%, which is the legend code. **Seen red without the fix**: forcing the legend back to `None` fails 5 of 6 layout tests and the render pixel test with *"the pie still reaches where it did without a legend"*. The sixth is the too-small-frame guard, which is correctly unchanged either way. **What is honest about the result**: layout measures the *bundled* face and the canvas measures the browser's `system-ui`, so the plot rectangles differ slightly. That is the same disagreement those two have had about every other string since the PNG path existed, and it replaces an error the width of the whole legend. | Pixel assertions at named coordinates — a reversed pie sweep puts accent 4 at (250,48) where accent 1 belongs, and a doughnut with `inner_radius: 0` fails as "a doughnut with no hole is a pie"; both seen red here |
| RND-12 | Only PNG is decoded; every other picture format is named, not drawn | Done | P2 | `RND-06` decodes PNG because tiny-skia already could. jpeg, gif, bmp, tiff, webp, emf, wmf and svg are sniffed by magic number and reported as `UnsupportedFormat`, which honours the no-silent-loss rule but leaves a real gap: **the browser draws all of them**, so the canvas and the headless render disagree about the same document. A thumbnail is exactly where that shows. Deliberately not fixed inside `RND-06`: every candidate decoder is a new dependency in a crate compiled into the wasm bundle, which is an ADR question and not a detail to settle while doing something else. **Done, to the owner's decision: decode raster, refuse vector.** `image` 0.25 with default features off and five codecs named one by one — jpeg, gif, bmp, tiff, webp. `emf`, `wmf` and `svg` stay refused *by name*, and not as a gap to close later with the same trick: they are drawings to be **executed**, not pixels to be unpacked, and half-executing one produces a picture that is wrong rather than one that is missing. **The bundle cost turned out to be zero, and finding that out changed the design.** Compiled in unconditionally the decoders were **+789,531 bytes (+10.5%)** of the WebAssembly bundle — for code the browser cannot reach: nothing in `casual-calc-wasm` exports a PNG render, and the editor draws pictures with the browser's own decoders. Behind a `raster` feature, on by default and off for WebAssembly, the bundle is **byte-identical** to before (7,553,213). That is not the "fidelity split by deployment" the owner rejected — it is dead code removed from the one build that cannot run it. **A regression the feature gate introduced, caught by running both configurations.** The first draft moved the *format names* inside the feature along with the decoding, so a build without decoders called a JPEG "an unrecognised format" — a no-silent-loss report quietly getting worse because a codec was not compiled in. The name table is unconditional now and `a_jpeg_is_still_named_when_the_decoders_are_compiled_out` holds it there. Same class as `DOC-031` and `RND-11`: a second feature configuration is a second program, and only running it finds this. **The bomb seam.** Dimensions are read from the header *before* any allocation, as the PNG path already did, plus `image`'s own `Limits` because a header can lie — and `MAX_IMAGE_SIDE`, since 16,000,000x1 is inside `MAX_IMAGE_PIXELS` and is not a picture. **Seen red without the fix**, twice: removing the decode branch fails 3 tests; *flipping the decoded image vertically* — the classic BMP bug, since BMP stores rows bottom-up — fails the quadrant assertions with `top-left: pixel (48,15) is (0,0,255), expected (255,0,0)`. "It decoded" and "it is not blank" both pass against that. **Fixtures are third-party by design**: `sips` (Apple ImageIO) for jpeg/gif/bmp/tiff and `cwebp` (Google's reference encoder) for webp. A fixture made by the crate that decodes it proves only that the crate agrees with itself. | A JPEG in a workbook renders headlessly, or the decision to refuse it is an Accepted ADR |
| RND-13 | The canvas letterboxes a picture where Excel stretches it | Done | P2 | **This row was mine and it was wrong, twice over.** It first said the canvas letterboxes where Excel stretches, and a change was ordered on that — which would have distorted every one-cell-anchored picture in every document. Checking the format instead of asserting it changed the answer, and the real defect was upstream of the argument: only a `twoCellAnchor` describes its size with cells. `oneCellAnchor` and `absoluteAnchor` carry `<xdr:ext cx cy>`, and `casual-calc-import` **read straight past it**, substituting a nominal eight columns by fifteen rows. Its own comment admitted the frame was a guess, on the reasonable grounds that a chart drawn a column out beats one not drawn — true for a chart, which redraws itself into whatever box it lands in, and false for a **picture**, which is scaled to fill its frame. A guessed frame is a fabricated aspect ratio, so every one-cell-anchored photograph rendered visibly squashed. `<xdr:ext>` is now read and carried to `ImageView::extent` (additive by ADR-010 — `SCHEMA_VERSION` is still 1 and verified so), and layout uses the authored size where the file states one, falling back to the measured rectangle for a two-cell anchor whose `<xdr:to>` *is* the picture's corner — including a distortion the author made by dragging a handle, which must be reproduced rather than corrected. **The second version of the guard test could not fail either:** it fed an ordinary two-cell anchor, and a real one has no `<xdr:ext>` at all, so removing the guard changed nothing. It now feeds a document carrying both — malformed, which is what the guard is *for* — and the mutation reds it. | A one-cell anchor keeps the 3:1 size the file states rather than a nominal 8x15; a stray extent never overrides a two-cell anchor's own rectangle; both seen red |
| RND-14 | Nothing in the SDK can ask for pictures | Done | P2 | The render crate's picture support was complete and tested and **reached nobody**: `render_sheet_png` — what the editor, WOPI and every host call — still used the pictureless form. The media needed no new plumbing at all; it was already in `Workbook::retained_parts` under the same package path the display list carries, **which is why the gap was invisible: nothing looked missing from any signature**. Fixed by indexing the retained parts as an `ImageSource` so `render_sheet_png` draws pictures at its existing signature, plus `render_sheet_png_with_report` and `WorkbookSession::render_png_with_report` for the half the old signature cannot carry. **`ImageReport` is folded into `CompatibilityReport`** using the `UndrawnReason::feature()` keys the render crate had already provided for exactly that, so a host gets one answer about what a document lost instead of two shapes it must merge itself. **Third instance of one pattern this round** — with `SEC-017` and `WASM-01` — and the cause is the same each time: a capability whose tests all live in the crate that implements it is fully green while reaching no caller. The gate has to sit at the *seam*. | A session render draws the workbook's own media, asserted on the pixel at the frame's centre; seen red with the index returning `None` — white, and a drawn picture reported lost |
| DOC-033 | The documentation index stops at 68 | Done | P3 | `docs/00-README.md` stopped at 68, so `14a` and `69`-`76` were absent — each added by the work that needed it, none updating the index, which is the predictable failure of an index kept by hand. Fixed as a **gate** rather than eight lines: `tools/check-doc-index.py` asserts every `docs/*.md` has a row whose number matches and whose link resolves, that no number is claimed twice, and prints the ready-to-paste row built from the document's own H1. Redirect rows — a number whose file is gone — stay legal on purpose: a file must have a row, a row need not have a file. It caught real drift on its first run here, naming `docs/76`, which had landed after the gate was written. | Every document is on the map; seen red on eight missing rows, a repointed link and a duplicated number |
| RND-07 | Data bars reach no renderer | Done | P2 | `conditional::effect_for` resolved the fraction and colour and nothing consumed it. A `PaintItem::DataBar` now carries the **cell** rect plus the fraction rather than a pre-multiplied bar rect — the inset is a device-pixel quantity layout cannot know, and pre-multiplying makes a wrong fraction indistinguishable from a different inset. Ordered after the background and before the text, or the number is unreadable. | `a_data_bar_reaches_the_display_list_at_the_right_fraction`, `a_data_bar_is_ordered_between_the_fill_and_the_text`, and a render test asserting pixels |
| RND-05 | Headless render omits conditional formatting | Done | P1 | The rules were resolved inside `casual-calc-wasm` — a host crate the render path cannot depend on — so the canvas showed colour scales and highlight rules and every headless PNG showed plain cells. The logic was not missing; it was in the wrong crate. Moved to `casual-calc-layout::conditional`, so both renderers ask one function and a rule cannot be fixed in one of them. Fills, font colours and bold now reach the display list. The row named charts and images too; those need paint primitives that do not exist and are RND-06, not folded in here. | `a_highlight_rule_paints_the_headless_render_too`, `a_colour_scale_varies_across_the_range`, `a_rule_overrides_the_cells_own_fill` |
| PERF-11 | A filled-down column still costs N ASTs | Done | P1 | docs/40 promises more than dedup: "a filled-down column of the *same* relative formula shares one AST plus a per-cell origin". PERF-09 shares *identical* ASTs, and fill-down produces different ones — the references shift, so `A1*2` and `A2*2` are distinct trees. Sharing them needs relative references in the AST and an origin per cell, which changes the parser, the evaluator, the shifter, import's shared-formula path and export. Architectural, and deliberately not folded into a row about deduplication. **Designed in [75](75-RELATIVE-FORMULA-SHARING-DESIGN.md); not started.** Two things came out of writing it. **The per-cell origin docs/40 asks for does not need storing** — the origin *is* the cell's own address, which the sheet already knows, so this is a pure memory win with nothing added per cell. And **the relativity has to be in the type, not a convention**: after normalising, a reader holding a stored formula gets a delta where it used to get an address, and a single missed call site computes a plausible wrong answer in a spreadsheet, silently. Distinct `StoredRef`/`ResolvedRef` types make the compiler find all of them. The note also names the interaction that will break first — a cut must travel verbatim (`UX-CUT-03`), so it has to resolve at the old origin and re-store at the new one — and states what would make the work not worth doing. **Stage 1 of 4 landed: the types, unused.** `StoredRef` (relative unless anchored), `ResolvedRef` (an address) and `Origin`, with `store`/`resolve` between them and lossless conversion to the existing `CellReference` so stage 2 can move the evaluator without changing meaning. Nothing consumes them yet — that is the point of staging it this way, and it is why this stage is safe to land on its own. Eight tests; four mutations red, including the two that matter most in opposite directions: anchoring ignored (so `$A$1` drifts down a filled column) and every shape collapsing to one (which would make the memory figures excellent and the documents wrong). Off-sheet resolves to `None` rather than wrapping — one column left of A is `#REF!` in Excel, not column 4294967295. **Stage 3 started and stopped, on a blocker the design missed — which is the finding.** `Workbook::formulas: Vec<Expr>` is **serialised**, into snapshots that are durable *and* on the wire (`casual-calc-collab-server` holds one per document and hands it to joining clients). The design lists seven readers — parser, evaluator, shifter, import, export, structural rewrite, cut — and not this one. A relative `A1` writes today as `{"reference":{"col":0,"row":0}}`, bare `col`/`row` with the anchor flags skipped when false; the moment `Expr` holds a `StoredRef` of that shape, the same JSON reads back as **offset zero — the cell itself**. An existing `B1 = A1*2` returns as `B1 = B1*2`, a circular reference: **every relative reference in every saved document, silently**, with `ADR-010` closing the usual escape by forbidding a `SCHEMA_VERSION` bump. **Proved, not reasoned about**: adding the derive and feeding a real serialised formula back through it is two lines, and it was written before the claim was made. **Owner's decision: the format does not change.** Relativity is an in-memory representation only — resolved to absolute per cell on write, stored relative at the cell's origin on read. No migration, no version bump, mixed-version clusters keep working. The cost is stated rather than hidden: snapshot and collaboration payloads keep their present size, and **memory** — the thing this row is for — is what improves. **Two decisions locked.** The AST is one `Expr` holding `StoredRef` with entry points taking an `Origin`, not a generic `Expr<R>`: the compiler still finds every call site, since `.row` stops being a `u32` address, so the mitigation survives without a type parameter in every signature. And a **new stage 2a** — move the arena's serialisation from `Expr` up to `Workbook`, the only place that knows which origin belongs to which tree — lands before stage 3, gated on a snapshot reading back byte-identical. **One thing turned out cheaper**: `Evaluator` already carries `current`, the cell being evaluated, saved and restored per formula for `ROW()`/`COLUMN()`. The origin stage 2 was going to thread is already there and already correct. **Then measured, because the design says measuring is the cheaper thing to do and the blocker had just raised the price.** The benchmark reports an `arena` block — nodes times `size_of::<Expr>()`, counted rather than sampled, so it works on a machine with no `/proc`. A 5 000-cell filled `=A1*2` column holds **5 000 distinct trees** (confirming directly that `PERF-09` collapses none of it), 15 000 nodes, **240 arena bytes per cell** against a cell's own ~83. So three quarters of a filled column is tree, and nine tenths for a nine-node `IF(…)`. **The win is not theoretical**, and the number is now reproducible and pinned by a test that fails the moment a filled column starts sharing — which is to say when stage 3 lands. **Stage 1b landed: the compatibility story, proved.** `StoredRef` now carries `CellReference`'s **exact** encoding — same camelCase, same `skip_serializing_if`, same `deny_unknown_fields` — and `ABSOLUTE` names the origin `(0, 0)` at which storing subtracts nothing, so a stored reference's `col`/`row` *are* the address. Two tests hold the invariant the whole compatibility design rests on: a reference stored at the zero origin serialises byte-identically to the absolute one, across all six shapes that differ in encoding (plain, each axis anchored, sheet-qualified, whole-column, whole-row), and reads back meaning the same thing. **Seen red** by dropping one `skip_serializing_if`. `StoredRef::absolute`/`as_absolute`/`at` are the conversions the parser, printer and snapshot will each go through. **What that buys**: the snapshot never has to change, so there is no migration and no `SCHEMA_VERSION` move — which was the blocker found last round — and it is now a tested property rather than an argument. **What stage 3 still needs, scoped precisely.** Swapping `Expr`'s payload compiles down to three semantic decisions and about a hundred literal test constructions, not just a type change: **printing becomes origin-dependent** (the same shared tree reads `A1*2` in `B1` and `A2*2` in `B2`, so `Display` becomes the absolute case and a `print_at` carries the origin — done and reverted here, since it cannot land without the swap); **`shift_references` becomes a no-op for copy and fill**, because resolving at the new origin *is* the shift, leaving it needed only for structural inserts; and **off-sheet stops clamping**, since `resolve` answers `None` where the old `shift_ref` clamped at zero — `#REF!` is Excel's answer and the more correct one, but it is a behaviour change with tests to match. **The swap was then carried through five crates and reverted, on a second gap the design misses.** What worked: `casual-calc-formula` fully migrated (71 tests, including the new semantics — copy shifts *by being read elsewhere*, `restore_at` replaces `shift_references` for moves, off-sheet is `#REF!` rather than a clamp); `casual-calc-layout` (a chart's series reference has no holding cell, so it resolves at `ABSOLUTE`); `casual-calc-model` (`store_formula_at`); `casual-calc-eval` — evaluator, dependency graph and `functions.rs` all threading the origin, where **`Evaluator.current` turned out to already be it**; and `casual-calc-import`, where the shared-formula path becomes the *ideal* case: `<f t="shared">` means precisely "one tree, many cells", so a whole shared group now lands on one tree with no shift at all. **A defect in the landed stage-1 types, found by an existing test**: `store`/`resolve` carried over `shift_ref`'s rule for `$`-anchored axes but not its rule for **unnamed** ones, so `A:A` read six rows down came out `A6:#REF!`. Fixed and tested — and the test that caught it is one that already existed, asserting the property rather than the mechanism, which is why it survived the mechanism being replaced. **Where it stopped: `structural.rs` moves the cells and *then* rewrites the formulas.** Under absolute storage the order is irrelevant; under relative storage it decides the answer. `B5 = A2*2` with a row inserted at 3 moves to `B6`, and its offset — three rows up, measured from `B5` — resolves at `B6` to `A3`, while `A2` never moved. The rewrite exists to correct exactly that and cannot: `job.at` is the **new** address and the offsets were taken from the **old** one, which has already been discarded. Either the rewrite runs before the move, or it is given both origins. **Resolved by working the cases out, and neither candidate answer was right.** Rewriting before the move does not help: for `B5 = A2*2` with a row inserted at 3, the rewrite correctly leaves the tree alone (the *reference* is at row 2, before the insertion), and then the move to `B6` turns the untouched offset into `A3` anyway. What changed the meaning was the **move**, not the rewrite. So **the move accounts for itself**: a cell carrying a relative tree that moves is doing what a *cut* does, and `restore_at` is already the primitive — `shift_cells_insert`/`_delete` re-store any formula they move, from old origin to new. The existing address-based rewrite then runs afterwards **completely unchanged**. The two compose: the move keeps a tree's meaning invariant, the rewrite applies the change the insertion implies. `B5 = A5*2` → `A6` (shifted, 5 ≥ 3); `B5 = A2*2` → `A2` (untouched, 2 < 3); both Excel's answers. **The order the code already has is the correct one, and there is no unknown left**: `restore_at` exists and is tested, and the rewrite needs no change. **Done. `Expr` holds `StoredRef`, and a filled-down column is one tree.** Measured by the benchmark that was built to answer it: 5 000 cells, **5 000 distinct trees → 1**, 15 000 nodes → **3**, **240 arena bytes per cell → 0.05**. The pinning test written to fail at exactly this moment did, and is now inverted to assert the arena **does not grow with the column** — stronger than any per-cell figure and independent of how many cells are measured. **The format did not move**, which was the blocker's whole resolution: one tree in memory, three *absolute* references on the wire, asserted as the literal JSON text rather than against this build's own output so it cannot pass by both sides drifting together. No migration, no `SCHEMA_VERSION` change, mixed-version clusters unaffected. **Two honest costs.** `size_of::<Expr>()` is **80 → 96 bytes**, because a `StoredRef` carries `i64` offsets where a `CellReference` carried `u32` addresses — so an *unshared* formula costs 20% more, and only sharing pays that back. And the WebAssembly bundle grew **7,547,191 → 7,597,111 (+0.66%)**. **The pair inverted, exactly as `75` predicted.** Copy and fill now rewrite **nothing** — resolving a shared tree at the destination *is* the shift — while a cut, a row insert and a row sort must `restore_at`, because moving a cell changes what its offsets mean. `shift_references` is deleted. The structural rewrite is **unchanged**: callers convert to the absolute form at the boundary and back, which is where "is this reference past the insertion point" remains a question about addresses. **`<f t="shared">` became the ideal case**: the format already means "one tree, many cells", so an imported shared group now lands on one tree with no shift at all. **A defect the type change did *not* catch.** `OFFSET` computed `base.row + rows` — an **offset** used as an address. Both are integers, so it compiled; the test caught it. That is worth recording against the design's own claim that putting relativity in the type finds every site: it finds every site where the types differ, and `i64 + i64` is not one. **A side effect worth naming**: a snapshot round trip now reclaims trees no cell references, because the conversion is driven by the cells and an orphan has no origin. `store_formula`'s own comment had lamented that nothing reclaimed. Tested. **Verified**: 1 393 workspace tests, 129 browser tests, 13/13 repository gates, 5/5 compiler gates. **And it still broke `oracle-diff` on main, which is the finding.** `cargo test --workspace` is green because the fidelity tool has no tests of its own — it *is* the test, and it only runs where LibreOffice does. Two things in it stored formulas absolutely and were missed: the **corpus harness**, which places each line on its own row, so a formula on row 5 reading `C1:C10` resolved to `C6:C15` and every lookup came back empty against an oracle that had values; and `describe_cell`, which rendered `={expr}` **absolutely** in the comparison report. The second is the worse one — it is not cosmetic. Two *different* formulas can both render `#REF!` and compare equal, so the check quietly stops catching what it exists for. **The lesson is about which gates a change reaches.** `check-rust.py` runs the compiler gates and `check-all.py` the repository ones; neither runs the oracle, because it needs LibreOffice. A change to formula *semantics* is exactly the class that only the oracle sees, and the two runners gave a green that meant less than it looked. LibreOffice **is** available on this machine — `cargo run -p casual-calc-fidelity` reproduced the failure in one command, before and after. **`check-rust.py` now runs it**, so the gap closes rather than being remembered: three oracle steps after the compiler gates, **skipped loudly** when LibreOffice is absent (a gate that quietly does nothing is how four counters reported zero while being scraped — `SRV-05`) and skipped when a compiler gate already failed, since there is no point asking the oracle about a tree that does not build. Proved by reintroducing this exact regression: `FAIL oracle: corpus`, while package and ods stay green. **Seen red**: making `store_formula_at` skip the re-store fails both sharing tests *and* 82 evaluator tests.  The design says a half-migrated codebase is worse than not starting, and starting the migration before the serialisation question was settled is precisely how that happens. | N filled-down cells hold one AST |
| PERF-10 | Resident memory is still not measured | Done | P2 | The row's own reasoning was right and is what held this open: an uncalibrated gate is worse than none, no machine here has `/proc`, so the threshold could only come from the runner. It now has, from **thirteen successful CI runs**: 82.32 x2, 82.41 x2, 83.64 x4, 83.72 x5 — a spread of 1.40 bytes, or **1.7%**. The measurement is very nearly deterministic, which is what makes an absolute ceiling honest rather than a guess dressed as one. **Ceiling 88**: 4.28 bytes over the highest ever seen, so 3.1x the observed spread and it will not flake, while still catching one extra pointer per cell (+8 bytes to 91.7) — the smallest change that would actually matter. **Floor 70**, and that half is the one worth explaining: a sudden *drop* is far more likely to mean the measurement broke than that the engine got 16% smaller, and a gate with only a ceiling passes forever once it starts measuring the wrong thing. That is precisely how four counters on this project reported zero while being scraped (`SRV-05`). Verified by extracting the step's shell and running it against synthetic reports rather than by reading it. | A cell costs between 70 and 88 bytes on the runner; driven with 82.32, 83.72, 88.00, 88.01, 69.99 and 91.70 and each answered correctly |
| PERF-09 | No AST interning | Done | P1 | `store_formula` appended unconditionally. The row named the duplicate cost; the larger problem was unnamed — **nothing reclaimed**, so every edit replacing a formula orphaned the old AST and a person editing one cell back and forth grew the arena once per edit for the session's life. A thousand edits between two formulas left a thousand ASTs. Now interned by fingerprint, with equality deciding, so the index does not hold a second copy of every AST. The index is derived state and not serialised, so it is rebuilt on load — without which a round trip silently restored the old behaviour. | `editing_one_cell_does_not_grow_the_arena_forever`, `interning_survives_a_snapshot_round_trip`, `a_colliding_fingerprint_does_not_return_the_wrong_formula` |
| PERF-07 | Cold and adversarial recalc unproven | Done | P1 | Worse than the row said: the <50 ms cap was asserted **nowhere**. Every recalc case was a scaling *ratio* — how the work grows, never how long it takes — while docs/30 calls T3 a hard cap and says a PR past it fails CI. Two absolute cases now: the cold first edit, with the precedent graph built inside the clock, and the adversarial sheet docs/30 defines — a 5,000-deep chain and a 5,000-wide fan-out, edited at the chain's root, which is also what the fan reads, so one keystroke dirties depth and breadth at once. Gated on the **worst** sample, not the median: T3 says worst-case, and a median hides the tail a person actually complains about. | `recalc-cold-first-edit` 2.90 ms and `recalc-adversarial-chain-and-fanout` 3.22 ms against the 50 ms cap, release |
| PERF-08 | Memory gate measures payload, not RSS | Done | P1 | Three things, each a gate that ran and passed without gating what it described. (1) **CI measured a debug build** — the same frame is 0.13 ms in release and 5.7 ms in debug, so a "sixty frames a second" gate was applied to a binary nobody ships; the job now runs `--release` and the report carries `profile`, which CI asserts. (2) The frame ceiling was **four whole frames**, enforcing fifteen fps against a comment and a doc that both said sixty; it is now docs/30's engine-side working budget (≤8 ms of a 16.6 ms frame), 65× above the release measurement. (3) The memory gate was `size_of` × a target — arithmetic that passes unchanged if the store boxes values or cannot reach a million. A million cells are now built. RSS remains unmeasured (PERF-10). | `the_frame_budget_is_the_engine_side_working_budget`, `a_million_cells_can_actually_be_held`, and CI rejecting a `profile: debug` report |
| UX-FREEZE-01 | A freeze can only be made from a menu, never by dragging | Done | P2 | `freezeHit` matches the divider, and the divider is drawn at the freeze line — so with nothing frozen there is nothing to grab. Every drag gesture operated on a freeze somebody had already made from the menu, and no gesture made one, which is the affordance a user reaches for first. Two corner handles now start the drag; they live inside the select-all box because a grab zone on the body's leading edge would sit on column A and swallow selection clicks. | `editor.grid-handles.spec.mjs` — dragging the corner handle freezes columns |
| UX-HIDE-01 | Reported: a hidden band cannot be revealed from its handle | Rejected | P2 | Reported by the user, and **not reproduced**: `mousedown` already checks `hiddenMarkAt` before the resize boundary, and a browser gate now shows one click on the handle revealing the column. A first reading of the handler missed the existing check and nearly added a duplicate. Kept as a row because the report was real — if it recurs the gate names the case that does work, which narrows what to look for. | `editor.grid-handles.spec.mjs` — a hidden column is revealed by clicking its handle |
| UX-CUT-03 | A cut does not repoint formulas that referenced it | Done | P1 | Excel rewrites every other formula pointing at cut cells so it follows them. Only the moved cells' own formulas are handled. | A formula referencing a cut cell follows it Confirmed independently by the docs/46 audit (CP-040), which adds the reason it stays unnoticed: the visible parts — marching ants, the source cleared in one undo step, Esc disarming — all work, so a cut *looks* complete while every other formula keeps pointing at the old addresses. **Fixed.** `repointed_after_move` in `casual-calc-transaction::structural` decides which references named a moved cell, reusing the qualified/unqualified home-sheet rule the insert/delete rewrite already uses so the two cannot disagree about what "this sheet" means. The wasm paste path folds the rewrites into the cut's own op batch, ahead of the paste, so the move stays one undo step and a repointed cell the paste also lands on keeps the pasted content. `$` anchoring does not exempt a reference: it governs what a *copy* does, not whether the cell named may move. A range only partly inside the block is left alone -- moving one endpoint would silently resize somebody's range. Gated by two tests in `casual-calc-wasm`, each seen red under five mutations (repoint disabled; block bounds dropped; `$` treated as exempt; sheet-reaching forced true; qualified references ignored) with every failure landing on the assertion that owns it. |
| UX-CUT-04 | A cut does not repoint the defined names that pointed at it | Done | P1 | Sibling of `FID-24`, which made an insert or a delete shift defined names. A cut left them behind, so `Rate` went on meaning `$A$1` after `$A$1` had gone to `G6` and every `=Rate` silently read a different cell. Worse than the formula case it was found beside (`UX-CUT-03`): a name is the indirection people reach for *so that* they need not track addresses, and nothing on screen shows a name has gone stale. Found by probing the paste path for `defined_name` after fixing UX-CUT-03 and getting zero hits. **Fixed.** `defined_names_after_move` reuses the same expression rewrite and the same sheet-scoped/workbook-scoped home rule as `FID-24`, and the wasm paste path pushes the result as `Operation::SetDefinedNames` -- which inverts -- into the cut's own batch, so the move stays one undo step. Gated in `casual-calc-wasm`, seen red under three mutations: repoint disabled, block bounds dropped (the untouched-name control), and the op applied outside the batch (the undo-step claim). |
| COL-40 | Owner cannot change access or lock a live document | Done | P1 | A session-scoped override on top of the token, never instead of it (docs/72). The safety property is that it may only ever **reduce**: the enforcement point takes the more restrictive of token and override, so a compromised client cannot promote itself and the token stays the ceiling — granting more means minting a token, which involves the system of record. Who may set it is a separate `owner` claim, not inferred from `Access::Edit`, because every editor being able to lock out every other one is a different feature and a worse one. Ephemeral by ADR-012: an evicted document returns with the token's own permissions. The affected participant is told in words rather than discovering it when a keystroke stops working. | `an_override_cannot_raise_access_above_the_token`, `an_editor_who_is_not_an_owner_cannot_reduce_anyone`, `an_owner_can_reduce_another_participants_access` |
| COL-32 | Filter sharing and personal views | Done | P1 | Built to [71](71-FILTER-SHARING-AND-VIEWS.md). A personal view lives on `WorkbookSession`, never on `Sheet`, and that placement **is** the design: `SUBTOTAL`'s 101-111 codes and `AGGREGATE` reach the sheet, so anything stored there is shared by definition, and a personal view that moved a subtotal would let two participants hold different numbers for one cell - which makes the convergence property every other part of the collaboration design assumes simply false. Two questions that look identical and are not: the layout asks the session and gets shared union personal; the evaluator asks the sheet and gets shared only. The engine computes *which* rows a value-set hides (`session_rows_hidden_by_values`) using the same helpers the shared path uses, so the two cannot disagree about the same tick-boxes - only where the answer is stored differs. The scope choice defaults to **shared**, because that is what a spreadsheet has always done and the only kind `.xlsx` can express. **Two things learned in the building:** the file format has no separate notion of a filter-hidden row - ECMA-376 stores one as `<row hidden="1">` - so a shared filter returns from a round trip in `hidden_rows`, and the first version of the save test asserted on `filter_hidden` and failed. And a view keyed by sheet index needs resequencing on insert, remove and move, or it silently goes on hiding rows on whichever sheet inherits the number, with nothing on the wire to explain it and nothing in the history to undo. Named, shareable view *definitions* remain out of scope, as 71 says: that is a document-level list, which is a new operation, which is an ADR trigger. | Browser: a personal filter hides rows and moves no value; clearing restores them; the scope choice defaults to shared. Rust: nothing relayed, nothing in the history, nothing saved, subtotal unmoved, one session's view invisible to another |
| UX-FOCUS-01 | The grid never takes the keyboard until it is clicked | Done | P1 | Reported from a running Docker stack: Cmd+A and Cmd+X "not supported". Both **were** implemented — `k === "a"` calls `ctrlA()`, `k === "x"` calls `doCut()`, and the modifier test is `e.ctrlKey || e.metaKey`, so Cmd was handled everywhere. The handler is bound on `<canvas id="grid">`, and **nothing focused it at startup**: the editor opened with `document.activeElement === body`, so every grid shortcut — select-all, cut, copy, the arrows, Ctrl+Arrow, the lot — did nothing until the user happened to click a cell. On macOS Cmd+A then ran the *browser's* select-all over the page chrome, which is exactly what it looked like from outside. Fixed by focusing the canvas at the end of `main()`, and **only when nothing else holds focus** — an embed lives in somebody else's page and a component that steals focus on load moves the caret out of whatever the person was typing in. **Seen red without the fix**: `activeElement` is `BODY#`, Cmd+A leaves the selection at `{r0:0,c0:0,r1:0,c1:0}`, and Cmd+X leaves the status line reading `engine v0.0.0` instead of `cut`. **What made it invisible**: 53 `canvas.focus()` calls existed to put focus *back* after every dialog, menu and colour picker — so focus was correct in every state the tests drove, because every one of those tests clicked the grid first. The missing call was the one that puts focus there to begin with, and no test started from a keyboard. | The grid answers a shortcut on a freshly opened document, with no click first |
| FID-25 | Four more position-indexed things a row insert never moved | Done | P1 | Found by driving the editor rather than by reading it: insert a row above a validated block and the rule went on policing the rows the data had **left**. `shift_metadata_insert` moved merges, the autofilter, filter-hidden rows, sizing, hidden lines, the freeze boundary, the outline and — since the table fix — tables. The sheet also holds **validations, conditional formats, comments and hyperlinks**, every one a range or an address, and none of them moved. **The same shape as the table defect and just as quiet**: no error, no compatibility report, a saved file that looks right. The consequences are worse than losing them — the row that moved *out* keeps the constraint and the row that moved *in* is unguarded, so a person typing in the cell they just created is refused and the cell they meant is not; and a comment left behind annotates whatever moved into its row, which reads as though it belongs. **They were in the metadata bundle already**, so a delete has always restored them; what was missing was only the *shift*. That is why the fix goes in `Positional` beside merges rather than next to it — the trait exists because "a shift implemented twice is a shift that disagrees with itself the first time a field is added", and this is that field, four times. Ranges clamp-or-drop on delete exactly as a merge does; a comment names one cell and goes with its row. **Seen red before the fix**: `left: (4, 7), right: (5, 8)` — the validation on the rows its data left. Six tests, including that undo restores a validation a delete swallowed, which is the half that proves the bundle was already doing its job. **Still open, and filed rather than fixed here**: `charts` and `pivots` are position-indexed too and are *not* in the bundle — a chart's series is a reference **string**, so moving it means parsing and re-emitting rather than shifting a `CellRef`. Confirmed by probe: a chart's series stayed on `$D$2:$D$11` across an insert. `FID-26`. | A validation, highlight, comment and link move with their rows, and a delete's undo restores them |
| FID-26 | A chart's series and a pivot's ranges never move either | Done | P2 | The other half of `FID-25`. `charts`, `images` and `pivots` were the last three position-indexed collections `structural.rs` never touched. **Confirmed by probe**: insert a row above a chart's data and the series stayed on `$D$2:$D$11` and the frame did not move, so the chart plotted the wrong rows from the wrong place, silently. Three different shapes, so three mechanisms. A chart's and an image's **frame** is a range and moves like a merge. A chart's **series** is a reference *string*, so it is parsed, put through the same `rewrite_expr` cells and defined names already use, and printed back — text that will not parse is left alone, on the rule `Expr::Raw` already follows, because a reference that cannot be understood cannot be shifted without guessing and a guess here corrupts a chart. A **pivot** splits: `anchor` and `output` are on the sheet holding it, `source` is on `source_sheet` and very often another sheet entirely, so the source shift runs at workbook level where the two identities can actually be compared. Frames and pivot blocks went on the `Positional` trait, so the OT transform path gets them too; images and pivot sources could not, and the reason is recorded on the trait rather than left to be rediscovered — `SheetMetadata` has no images and no sheet identity, and a trait method one implementor cannot answer honestly is a lie. A lost `output` clears to `None` instead of dropping the pivot: the definition is still valid and a refresh will write a new extent, where dropping it would delete a report the user can still see. **Complete for an authored chart, partial for an imported one** — see `FID-27`. | `insert_rows_moves_charts_images_and_pivots`, `insert_rows_moves_a_pivot_source_and_output_independently`, `delete_rows_clamps_charts_and_images_and_breaks_lost_series`, `deleting_a_pivot_output_band_keeps_the_pivot`, plus quoting and cross-sheet controls. Insert path seen red first; every delete-path behaviour then mutated one at a time and each fails its own assertion |
| FID-27 | An imported chart shifts on screen but not in the saved file | Done | P2 | `FID-26` shifted the model; a chart read from a file is written back from its **retained part**, so the picture moved and the saved bytes did not. **The design fork this row was filed to decide turned out not to exist.** It looked like a choice between breaking the *inert retained part* invariant and calling `detach` and losing the formatting retention exists to keep. Reading the writer settled it: export only ever *reads* `retained_parts` and emits new bytes — `strip_dangling_anchors` already parses and edits retained drawing XML on the way out — so the invariant is about the **model**, not about the bytes leaving the writer. There was a third option present in the codebase the whole time. The corrected part is emitted as a *generated* part at the same path, and the writer's existing "a part we also generated wins" rule does the rest. `RetainedPart` is never touched. Only the text inside a series' `<c:f>` changes — `<c:tx>` is left alone, because a series *name* is not a position — and an unchanged chart produces no generated part at all, so it stays byte-identical. **A test that passed first time and was wrong to.** The end-to-end case asserted that `<c:serAx>` and `<c:valAx>` survive, and the doc comment claimed this proved the matcher does not match on a prefix. Mutating the matcher to do exactly that left the test green: valid OOXML orders the axes outside the series, so ordering alone spares them. The claim was corrected and the guard given tests that do fail — matching `ser` inside `serAx`, and a closing tag matching `</c:serAx>` for `<c:ser>`. | `a_retained_chart_part_is_re_emitted_with_its_shifted_series` and `an_unshifted_retained_chart_part_is_written_back_unchanged`; the fix mutated away reproduces the defect verbatim (`$D$2:$D$11` still in the saved part). `chart::tests` covers whole-name matching, each seen red under its own mutation |
| FID-29 | An imported chart's *frame* still does not move in the saved file | Done | P3 | The remainder of `FID-27`. A chart's series live in its own part, which `ChartView::part` names outright; its **frame** lives in the retained *drawing*, where nothing names the chart. The link is the anchor's `r:id`, so matching an anchor to a modelled chart means resolving that id through the drawing's own relationships to a part path and comparing it with what the chart says it came from. Images work the same way through `r:embed` and their media part. Only the four cell coordinates change; offsets stay exactly as the file had them, because a row insert moves a frame by whole rows and rewriting the offsets would snap every edge that was ever dragged between gridlines. `oneCellAnchor` has an extent rather than a `<xdr:to>`, so only its corner moves; `absoluteAnchor` names no cells and is left alone. A media part placed twice gets a queue rather than a lookup, so the nth anchor naming it takes the nth frame. | `a_retained_drawing_anchor_follows_the_frame_the_model_holds`, seen red first with the file still saying `<xdr:row>4</xdr:row>` where the model said 6 |
| FID-30 | Deleting an imported chart makes the workbook refuse to save | Done | P1 | **Found while doing `FID-29`, and much worse than it.** Not a fidelity defect — a total failure to save. Deleting an imported chart leaves a dangling anchor; stripping it rebuilds the drawing, so the sheet contributes a drawing part with **no chart parts**. Two places then write that drawing's `.rels`: the chart builder writes it for every non-empty `drawing_part`, and the retained-relationship pass writes it for every source it has not already seen — but that pass only stepped aside for a build that had *chart parts*. With none, both fired, the package got two entries at one path, and the zip writer rejected the whole file with `Duplicate filename`. Having a chart part was never what caused the rels to be written; a non-empty `drawing_part` is. The guard now says so. **What made it invisible**: the enclosing comment 30 lines above already stated that a sheet can contribute a drawing with no chart parts — the guard simply did not match its own documentation, and no test exercised the combination. It surfaced only because `FID-29` made the drawing bytes change on a path that had nothing to do with charts. | `a_drawing_rebuilt_without_chart_parts_does_not_write_its_rels_twice`, seen red on **unmodified `main`** with `Package("invalid Zip archive: Duplicate filename")` |
| FID-31 | Twice now, an end-to-end test claimed to prove whole-name matching | Done | P3 | Worth its own row because it is a *method* defect, not a code one, and it recurred. Both the `FID-27` and the `FID-29` end-to-end tests asserted that a lookalike element survives — `<c:serAx>` beside `<c:ser>`, `<xdr:colOff>` beside `<xdr:col>` — and both doc comments claimed this proved element names are matched whole rather than as a prefix. Mutating the matcher to match on a prefix left **both** green. The reason is the same each time: prefix matching only bites when the **longer name comes first**, and valid OOXML writes the shorter one first in every case that appears — `col` before `colOff`, the axes outside the series. An end-to-end case built from realistic XML therefore *cannot* tell the two matchers apart, and reads as coverage while proving nothing. Both claims were corrected to say what the assertions actually cover, and the guard is now proved by direct cases written in the biting order, each isolated so an earlier assertion cannot shield it. | `chart::tests::a_frame_coordinate_is_not_read_out_of_its_offset` and siblings; under prefix matching the column reads as `12700</xdr:colOff><xdr:col>5` |
| FID-28 | A pending metadata bundle's series and pivot source do not transform | Done | P3 | **The row said this needed a wire change and a `PROTOCOL_VERSION` bump. It does not, and that claim was never checked.** A chart's series is a reference *string* and a pivot's `source` is on another sheet, so neither can be decided from the sheet *index* an operation carries — the conclusion drawn from that was that identity had to travel on the wire. But `transform` being a **pure function** does not mean it may not take more arguments; it means it may not go and look something up. Both its callers — `receive` and `commit` in `session.rs` — already have `&mut Workbook` in scope, four lines from the call. So they pass what the indices name, the wire is untouched, and `PROTOCOL_VERSION` stays at 6. The alternative that was nearly built instead — two new fields on each of four structural operations — would have meant ~200 construction sites and a protocol break, to carry data the caller already held. | `a_pending_chart_series_follows_a_concurrent_insert`; with the identities withheld it reinstates the pre-insert `S!$D$2:$D$11` |
| UX-THEME-02 | A filled cell's text is chosen against the theme, not the fill | Done | P1 | Reported from a running editor: *"font color change on dark light mode change but not bg of cell .. making it harder to see"*. Exactly right, and the asymmetry is the bug: a cell's **fill comes from the file**, so it is the same colour in either theme, while `cellFg` fell back to `colors.fg` — a *theme* token. Switching to dark mode therefore put near-white text on an authored pale fill and the contents vanished. `contrastInk` already existed, already computed WCAG relative luminance, and was already used for column headers — it simply was not consulted for cell text. An explicit font colour still wins: that pairing is the author's, and repainting it would be second-guessing somebody's formatting. | `editor.cell-contrast.spec.mjs`; reverted, a yellow fill takes the theme's `#0b0d12` instead of `#111418`, and follows the theme into dark mode |
| UX-RESIZE-01 | A column resize does not reflow its contents until the mouse is released | Done | P1 | Reported as *"not realtime .. untill i leave it .. than content rearrange .. not fluidly"*. Half of it was already live: the client overrides the dragged line's size in its own `geo`, so the **edge** moved. The **text** did not, because display text comes from the engine and the engine still held the old width — so the edge slid out from under stationary content and everything snapped on release. The obvious fix is the wrong one. `session_set_col_width` records an undoable transaction, so calling it per mouse-move would bury the undo stack and make one Ctrl+Z step back a single pixel. `session_preview_line_size` writes straight to the sheet instead — no transaction, no history, nothing relayed to a collaborator, because a drag nobody has finished is not an edit anyone else should see. The commit path restores the size the drag *started* from before recording the real edit, so undo inverts to that rather than to the last frame. | `editor.resize-live.spec.mjs` asserts both halves — the engine sees the width mid-drag, and nothing is undoable until release. Without the preview the width stays at 64 for the whole drag |
| UX-FREEZE-02 | A frozen pane looks like it cannot be extended or removed | Done | P3 | Reported as no way to extend or remove a frozen column. Both already existed — an **Unfreeze** item in the freeze menu, and `commitFreezeDrag` maps a drag back to the header as zero — so this was discoverability, not a missing feature. `FREEZE_GRAB` was **4px** against a 1px divider: under half a fingertip, so a deliberate aim missed more often than it caught, and the only reliable route was two clicks inside a popup nobody opens looking for it. 8px is the smallest that catches an aim without stealing clicks from the cell beside it — the divider sits on a gridline, so a much wider zone starts swallowing selections. | The existing freeze-drag gate still passes at the wider target |
| UX-CLIP-02 | A paste cannot carry column widths, even when asked | Done | P2 | Reported from a running stack: pasting from LibreOffice "doesn't keep width and height". **It does not, and `68` records that as a decision** — applying them on a plain paste silently reshapes the sheet somebody pasted *into*, and Excel's own `Ctrl+V` does not do it either. What Excel has and this did not is the **explicit** option, so there was no way to ask for the thing the person actually wanted. Paste Special gains **Column widths only**. The clip captures the source columns' widths **on copy**, not on paste: by then the source may have been resized, or after a cut may not be there at all. Only columns with an *explicit* width travel — a column sitting at the sheet default has no width to carry, and writing the default onto the destination would pin a column that had been following it. Widths and not heights: a row's height belongs to the row, and a paste that set those would resize rows the copied block merely passed through. **Seen red**: disabling the mode fails with *"the column width did not travel"*, while the two guards stay green — a plain paste still reshapes nothing, and a default-width source still overwrites nothing. That is the point of having all three: the feature and the two things it must **not** become. | Paste Special carries the source columns' widths, and a plain paste still does not |
| UX-EDIT-01 | The editor has no width breakpoint at all | Done | P2 | Found by checking the one surface nothing had ever measured at a phone width. `editor.css` has seven media queries and **not one of them is about width** — they are dark mode, reduced motion and contrast. `.menubar` and `.toolbar` are unwrapped flex rows, so on a 390px phone they ran past the edge and widened the document to **779px**: the page slid sideways, and half the toolbar could only be reached by dragging the whole editor across. Same at 768px. They scroll **within themselves** rather than wrapping. Wrapping changes the editor's height and every pixel of that comes out of the grid, which is the one part of a spreadsheet nobody wants smaller — Excel's answer on a narrow window is to collapse groups and then scroll, which is the step after the existing `.tb-collapsed` mechanism runs out of groups to fold. The scrollbar is hidden because these are chrome rather than content, and a visible track costs 8–15px of a 48px bar on platforms that reserve space for one. **A mutation that passed, and why.** The first attempt to prove the test flipped `overflow-x: auto` to `visible` — and everything stayed green, because `overflow-y: hidden` is still set beside it and CSS computes the other axis back to `auto` when one is not `visible`. The mutation had changed nothing. Removing the block outright is what actually reproduces it. A mutation has to be checked for having *happened* before its result means anything. | `editor.narrow-screens.spec.mjs`; without the block the document is 779px in both a 390px and a 768px window. It also asserts the toolbar can be scrolled to its far end — 401px of controls lie past the edge, and a *clipped* toolbar would report nothing unreachable just as a scrolling one does |
| UX-SITE-03 | The site scrolls sideways on every phone | Done | P1 | 763px of document in a 390px viewport on the landing page; 706px and 756px on `deploy` and `docs`. Every phone visitor got a page that slid horizontally. **`overflow-x: auto` was already on several of the `<pre>` blocks and never engaged.** A grid and flex item defaults to `min-width: auto` — *do not shrink below your content* — so the card grew, the row grew, and the scrollbar the `<pre>` was meant to gain never had a reason to appear. `min-width: 0` is what lets the overflow rule do its job. **Three separate mistakes on the way, each caught by measuring rather than reasoning.** (1) A catch-all `html, body { overflow-x: clip }` looked like sensible insurance; a clip makes the element a scroll container and I then measured the page as unable to scroll at all. That measurement was itself wrong — `scroll-behavior: smooth` means `scrollY` is not updated synchronously — so I nearly reverted a working page on a broken check, and then nearly kept a guard that hides overflow instead of fixing it. Removed: a guard that can silently disable the page is worse than the overflow. (2) The `min-width: 0` rule enumerated six selectors and missed `.doc`, so both documentation pages stayed broken after the 'fix'. (3) The `<pre>` rules named `.step pre`, `.code-card pre`, `.contrib pre` — and the blocks actually forcing the page wide were **bare** `<pre>`s with no class, matching none of them. Both are the same error: a rule that has to list its subjects is one omission away from being wrong. Tables scroll inside themselves rather than collapsing, because a documentation table's columns *are* the content. | `site.no-sideways-scroll.spec.mjs` — 3 pages × 3 widths. It asserts the document is no wider than the window **and** that anything still wider sits in something that can scroll it into view, because clipping the page would satisfy the first check while making the content unreachable |
| UX-SITE-02 | Dark mode shipped on one page of three | Done | P2 | `UX-SITE-01` added dark mode as a token swap and I verified it on the landing page — which is the only page whose styling lives *entirely* in `style.css`. `deploy.html` and `docs.html` each carry their own `<style>` block with literal light values for inline code and the two callout tints, so both shipped painting white boxes onto a dark ground. Readable enough to miss at a glance, wrong on any dark machine. **Found by checking the pages I had not looked at**, immediately after merging — the same omission as the round before it, where three passes went into a stylesheet without once rendering the page. The habit that keeps catching things is looking; the habit that keeps missing them is assuming the one sample generalises. The literals now read `--code-bg`, `--code-fg`, `--note-bg` and `--note-warn-bg` from the shared sheet, so a page-local copy cannot drift from it. The dark values are tinted toward the accent and the warning hue at low saturation: the light tints are washes on white and become glare on black. Also removed a duplication I had introduced in the same paragraph — the three image names appeared twice in consecutive sentences. | `site.dark-mode.spec.mjs` walks **every** page: nothing of consequence may paint near-white, and the body must actually be dark, so a page that never applied the theme cannot pass by having no offenders. Restoring one literal fails it |
| UX-META-01 | A workbook's own properties are invisible, and xlsx never read or wrote them | Done | P1 | Reported: *"we don't even have a way to show file properties, metadata and others"*. True, and the missing UI was the smallest part of it. `DocumentProperties` — title, subject, description, keywords, creator, last-modified-by, created, modified, language — has been in the model throughout. **The `.xlsx` reader never parsed `docProps/core.xml` into it** (the part was *retained* as bytes and nothing more), and **the writer never emitted one**. So an Excel file's author and title were invisible to every host, and anything authored here was lost on save. `casual-calc-ods` has parsed the equivalent `meta.xml` since `ODS-03`, so the two formats disagreed about whether a document knows its own name. Reader, writer and a File ▸ Properties dialog. Five fields editable; `created`, `modified`, `lastModifiedBy` and `language` shown **read-only**, because those are what happened rather than opinions about it and a text box there invites writing a false history into a document. **Two bugs the tests caught before this shipped.** The round-trip assertion failed `Expected "Q3 Forecast", Received ""` — the dialog looked perfect and persisted nothing, which is worse than no dialog because it tells somebody their document is attributed when it is not. Then generating our own part **replaced** an imported file's `core.xml`, losing the producer extensions this model does not hold; `parts_attached_at_the_package_root_survive_a_save` caught that. The retained bytes are now edited **in place** — each element the model holds has its text replaced and everything else survives, so an unedited workbook saves the bytes it arrived with. That is `FID-27`'s rule and its method. | `editor.document-properties.spec.mjs`: properties set in the dialog survive a save and reopen; the dialog separates editable from filed; Escape abandons rather than saves |
| UX-KEY-01 | Excel's keyboard shortcuts had no gate, and an audit without one reported six false failures | Done | P1 | Raised under the migrating-user priority — *"cognitive burden while a user transfers from Excel to our app"* — whose sharpest edge is a shortcut the hands already know doing nothing. **The audit found no such shortcut. It did find that my probes were worse than the editor.** `F2`, `Alt+=`, `Ctrl+;`, `Ctrl+Space`, `Shift+Space`, `` Ctrl+` `` and `Ctrl+PageDown`/`PageUp` all work and always did. Six were reported broken and **the wrong claim reached the body of `UX-META-01`'s merged PR** before it was checked; it is corrected here rather than only there, because a false 'this is broken' costs the next reader the same hour it cost me. Three distinct probe defects, each of which agreed with the mistake: a key sent without focusing `#grid` first, which lands on `<body>` and is bound to nothing; **`session_active_sheet()` read as the displayed sheet when it reports the workbook's saved `tab_selected` flag** — the sheet a reader *opens* on, which does not move when a user switches tabs; and a whole-column selection expected to span a million rows when it spans the used range, which is the better behaviour and was scored as a failure for not being the worse one. **What made it invisible: nothing asserted any of this, so there was no red to contradict me.** An engine surface with no gate is one where a confident wrong report and the truth look identical. The gate is the fix, and it is written against observables each checked to mean what it is read as saying — the visible `.sheet-tab.active` label rather than a flag that sounds like it. | `editor.excel-shortcuts.spec.mjs`: seven shortcuts, each seen red under a mutation — `mod` forced false takes the four `Ctrl` tests, `e.altKey` forced false empties `Alt+=` to `""`, and renaming `case "F2"` leaves `#inline-edit` hidden |
| UX-KEY-02 | Three Excel chords the engine could already serve reached nothing, and the two docs describing them disagreed with the code in opposite directions | Done | P1 | Follows `UX-KEY-01`, which gated the shortcuts that work. These are the ones that did not, and each is the same shape: **the capability existed and no key reached it** — the fourth instance of that pattern after `autoSum`, `DocumentProperties` and `build_set_op`. **`Ctrl+Shift+L` left-aligned instead of toggling the filter.** Borrowed from Word. `toggleFilter` had been there all along, so the one chord an Excel user hits without looking made a silent formatting change and no filter — worse than a missing key, by the rule this repository already states at `Ctrl+9/0`: finger memory that is *wrong* costs more than finger memory that is unanswered. Left-align now has no chord rather than a made-up one; centre and right keep theirs because neither means anything in Excel and so neither can contradict it. **`Ctrl+H` opened nothing.** `#replace-input`, `#replace-one`, `#replace-all`, `session_replace_all` and `session_replace_at` all existed, reachable only by opening Find and tabbing across. `openReplace()` lands the caret in the replacement field, which is the whole point of a separate chord from `Ctrl+F`. **`Ctrl+Enter` was absent.** Now fills the selection through `session_fill`, so relative references adjust per cell — `=A1*2` across B1:B3 gives `=A2*2` in B2 — and it is **one** undo step rather than N. It routes through `commit` first, so the entry meets the same validation rule and formula guard as any typed value. **The documents were wrong in both directions, which is the finding worth keeping.** `docs/73` recorded five keyboard defects; three were real (fixed here) and two had gone stale — Alt+PageDown is not nested in the Ctrl branch (`editor.core.js:5930`, ahead of `if (mod)` on 5940) and Shift+Enter moves up, as Excel does. Meanwhile `docs/47` listed `Ctrl+X`, `Ctrl+Space`, `Shift+Space`, `F4`, `Ctrl+D/R`, `Ctrl+1` and `Ctrl+;` as **missing**, and every one of them was bound and working. **What made it invisible: prose was the only record, so it could drift in either direction and nothing went red.** Understating is not the safe direction — it invites somebody to rebuild what already exists. Both documents now defer to the spec rather than restating it. | `editor.excel-shortcuts.spec.mjs` grows to 9. Each of the three was seen red first: `Ctrl+Shift+L` gave `filter: null`, `Ctrl+H` left `#find-bar` hidden, `Ctrl+Enter` returned `["=A1*2", "", ""]` |
| UX-SITE-01 | The landing page is light-only, and its features are a 3×2 table | Done | P2 | Two things, found by **screenshotting the page** instead of reading its stylesheet — which is also how `UX-EMBED-01` was found, after three rounds of editing colours that were never the problem. **No dark mode at all.** The site had zero `prefers-color-scheme` rules while the editor has had nine throughout — and the hero *embeds that editor*, so on a dark machine the page rendered white with a dark application sitting inside it. Done as a token swap: every rule already reads the tokens, so there is no second stylesheet to keep in step. Not an inversion — `--panel` is *lighter* than the page in dark and darker in light, because a raised surface catches more light than what it sits on, and flipping the values literally would turn every card into a hole. `--ink` deliberately does not flip: that band is meant to be dark in both themes, so it becomes a *lifted* panel in dark rather than a darker hole. **Six features in three equal columns** is two tidy rows of three — the shape that says every claim carries identical weight, so none of them lead. A six-track grid with each row opening wider: 3+3 is a table, 4+2 has a subject and a footnote. Spans are confined to a `min-width` query, so the single-column layout is untouched. | Rendered and inspected in both schemes at 1440×900; the CTA band measured `rgb(11,13,18)` in light and `rgb(28,36,48)` in dark, which is the non-inversion behaving as intended |
| UX-EMBED-01 | The landing page loses its hero every time it loads | Done | P1 | Reported as the site looking wrong, and it was — but not in the stylesheet, which is where three rounds of looking went first. `index.html` embeds the live editor in an iframe below the fold, and **two independent things inside that editor scrolled the host document as it booted**. Between them the page arrived scrolled ~440px down: badge, headline and opening paragraph all pushed off the top. Every visitor saw a landing page with no headline on it. **One.** The boot focus (`UX-FOCUS-01`) guarded itself with `document.activeElement === document.body` and a comment saying an embed must not steal focus from the page around it. Inside an iframe that check asks about the **iframe's own** document, where nothing is focused — so it passed every time, and focusing the canvas made the parent scroll the frame into view. The intent was written down and never implemented. Now `window.top === window`, with `preventScroll` behind it. **Two.** `renderTabs` kept the active sheet tab visible with `scrollIntoView`, which walks **every** scrollable ancestor — and when embedded, the outermost is somebody else's page. `block: "nearest"` bounds how far each ancestor moves, not *which* ancestors move. Replaced with arithmetic on the strip's own `scrollLeft`, which cannot reach outside the element. **What made it invisible**: it looks exactly like a CSS problem, so that is where you look. It also cannot be seen from the editor's own page, where there is no host to scroll — and the earlier fix for it was reasoned about rather than run. Found by screenshotting the page and seeing the `<h1>` sitting at `y: -220`. | `editor.embed-does-not-scroll-host.spec.mjs`; each cause restored on its own scrolls the host 757px and 653px. Asserts `toBeInViewport()`, not presence — the element was in the DOM throughout the bug |
| UX-CLIP-01 | Cross-application rich HTML paste | Done | P1 | The inbound path: values, merges, fonts, colours, alignment and number formats all land. **Borders were the one thing left, deferred on a reason that did not survive being looked at.** `68` recorded them as unmappable because the CSS shorthand "collapses several of those into one declaration whose meaning depends on the table's border-collapse". `border-collapse` decides which of two *competing* declarations paints a **shared** edge — and the model has no shared edges: every cell carries its own four. Mapping what a cell declares for itself is exact, not approximate, and the property is simply never consulted. The captures agree: of the three, only LibreOffice carries a border at all, and it is a clean per-edge longhand. Weight maps to `thin`/`medium`/`thick`; `double`, `dashed` and `dotted` keep their style; `groove`, `ridge`, `inset`, `outset` become a solid line of the same weight, because Excel has no such styles and a line where a line was asked for is closer than nothing; `none`, `hidden` and a zero width are no edge. A per-edge longhand beats the shorthand, and an explicit `border-top: none` beats it too, so **presence** of the declaration is tested rather than what it parsed to. An edge the clipboard said nothing about is left alone, the same rule every other pasted property follows. The line-style token is checked against the OOXML list on the Rust side. Nothing is rejected today — the parser emits from a fixed set — but the token is written into the file verbatim, and a value that reaches the writer unchecked is one a clipboard could choose. Borders had no read path either: `session_range_bordered` answers only "are all four present across this whole range", which cannot see a cell carrying a bottom rule and nothing else. `session_cell_format` now reports the four edges, which the border picker can use as well. | Both tests seen red: without the mapping, `Expected "thin", Received undefined`. Then each branch mutated on its own — shorthand precedence (`none` lost to `thin`) and zero width (a line where there should be none) |
| WOPI-01 | No WOPI discovery, so no host can register OpenCalc | Done | P0 | Nextcloud, ownCloud, SharePoint, Moodle and Alfresco all integrate an editor by reading `/hosting/discovery`. Nothing served one, so none of them could be pointed at OpenCalc at all. Now served by `casual-calc-wopi`, advertising edit and view actions for every format the save leg can write back unchanged — a format it would rewrite is silent data loss with an administrator's blessing. That was `xlsx` alone until `WOPI-05`, and is `xlsx, csv, tsv, psv` since. | `an_edit_action_is_advertised_and_takes_parameters`, `only_formats_that_round_trip_are_advertised`, `discovery_is_served`, CI `docker-build` parses the XML |
| WOPI-02 | No WOPI action URL, so a host cannot open a file | Done | P0 | A host opens an editor by sending a browser to `<action>?WOPISrc=..&access_token=..`, which OpenCalc had no entry point for. `/wopi/edit` and `/wopi/view` now check the file, take a lock, mint the ordinary collaboration claims and serve the editor — so the collaboration server needed no changes to gain a WOPI integration. | `a_file_opens_edits_and_saves_back_to_the_host`, `an_unlisted_host_is_never_contacted` |
| WOPI-03 | No `CheckFileInfo`, so the file's name and rights are unknown | Done | P0 | WOPI carries the filename, rights and version in `CheckFileInfo`, not in the token. Calling it is also what *validates* the access token — we hold no key that could check somebody else's credential. `UserCanWrite` and `SupportsUpdate` are both required for a writable session: a host can claim the first without the second, and finding out at the save is the worst moment. | `write_permission_is_not_assumed`, `writing_needs_both_permission_and_a_host_that_accepts_it`, `read_only_is_enforced_in_the_token` |
| WOPI-04 | No WOPI locking, so SharePoint cannot save | Done | P1 | `PutFile` against a locked file is a 409 without a matching `X-WOPI-Lock`, and SharePoint locks on open. Lock, RefreshLock and Unlock are implemented in the adapter, refreshed on a timer rather than on activity — a document left open over lunch is exactly the one whose lock must survive — and released on close, on expiry and on shutdown. | `the_lock_operations_are_named_on_the_wire`, `a_save_is_addressed_the_way_wopi_specifies`, `locks_come_due_on_a_timer`, `a_file_locked_elsewhere_still_opens` |
| WOPI-05 | A save always emits xlsx, so only xlsx can be advertised | Done | P1 | The save leg wrote an OOXML package whatever it opened, so a host handed `books.csv` got a zip back under that name and lost the original — and discovery had to advertise `xlsx` alone to stay honest. A session now **remembers the format it opened** (`SessionFormat`, set where the bytes are read, because by save time nothing on a `Workbook` says how it arrived) and `save_as` writes any format from any session. The WOPI adapter converts on both legs — it is the only process that holds the host's format *and* talks to a collaboration server that reads packages and only packages — so `EDITABLE` is now `xlsx, csv, tsv, psv`, held to `SessionFormat::for_extension` by a test rather than to a comment. A filename whose extension that table does not know is **refused before the file is locked**, where the old code assumed `.xlsx`. What CSV cannot carry is counted and named — sheets, formulas, formatting, merges, panes, validations, comments, charts — in the compatibility report, logged per save and shown in the session chrome; `record_n(…, 0)` records nothing, so a clean CSV does not cry wolf. **What made it invisible:** every layer round-tripped perfectly *in its own format*, and no test ever asked one layer for another layer's format. `session_save()` in the WASM bridge was deliberately left xlsx-always for the same reason — the editor's "Save as Excel" names its download `.xlsx`, and making that call format-native would have reintroduced the identical defect pointing the other way (`session_save_native` is the one that follows the format). | `a_csv_opens_as_a_package_and_saves_back_as_a_csv`, `a_csv_save_names_the_sheets_and_formulas_it_cannot_carry`, `a_format_this_service_cannot_write_is_never_opened`, `only_formats_that_round_trip_are_advertised`, `a_filename_names_the_format_and_an_unknown_one_names_nothing`, `a_save_is_addressed_the_way_wopi_specifies`, `an_edited_csv_session_saves_csv_that_reads_back_with_the_edit`, `converting_an_untouched_session_does_not_return_the_original_bytes`; mutations give `assertion failed: the host's .csv was overwritten with something else: expected "Item,Qty\r\nWidget,7\r\n"`, `a second sheet and a formula were dropped and nothing said so`, and `` `csv` is advertised, and a save would rewrite it as something else `` |
| WOPI-07 | `.ods` is still not advertised, because nothing reads or writes one | Done | P2 | The row text was stale twice over. `casual-calc-ods` already had a reader **and** a writer; what was missing was that `SessionFormat` had no `Ods`, so nothing could *remember* a session had come from one — the same defect `WOPI-05` closed for `.csv`, one format later. `SessionFormat::Ods` now records the format at open (with the untouched-saves-byte-for-byte guarantee), `save_as` writes ODF, `loss_writing` delegates to a new `export_loss`, and `.ods` is in `EDITABLE`, so both actions appear in discovery. **Advertising it first required fixing five defects in the converter, four of them silent corruption.** (1) `export_ods` **wrote no formulas at all** — a `.ods` opened and saved came back a table of constants, every formula frozen to its last value; main's whole crate contained one mention of `table:formula` and it was in the *reader*. (2) The reader could not parse ODF's `;` argument separator — the one `of:` actually uses — so every multi-argument function in every real LibreOffice file was dropped and reported. (3) A self-closing `<table:table-row number-rows-repeated="N"/>`, which is exactly how LibreOffice writes the gap between two blocks of data, never advanced the row cursor, so the second block landed on top of the first. (4) `MAX_REPEAT` was clamped on the cursor rather than on materialisation, shifting everything after a gap of more than 4,096. (5) XML entities were never unescaped, so `Ada & Co` grew four characters on every save and any formula containing `&` stopped parsing. **What made all five invisible: every one produces a file that opens without complaint.** They were found by round-tripping a real LibreOffice fixture and asserting on the *bytes*. **What the format still costs, named not silent:** the writer keeps values, formulas and sheets only — formatting, merges, widths, validation, conditional formats, comments, hyperlinks, print setup, charts, images and defined names are counted by `export_loss` and logged on the WOPI save leg; the reader models less still, and that loss is now logged on the read leg too. Error values are written as text and reported `Degraded`. Function-name mapping to OpenFormula is not attempted, so a function ODF spells differently will show `#NAME?` in LibreOffice. | A LibreOffice `.ods` opens through WOPI and saves back as ODF with its formulas intact; `.ods` appears in `EDITABLE` and in discovery. Nine tests seen red first, six mutations caught |
| WASM-01 | The browser editor cannot open anything but `.xlsx` | Done | P1 | The bridge hard-coded `SessionFormat::Xlsx` and the page kept a **second** extension table, so a `.csv` was openable through the WOPI service and not in the editor that almost everyone uses. There were in fact **five** copies of "which formats we support" — the bridge, `openBytes`, the picker's `accept`, and two doc strings — all drifting independently, and each individually plausible. Only `.tab`, which `casual-calc-io` has always read and the picker never offered, showed they had diverged. All of it now delegates to `SessionFormat`, with a catch-all match arm so a format the SDK grows needs no change here. An extension the engine does not claim is **refused by name** rather than handed to the package importer, because the SDK answers `None` precisely so a file is never opened as one thing and saved back as another. Download gained "Same format as opened", reaching four engine functions that existed and were called by nothing, and the delimited export now takes its MIME type from the engine — it hard-coded `text/csv` for all three, which is right for one. | Four browser tests; seen red by guessing an unclaimed extension as a package — "a .xls was opened by guessing it was really a package" |
| ODS-01 | `casual-calc-io` is not the format registry the design says it is | Done | P2 | `19` places `casual-calc-ods` "under the `-io` registry" and `36` repeats it. `casual-calc-io` handles delimited text only, and the SDK now depends on `casual-calc-ods` **directly**, exactly as it does on `casual-calc-export`. So either the document or the crate DAG is wrong, and this is precisely the case where editing the prose is the failure mode — the registry was a structural decision about where format knowledge lives, and quietly restating it as whatever the code grew into would erase the decision without anybody making it. **Done, by ADR-022, to the owner's decision: detection below, dispatch above.** `-io` was worse than the row said — no identity, no detection, *and* no dispatch, just the delimited adapter and two helpers. `-io` gains `detect`, which identifies XLSX, ODS and delimited text from the bytes and **needs no format crate to do it**: a zip's first local file header names its first entry, and ODF requires `mimetype` to be first and *stored uncompressed* precisely so a reader can identify a document without decompressing it. Dispatch stays in `casual-calc-sdk`, which is the entry point hosts actually call. Giving `-io` the whole OOXML stack so the graph matched the sentence was rejected: a CSV-only consumer would carry all of it. **This closed a real gap, not just a documentation one.** Format came from the *filename extension* and never from the bytes, so a mislabelled `.xlsx` that was really an `.ods` opened wrong and an upload with no filename could not be opened at all — while `foreign_format` in `casual-calc-render` had been refusing to believe a file's claim about itself, for the pictures *inside* those same workbooks, all along. `SessionFormat::for_bytes` is the way in. Detection returns `None` rather than guessing: "probably a CSV" over a binary file is a sheet of mojibake. **Seen red without the fix**: reducing detection to "starts with PK, call it XLSX" fails 4 of 26 — the ODS, the ODF *text* document, the unrelated zip, and the truncated package. **Fixtures from two directions**, which is the pairing that matters: Python's `zipfile` writes the packages `-io` parses, so the header offsets are a real writer's; and the SDK tests detect packages **our own writers** produced. Either alone is half a test. **One correction to the row itself**: `36` does *not* repeat the claim — it says "the `casual-calc-io` adapters", which is accurate — so it was left alone. Correct prose is not a defect. **A defect this turned up in my own tooling**: `check-all.py` globs `check-*.py`, so the `check-rust.py` added last round was swept into the fast runner — a four-minute build inside the two-second check, which is the exact thing its docstring forbids. Excluded by name, with the reason. | The crate graph matches `19`'s DAG, or `19` is amended by an ADR that says why it changed |
| ODS-02 | Nothing fuzzes ODS, and the fidelity harness has never seen one | Done | P2 | Built, and it paid for itself before it was merged. An `ods` fuzz target runs in `fuzz-build`, and `tools/casual-calc-fidelity` gained an `--ods` mode with a **LibreOffice-authored** corpus wired into `oracle-diff`. The target makes two passes: the raw bytes as a package, and the same bytes wrapped as `content.xml` inside a zip the harness builds — because arbitrary bytes never form a ZIP, and the element walk is where every parser-shaped defect lived. Refusal passes; only a panic, a hang or an OOM is a defect. **It found three more defects immediately** — `ODS-04`, `ODS-05` and `ODS-06`, two of them P0, one of which this project had shipped two hours earlier. The corpus records divergences with a reason and they are **self-expiring**: when the two sides agree the run fails as a *stale marker*, so an excuse cannot outlive the defect it excuses. That is how both P0 fixes were confirmed — nine markers went stale in one run, and LibreOffice agreement went from 70 of 80 formulas to 79 of 80. **What made all of it invisible before:** every ODS test fed the reader a document this repository had written. | `cargo fuzz run ods` clean over 682k executions; `--ods` agrees with LibreOffice or names the divergence with a reason that expires |
| ODS-03 | Document metadata is dropped, and it is the one loss that is silent | Done | P2 | `import_ods` opened `content.xml` and nothing else, so author, title and timestamps vanished on a round trip **with an empty report** — silent for a structural reason: the report is assembled while reading `content.xml`, and nothing can count what was never opened. `meta.xml` is now read into a new format-neutral `Workbook::properties` and written back, so the metadata **survives** rather than merely being counted; whatever `<office:meta>` held that the model does not is named per element, and `styles.xml`/`settings.xml` are named from the zip directory without decompressing them. Additive by ADR-010 — defaulted in, skipped out when empty — so `SCHEMA_VERSION` does not move and an untouched workbook serializes to the bytes it always did. **`retained_parts` was rejected deliberately**, though it is where the `.xlsx` importer parks `docProps/core.xml`: the OOXML writer emits every retained part unconditionally, so an ODS to XLSX save would have leaked a stray `/meta.xml` into the package — a new defect in place of an old one. Every part is now bounded **twice**, on the declared size and on the read, because the declared size is the archive's own claim; `content.xml` gained the stronger bound too. **The mutation worth keeping:** transposing `dc:creator` and `meta:initial-creator` — which mean opposite things in ODF and OOXML — passes a round-trip test perfectly, because reader and writer are symmetric. Only a test that names the two roles apart catches it, and the real fixture records neither name. | A LibreOffice document's metadata is read, survives a round trip, and what is not modelled is named per element; seen red reading `content.xml` alone, and under a creator/last-saver swap |
| ODS-04 | LibreOffice's padding was silently trimmed away | Done | P1 | LibreOffice does not write leading, trailing or repeated spaces as characters — ODF collapses whitespace inside `<text:p>`, so it writes them as `<text:s text:c="N"/>` **elements**, which this reader walked past because it only collected text events. `"  padded  "` arrived as `"padded"`, and a cell holding a single space arrived empty: a silent change to somebody's data, on the most ordinary content there is. `<text:tab/>` had the same hole. Found by the LibreOffice corpus of `ODS-02`, and gated against a **LibreOffice-written fixture**, because nothing this repository writes emits `<text:s>` — a round trip through our own writer could never have exercised it. Same circularity that hid `ODS-06`. The repeat count is bounded like any other attacker-controlled repeat. | A LibreOffice document's padding survives import; seen red with the `<text:s>` arm disabled — `"padded "` against `"  padded  "` |
| ODS-05 | A 329-byte file materialised 16.7 million cells | Done | P0 | `MAX_REPEAT` bounded each repeat attribute and **nothing bounded their product**. One `<table:table-cell>` carrying `number-columns-repeated` and `number-rows-repeated` together materialised 4096 x 4096 = **16,777,216 cells**; measured here at **1.50 GB of resident memory and 16.4 seconds from 329 bytes**, and ten such rows fit in two kilobytes. The ODF reader is reachable from a WOPI host's upload, so this was a denial of service behind a friendly file extension. Bounded now on the **product**, at the `max_populated_cells` the OOXML reader has enforced since `SEC-011` — deliberately the same number rather than a fresh judgement, because two readers admitting different amounts of the same workbook is its own defect. The document is **refused, not truncated**: keeping the first eight million cells of a file that claims sixteen would be the silent loss this project refuses. The cursor is untouched, so a document may still legally *span* a huge range. **What made it invisible:** every ODS test fed the reader a document this repository had written, and no such document contains an amplifier. It was found by the fuzz target of `ODS-04` — the first thing ever to hand this reader an input nobody wrote. | A tiny package declaring the maximum repeat product is refused with `TooManyCells`, and an ordinary wide fill still reaches its far corner |
| ODS-06 | LibreOffice could not read a single formula this engine wrote | Done | P0 | `WOPI-07` taught `export_ods` to write formulas, as `table:formula="of:=…"`, and **never declared the `of:` prefix**. A prefix used and not bound is not untidy XML, it is not well-formed namespace XML: **LibreOffice answers `Err:510` for every formula cell in a document OpenCalc saved**. Confirmed against a real `soffice` — `=SUM(A1:A3)` came back as `Err:510` before the declaration and `10` after. Arguably worse than the defect `WOPI-07` fixed: formulas used to be dropped, and now they arrived as errors, so the file looked like it had formulas and every one was broken. **Why nothing caught it, and this is the lesson:** `import_ods` matches attributes by *local* name, so it read the broken file back perfectly and every round-trip test passed. **A round trip through your own reader cannot see a namespace defect, by construction** — only a real LibreOffice round trip could, which is exactly what `ODS-04` built. The gate is mechanical rather than a re-test of this one prefix: any prefix used, including one inside an attribute *value*, must be declared. The first version of that gate passed with the declaration deleted — it only scanned element and attribute *names*, and `of:` lives in a value — so it was fixed before it was trusted. | Every prefix the writer uses is declared, value-prefixes included; seen red with `xmlns:of` removed, and against real LibreOffice |
| WOPI-06 | No proof-key validation | Done | P2 | WOPI's request-signing scheme lets a host prove a request came from the editor it registered. Optional, and no host currently requires it, but it is the next hardening step for SharePoint Online. **Built.** The access token is a bearer credential — anyone holding one can make the same calls — so every outgoing request now carries an RSA-SHA256 signature over the token, the URL as sent, and a .NET-tick timestamp, with the public half published as `<proof-key>` in discovery. **The row's acceptance was written the wrong way round**: this service is the WOPI *client*, so it signs and never verifies, and there is no incoming request here to refuse. What is testable, and what the gate does, is close the loop — verify our own signature exactly as a host would, using the modulus and exponent read out of the discovery document rather than the key pair in hand. No new dependency: `ring` was already in the tree via rustls, which also avoids the `rsa` crate's advisory history. Off unless `OPENCALC_WOPI_PROOF_KEY` names a PKCS#8 key, and a configured key that will not load is **fatal at boot** rather than a warning — starting anyway would advertise nothing and sign nothing while looking healthy. Eleven mutations, all seen red: URL not upper-cased, length prefixes dropped, DER leading zero kept, modulus and exponent swapped, Unix epoch, seconds instead of 100ns, the signed URL missing the access token, the token not percent-encoded, discovery publishing the wrong value, a proof key advertised with none configured, and the headers dropped. **The encoding one is the lesson**: it passed until the test token was changed from `host-token` to something base64-shaped, because a token of plain letters makes percent-encoding a no-op and hides whether the URL signed is the URL sent. | A host verifies the proof on a request this service actually made |
| BRAND-01 | No white-labelling | Done | P1 | Product name, logo and colours were hard-coded. One name now flows from `OPENCALC_BRAND_NAME` into the discovery document, the session chrome and the editor's own toolbar and About dialog, carried to the editor bundle on its iframe URL. Escaped at all three edges: XML attribute, query string, and DOM text — a brand arrives on a URL, so anybody who can send a link chooses it. | `a_brand_name_is_escaped_into_the_markup`, `the_brand_is_appended_to_the_editor_url_safely`, `editor.branding.spec.mjs` |
| DEP-03 | Redis op log never trimmed | Done | P1 | `RPUSH` with no `LTRIM`, `EXPIRE` or `DEL`, read back with `LRANGE 0 -1` every lease tick per document per node — while `since` carried a comment saying *"the log is compacted, which is what keeps this bounded"*. Nothing compacted it. Trim and expiry now happen **inside the append script**, the only moment the log is known consistent and already locked. The in-memory coordinator is bounded to the same window, which `contract!` caught. | `the_log_is_bounded…` (both backends) and `a_log_nobody_returns…`; mutation gives `kept 10250 against a window of 10000` and `no expiry (-1)` |
| DEP-04 | A lost coordinator is invisible to clients and to k8s | Done | P1 | `order()` failed its append and returned without acking, so clients resent forever, while `/healthz` — unconditional, and the only probe — kept saying `ok`. Now the append's refusal is sent back as `Refused{NotSaving}` and `/readyz` is a separate probe that 503s when `peers()` fails, leaving liveness alone so the node is drained rather than restarted. | `a_refused_append_is_reported_to_the_client_rather_than_swallowed`, `a_node_cut_off_from_the_coordinator_reports_itself_unready` |
| DEP-05 | Drain cannot finish inside a stop grace period | Done | P1 | Sequential saves, 10s each, no global deadline: thirty documents needed five minutes against Docker's 10s default, and the remainder was SIGKILLed mid-drain — the loss the drain exists to prevent. Now bounded-concurrent under a **global** deadline, keeping the per-document timeout so one hanging callback cannot eat the budget. Compose sets `stop_grace_period: 40s` against a 25s deadline. | `the_drain_stops_inside_its_deadline…`; mutation to the sequential shape gives *"the process never stopped"* at 5.04s |
| DEP-06 | No metrics | Done | P1 | `/stats` returned two integers, so "are saves failing?" could only be answered by tailing logs — which no alert can do. `/metrics` now serves ten counters and two gauges in Prometheus text, hand-written rather than adding a dependency to a server that reads untrusted files. | `metrics_count_a_save_that_really_happened` asserts the **delta**; mutation gives `0 -> 0` |
| DEP-07 | CI never built an image | Done | P1 | Both images now build in CI and the standalone stack is brought up and probed, so a Dockerfile that stops building fails here rather than at an integrator. **Published.** **Release shape decided (owner's call), and executed as `server-v0.0.0`.** **Two images** (owner's decision, after working the architecture through): `casualoffice/calc` and `casualoffice/wopi`. The split follows the one the code already makes — ADR-012/014 keep the collaboration server unable to mint tokens and holding no per-document state, which is what lets one node own a document and the cluster scale by adding nodes; WOPI needs both a minting key and a per-file lock, so folding it in would trade that property away. Collabora and ONLYOFFICE ship a single image because their daemon *is* both; this project chose otherwise, deliberately and on the record. `casual-calc-host` says in its own first paragraph that it is the demo integrator, so it ships as a role inside `calc` rather than as a third public name. One name because that is what was asked for; three containers because the admission budget reads the *container's* cgroup ceiling and this *process's* RSS, so co-tenanting the three would have the collab server measure only itself against a limit all three spend from, believe it had headroom it did not, and admit work until the OOM killer took every document on the node including unsaved work — the precise failure the 85% mark exists to prevent. Published on a **`server-v*` tag**, which `release-sdk.yml` already reserved for exactly this reason: once two release workflows watch one tag namespace, tagging either fires both. So image and SDK versions move independently, as required, and **nothing can publish until such a tag is pushed** — the hold is enforced by the trigger rather than by anybody remembering it. Credentials are in place as `DOCKER_USERNAME` / `DOCKER_SECRET`. Two things were owed at release and not before: a registry **overview**, which did not publish and is now `DEP-18`, and the **marketing site**, which is updated. Sequenced after the remaining P0/P1 work and immediately before `TAURI-001`. **The publishing half is now built, and deliberately dormant.** `release-images.yml` publishes `casualoffice/calc` (the collaboration server) and `casualoffice/wopi` (the adapter) on a **`server-v*`** tag — the namespace `release-sdk` had already reserved for it, precisely so image and SDK releases cannot fire each other. Multi-arch (amd64 and arm64), with an SBOM and a build-provenance attestation naming the repository, workflow and commit, on the same argument `CI-005` makes for npm. A prerelease never moves `latest`, and a tag that is not a semantic version is refused before anything is built. `fail-fast` is off across the matrix because a half-published release is worse than a failed one: the half that landed is already public. **The hold is enforced by the trigger, not by memory** — there is no branch trigger, and the manual path defaults to a dry run — and `tools/check-release-hold.py` asserts exactly that for every `release-*.yml`, so a future workflow cannot quietly acquire one. Registry pages live in `docs/registry/` and are published with the image, so an image cannot land undescribed and the text is reviewed like anything else. **Still owed at release, and not before:** the marketing site pointing at the published images. **A consequence worth naming:** `casual-calc-host` is not published. It calls itself the demo integrator, which is why — but it is also what serves the editor and stores documents in the standalone quick start, so that path still needs a `docker compose build`. Say the word and it becomes a third image. **Owner's decision: `casual-calc-host` is published after all**, as `casualoffice/calc-host`. It is the demo integrator and it is also what serves the editor and keeps the documents, so leaving it out meant the standalone quick start needed a `docker compose build` rather than a `docker run` — a worse first five minutes than the smaller public surface was worth. Three images now, each with a registry page the release gate insists on.  **Released as `server-v0.0.0`.** Main was verified green at `d2b9755` first — publishing three public images from a commit whose CI had not been seen pass is not a thing to do quickly. The tag fired `release-images.yml`, which published `casualoffice/calc`, `casualoffice/wopi` and `casualoffice/calc-host`. The marketing site followed publication rather than preceding it: `deploy.html` claimed *"No published image ... not yet pushed to a registry"*, and changing that before the images existed would have made the page assert something untrue — the same failure this repository files rows about, pointed outward at users. | `docker-build` job; images published on `server-v0.0.0` |
| DEP-13 | Redis itself is still a single unreplicated box | Done | P1 | Three things, unequal, and the row's framing was wrong: **"Redis is a single box" was hiding three defects in *this* repository**, not in Redis. **(1) The link never re-dialled.** It was a `MultiplexedConnection`, which does not: when the socket died the driving task ended and every later command — on it and on every clone, which is what the coordinator hands out — failed for the life of the process. **A Redis that merely restarted cost a restart of every node**, which is most of what "a single failure stops ordering cluster-wide" meant in practice. Now a `ConnectionManager` with a bounded budget, and subscriptions re-subscribe: the same defect had a quieter symptom there, because a dead message stream simply *ends*, indistinguishable from "nothing was published", so a document silently stopped renewing its lease while the node went on serving it. **(2) The epoch fence was `<` where it must be `!=`.** An appender *ahead* of the store looks impossible and is exactly what a failover produces — the promoted replica is missing the claim that raised the epoch. Under `<` that leader was believed, **and so was whoever the rewound store thinks holds the lease**: two live leaders, each committing locally before appending, and the one that loses the revision CAS is permanently diverged with no resync built. Verified by reverting it: the append returns `Ok(1)` where it must return `Fenced`. The change refuses strictly more and accepts nothing new, so it cannot introduce divergence. **(3) TLS.** The `redis` crate had no TLS feature compiled, so **every `rediss://` URL was refused at parse time** and no deployment could encrypt the coordinator link at all. Now `rediss://` with `OPENCALC_REDIS_CA` for a private CA and optional mutual TLS; certificates against a plaintext URL, and `#insecure`, are refused at startup. **Not done: replication and failover** — Sentinel over Cluster mode is `ADR-020` in `77`, and it stops there on purpose: Redis replication is asynchronous, so a failover can lose an append that was already acknowledged, which contradicts ADR-014 §4's durability promise. Shipping Sentinel without deciding that would make this row *look* finished while leaving a silent-loss path open. **What made it invisible:** the client-side half looked like infrastructure rather than code, and `docs/65` asserted nodes "recover by re-claiming" — describing the design, not what was built. **Replication and failover are now built (`ADR-020`, Accepted).** Sentinel over Cluster mode, with `min-replicas-to-write` as the decision rather than a detail: a collapsed in-sync set makes the coordinator **refuse** a write instead of accepting one it will lose, which turns the silent loss into a shape `ADR-012` already defines and clients already handle. Verified by starting a coordinator with the floor at zero and watching the append be accepted — *"this is the write a failover loses, acknowledged to a client that will stop resending it"*. The node **checks** the floor at startup *and after every re-resolution*, because the setting is per server and the standard mistake is configuring the original primary and nothing else. **What the design note did not foresee, and the reason this needed building rather than reasoning about:** `SENTINEL FAILOVER` demotes the old primary *in place*. The socket stays healthy, reads keep working, every write returns `READONLY`, and the driver classifies that as not worth retrying — so `DEP-13`'s re-dial reaches nothing and the node refuses every edit for the rest of its life. Re-dialling cannot fix it; **re-resolving** can, which is why the trigger is the error rather than the socket. A private CA is **refused** against a `+sentinel` URL rather than accepted and ignored, because this driver builds the resolved primary through a path that cannot carry one. | Two real failovers — a demotion and a kill — with ordering resuming and every acknowledged revision still in the log; a write with the in-sync set collapsed refused and the log not grown |
| CI-013 | A presence test's margin was below the platform's clock | Done | P2 | `platform (windows-latest)` failed on `answering_keeps_a_participant_in_the_roster_rather_than_expiring_their_cursor` — 270 passed, 1 failed — while every other platform was green. **Not a defect and not a retry.** The test asserted a participant who *is* answering is **not** expired, using a 60 ms presence TTL against a 20 ms ping: three pings of headroom. Windows' default timer granularity is about 16 ms, so two late scheduler slots on a loaded runner expire somebody who answered, and the build fails for a property that held. **A margin below the platform's own resolution is not a tight test, it is a coin flip** — and a suite that fails at random is one people learn to re-run, which is how a real failure gets waved through. Five pings of TTL (400 ms) and a wait of three whole TTLs, so the scheduler may be late and the assertion still means what it says. **Widening did not blunt it**: removing the `roster.heartbeat` that keeps a connected-but-quiet participant alive still fails it, `left: 0, right: 1`. **Its siblings were checked and left alone.** Two other helpers use 10 ms ticks, and both assert an expiry *happens* — a polarity a slow runner makes more likely, not less. Only this one asserted the negative, which is the direction that flakes. | A responsive participant stays in the roster on a runner whose clock ticks every 16 ms |
| CI-012 | The failover behaviour ADR-020 exists for is unexercised in CI | Done | P1 | Both halves of the coordinator work skip on the runner: the TLS tests need `redis-server`, and the whole `ADR-020` failover harness needs `redis-server` **and** `redis-sentinel`. They run locally — two servers, three sentinels, quorum two, a demotion failover and a kill failover — and in CI they print a skip and pass. So the property this project just decided to depend on is asserted nowhere that gates a merge, which is the same shape as a metric that is scraped and always zero. **Fixed, and the fix is two things.** The runner gets `redis-server` — which brings `redis-sentinel` with it — because the service container was never enough: it gives a Redis on 6379, and these tests start their *own*, since a failover needs two primaries and three sentinels it can promote and kill. **And the skips are now the assertion.** Installing the binaries fixes it today; nothing stops a runner image changing, or a test growing a new precondition, and going quiet again — and a green job would say nothing either time. So the job greps its own output and fails if a coordinator test announces it was skipped, naming which. That is the general shape of the defect this row is about, and it is worth stating: **a test that skips and passes is indistinguishable from a test that ran**, exactly as a counter that is scraped and always zero is indistinguishable from nothing happening. Both were live in this repository this week. | `redis-server --version` and `redis-sentinel --version` on the runner, and the job fails if any coordinator test prints its skip line |
| SRV-07 | A log-bound test is wall-clock flaky | Done | P2 | `the_log_is_bounded_rather_than_growing_forever::in_redis` takes a lease with a one-second TTL and then issues 10 250 appends; under full-suite load that outruns the expiry and it fails `Unled`. **Reproduced on unmodified main** before being blamed on the change that found it, which is the right order. It passes in isolation. A test whose result depends on how busy the machine is teaches people to re-run rather than to read, and this project has already paid for that habit once. **Measured rather than guessed at.** The appends take **0.64 s** against a key expiry of **4 s** — a six-fold margin, which is exactly the size a loaded runner eats, and why it passed alone and failed in the suite. The bound being asserted has nothing to do with time, so the lease is now long enough that no amount of load can reach it: 0.64 s against 2 400 s, a **3 750-fold** margin. Still an expiry rather than none, so a test that leaks a lease does not leave one behind for ever. Every other test in the file keeps the shared one-second TTL, because a handful of appends inside a second is fine — this one does ten thousand two hundred and fifty. The mechanism was confirmed by shrinking the lease until it reproduced the exact `Unled` the original failure showed, rather than by assuming the diagnosis. **Why it matters more now:** `CI-012` just made the runner execute these tests for real, so a wall-clock flake here would red main intermittently — and an intermittent red teaches people to press re-run instead of to read, which is the habit this project has already paid for. | 10 250 appends complete against a lease that cannot expire; shrinking it reproduces `Unled`, so the diagnosis is demonstrated and not asserted |
| DEP-15 | Four RUSTSEC advisories, one a live vulnerability | Done | P0 | `dependency-policy` went red on every branch at once, which is the signature of a newly published advisory rather than of anything a PR did. Four: `RUSTSEC-2026-0258` (**h2 unbounded empty DATA frames** -- a real denial-of-service, reachable on the HTTP/2 stack all three servers expose), plus three `unmaintained` notices (`rustls-pemfile`, `rustybuzz`, `ttf-parser`). **Fixed by upgrade, not by suppression, wherever an upgrade existed.** h2 -> 0.4.16 clears the vulnerability. `rustls-pemfile` is *gone from the tree*: `axum-server` 0.8 dropped it, and our own direct use moved to `rustls-pki-types`. Only the two with no published successor are ignored, individually and with reasons, tracked as `DEP-14`. | `cargo deny check advisories` passes with no vulnerability and no blanket relaxation |
| DEP-16 | The fuzz tree resolves its own lockfile, and nothing audited it | Done | P1 | `cargo audit` reads one lockfile. `fuzz/` resolves its own, and CI **compiles** it -- so a second dependency tree was outside the supply-chain gate entirely. It was still pinning `h2` 0.4.15, the version `RUSTSEC-2026-0258` is against, and linking it, while `dependency-policy` reported no vulnerabilities. Found because `DEP-15`'s workspace-lock update made `cargo fuzz build` re-resolve, which tripped the existing `git diff --exit-code Cargo.lock` assertion and turned `fuzz-build` red on main. That assertion is the only reason this surfaced at all: it checks that the lock is *stable*, not that it is *safe*, and it caught this by accident. **Fixed**: the fuzz lock is resolved to match (h2 0.4.16, axum-server 0.8.0, rustls-pemfile gone), and `cargo audit --file fuzz/Cargo.lock` now runs beside the workspace one, because a separately-resolved tree will drift again. Verified with the job's own pinned toolchain rather than an approximation of it -- `cargo +nightly-2026-07-20 fuzz build` then the diff -- and the new audit step is RED against the lockfile main carried an hour earlier. | Both lockfiles are audited, and the fuzz build leaves its lock unchanged |
| DEP-14 | The text-shaping stack is unmaintained | Done | P2 | `rustybuzz` and `ttf-parser` are both unmaintained with no published successor (`RUSTSEC-2026-0206`, `RUSTSEC-2026-0192`); ttf-parser is reached only through rustybuzz, so it leaves when the shaper does. Neither is a vulnerability -- they are "the author has stopped", not "this is exploitable" -- which is why they are ignored in `deny.toml` by id, with reasons, rather than by relaxing the category. **The destination is already half-built:** `casual-calc-render` ships `skrifa` (the crate ttf-parser's own advisory recommends) for outlines and uses rustybuzz only for glyph ids, so moving the shaper to the same `fontations` family makes the two agree by construction instead of by a hand-kept mapping. Amends ADR-018. **Done.** The shaper is `harfrust` — the same HarfBuzz algorithm, maintained under the harfbuzz organisation, built on `read-fonts`. Neither `rustybuzz` nor `ttf-parser` is in the lock file, and **both `deny.toml` ignores are deleted**, along with their `.cargo/audit.toml` entries. `cargo audit --deny warnings` exits clean; `cargo deny check bans licenses sources` passes. **`skrifa` moved 0.43 → 0.46, and that was the point rather than housekeeping.** The old argument for handing shaped glyph ids straight to `skrifa` was that a glyph id indexes the font's own tables, so the same bytes give the same ids to both — true, and true of *two different parsers that happened to agree*: rustybuzz read with `ttf-parser`, skrifa with `read-fonts`. On skrifa 0.43 the tree still held **two** `read-fonts` versions (0.40 beside harfrust's 0.43), which would have left exactly that two-parser argument in place under a new name. At 0.46 both resolve to one. The agreement is structural now. **The bundle shrank**: 7,553,505 → 7,547,191 bytes, and `cargo tree -p casual-calc-wasm` shows no shaper at all, so ADR-018's "native only" decision still holds rather than being assumed. **Behaviour held by the test that was already there**: Hebrew still comes back in visual order — the reverse of memory order — asserted against a naive `cmap` lookup rather than fixed glyph ids, so it tests the *relationship* and survived the shaper being swapped underneath it. That is what made this a dependency change rather than a rewrite. | Neither crate is in the lock file, and the two `deny.toml` ignores are deleted |
| DEP-08 | No Kubernetes manifests or Helm chart | Blocked | P1 | The design targets k8s ([59](59-COLLABORATION-SERVICE-STACK.md)) and ships nothing for it. DEP-04/05/06 are done, so it is unblocked by code and blocked by tooling: no cluster, no `helm`, no `kind` on any machine this has run on, and a chart that has never been rendered is unverified code. | A chart deploys and a pod restart loses nothing — needs a real cluster, not a dry run |
| CI-008 | The wasm gate's exclusions were a hand-kept copy of the host list | Done | P1 | `cargo check --target wasm32` excluded the two host crates by name. Adding a third broke the job on `main`, because the list was a copy of a fact the manifest already states. It is now derived from `server/` — the same boundary `dependency-policy` reads. | `cargo check --target wasm32-unknown-unknown --workspace $(derived)` exits 0 where the hand-listed form exits 101 |
| CI-009 | The ADR-012 boundary gate only checked one of the three hosts | Done | P1 | It grepped `crates/` for `casual-calc-collab-server` alone, so a crate depending on `casual-calc-host` or `casual-calc-wopi` was told the boundary was intact — while the workspace manifest states the rule for all three. Found by looking for the *class* of CI-008 rather than fixing only the job that went red. The host list is now derived, so a fourth host is covered on the day it is added. | A crate mentioning any host fails the gate; verified against all three |
| CI-007 | The fuzz workspace's audit could not read the project's advisory decisions | Done | P1 | `cargo-audit` reads `.cargo/audit.toml` relative to its working directory, and the step ran `cd fuzz`, where no such file exists — so the fuzz tree was audited against no decisions at all and failed on `RUSTSEC-2025-0134`, an entry already sitting in the root config. It now runs from the root with `--file fuzz/Cargo.lock`, so there is one list read from one place; a copy in `fuzz/.cargo/` would fix today and drift tomorrow. | `cargo audit --deny warnings --file fuzz/Cargo.lock` exits 0 where `cd fuzz && cargo audit` exits 1 |
| CI-005 | Supply-chain and provenance gates | Done | P1 | Workflow permissions turned out to be done already — all four are least-privilege — so the remainder was two inputs and one attestation. **Container bases were floating tags**: `debian:bookworm-slim` is retagged whenever Debian rebuilds it, so the same Dockerfile produced a different image with nothing in the repository changing — the hole the action SHA-pinning already closed, on the larger input. Pinned by digest, with a gate. **npm publish had no provenance**, so a consumer's only evidence a package came from this repository was the package saying so; `--provenance` plus `id-token: write` now mints an attestation naming repository, workflow and commit. And the `sdk-types` job I added last week used `npm install` rather than `npm ci`, so its result depended on the day it ran. | The digest gate fails on an unpinned base, verified both ways |
| DOC-025 | Live docs and ADRs reconciled | Done | P1 | This consolidation is its deliverable. **Its gate now exists and passes**: `tools/check-doc-claims.py` (`DOC-032`) enforces "no doc claims a gate CI does not run" mechanically, over 55 documents. Remaining, and both smaller than they looked: the pending-ADR list, now tracked as `ADR-019` for the overdue capability-trait decision; and the "142 stale claims", which **has no list, no tool and no artifact** — the string occurs once in the whole repository and `git log -S` shows it was introduced by the consolidation commit itself, not by any audit. A backlog nobody can enumerate cannot be worked down, only carried, so the honest next step is to re-run an audit that produces a list rather than to chase a number. **The "142" is now replaced by a number that is checked.** A mechanical audit of every code path the documents name — the one class of claim that *is* enumerable — found **53 paths and 3 wrong**, not 142. Two were name drift, where the thing exists under another name and the document was corrected — the view-only sample is `sdk/examples/viewer`, and the marketing documentation page is `webapp/docs.html`; `55` named both under names that were never on disk. One was a promise nothing keeps — a host-toolbar sample listed as shipping and never built — which is a row (`SDK-010`) and not a doc edit, because deleting the line is how a promise to integrators disappears without anybody deciding to drop it. `tools/check-doc-paths.py` runs in CI so it cannot drift again. **What the first version got wrong is worth keeping:** it treated every backticked path as a claim, and flagged `fuzz/.cargo/` — which `CI-007` names as the fix it *rejected* — and every `docs/65`-style citation. A gate that cries wolf is a gate somebody switches off, so citations are excluded and deliberate absences are listed with their reason, self-expiring: if the path ever appears, the exemption fails. **Closed, and the last item was one nothing was looking for.** The two remaining threads are both settled: the pending-ADR list emptied when `ADR-019` was decided (the capability trait, rejected — `RND-11` was built the other way), and the unenumerable "142 stale claims" was replaced by a measured 53 paths of which 3 were wrong, all fixed, with `check-doc-paths.py` in CI so it cannot drift. **What was left was a status.** `ADR-021` still read `Proposed` — while `RND-11` had been built to it, shipped and closed, its `Polyline`/`Polygon`/`Wedge` variants sitting in the display list with an arm each in the renderer. `RND-11`'s own row said "Proposed" in the same sentence that recorded the work as done. A decision record's status is load-bearing: it is the first thing a reader consults before following the argument, so a register that says `Proposed` about a decision the codebase is built on says the opposite of the truth about exactly the decisions that mattered enough to build. **Review cannot catch it** — the register and the row are different files and neither diff is wrong on its own — so `tools/check-adr-status.py` does: a `Done` row naming an ADR must find it settled. One direction only, deliberately; an `Accepted` ADR with no finished row is ordinary, since deciding without building is allowed. `Rejected` counts as settled, because a row can be closed *by* rejecting an approach, which is exactly what `ADR-019` was. **Seen red**: putting `ADR-021` back to `Proposed` fails with the row named. Passing: 22 decisions, 95 finished rows, none left proposed. | No doc claims a gate CI does not run, and no doc names code that is not there |
| DOC-027 | No self-hosting page on the public site | Done | P2 | `docs/65` is a thorough operator reference and lives in the repository, where somebody deciding whether they *can* self-host never sees it. The site had an SDK guide and nothing for the deployment half, so the answer to "what does running this actually involve" was a clone away. **Added `webapp/deploy.html`**, reusing the SDK page's shell so the two cannot drift apart visually: the two stacks, the host contract's four endpoints, the two addresses that get confused, every environment variable an operator changes across all three servers, tokens and JWKS, reverse proxying, TLS, health and metrics, storage and backup, WOPI, and a symptom-to-cause table. **Written from the code, not from memory** — every claim was checked against the source before it was written down, and the numbers came out of the binaries that read them. Gated by `tools/check-deploy-docs.py` in CI: every `OPENCALC_*` and every `opencalc_*` metric the page names must exist in a server. One direction only, because the page is deliberately what an operator changes rather than an exhaustive dump. Seen red four ways — a variable no server reads, a metric nothing emits, a rename *in the code* (the drift that actually happens), and the page gutted, which every other check would have passed. Says plainly what is missing: no Helm chart, single Redis, no version history, no published image. | The page names only settings that exist, checked in CI |
| DOC-028 | The homepage shipped two identical nav entries | Done | P2 | `DOC-027` linked the new self-hosting page into every page's navigation with a script that guarded against a repeated **link** and not a repeated **label** — and `index.html` already carried a `Self-host` anchor to its own section, so the homepage went out with two identical-looking entries pointing at different places. Reported by the user, not by anything here: it is not a broken link, not invalid HTML, and invisible to any test that renders the page successfully. It only looks careless. **Fixed** by giving the guide one name wherever it is linked — `Deploy docs`, parallel to `SDK docs` — leaving `Self-host` to mean the homepage section alone, and adding the guide to the homepage nav beside the SDK one so the two documentation pages are reachable the same way. Two stale links found alongside: the `#selfhost` section and the SDK page both still sent readers to `docs/65` on GitHub as "the deployment guide", which is the page `DOC-027` built. Gated by `tools/check-site-nav.py` in CI — no two entries in one nav share a label, every relative target exists, every `#anchor` is on the page. Seen red four ways, the first reproducing the exact mistake and its exact message. | No nav has two entries with the same label |
| DOC-030 | No published performance or sizing figures | Done | P2 | Asked for directly, and the honest first answer was that only *limits* were written down — `max_documents`, `max_participants` — which are configuration, not capacity. `PERF-10` landing gave the missing half: **83.64 bytes a cell**, measured as the peak resident delta on the CI runner. The self-hosting page now carries two matrices: what each engine operation costs against its budget, and what a node of a given size holds at three editors a sheet. Every figure re-derived from the benchmark report before publishing. **The useful finding is that the engine is not what runs out**: 3 000 editors at two edits a second is 6 000 edits a second, which at 0.25 µs each is about a sixth of one percent of a core — so sizing is memory and sockets, not calculation. Marked throughout which machine each number came from, because memory is the Linux runner and the timings are an Apple laptop, and conflating them would make the page confidently wrong. | The site answers "how big a box do I need" with measured numbers |
| PERF-12 | No server-side load harness | Done | P1 | **Measured.** `tools/casual-calc-loadtest` drives N documents by three clients over real WebSockets and reads the node's own `opencalc_resident_bytes`. Against a Linux container: idle 6.6 MB; 10 docs/30 editors 11.6 MB; 25/75 12.4 MB; 50/150 15.9 MB — **10 200 of 10 200 edits acknowledged**, p50 16-27 us, p99 1.1-2.8 ms with no degradation from 50 to 150 concurrent editors. Marginal cost between the 25- and 50-document runs is **143 KB per open document**; 190 KB is the figure published. **The harness was the defect.** Run against no server at all it printed a report of zeroes and exited 0, so wiring it into CI would have produced a job that goes green whether the server works, is broken, or was never started — the same shape as a submission silently dropped, which this repo has paid for before. It now exits non-zero when nothing was acknowledged, and when acknowledged falls short of sent, because the percentiles are computed only over edits that came back and so are blind to dropped work. That guard caught a real failure within a minute of existing: the first scaled run reported zeroes because every join was refused by the URL allowlist. **What made it invisible:** a load test's output is a wall of plausible numbers, and zero is a plausible number. **Correction it forced:** `webapp/deploy.html` sized from cells alone, which under-sizes a many-small-document deployment by 3.3x - at 1 000 cells the collaboration overhead is 70% of the cost. The rule of thumb now adds the measured 190 KB instead of hiding it in a x1.4 fudge. | `--documents N --clients 3` reports RSS and p99, and **fails** when nothing was acknowledged or the server dropped work |
| CI-011 | A registry blip reds the whole run, and nothing retries | Done | P2 | A `lint` job failed with `download of config.json failed -> curl failed -> [35] SSL connect error` **0.3 seconds** after starting, having compiled nothing, and passed unchanged on re-run. There was no `.cargo/config.toml` and no `CARGO_NET_RETRY` anywhere, so all 13 cargo jobs red the run on any registry hiccup. The gap between the fetch starting and the error is far too short for any backoff, so cargo never classified it as worth retrying - which means **raising `net.retry` would have been an inert fix**, configuration that looks like a remedy and changes nothing, and stops anybody looking again. The retry is therefore in the shell, in `.github/actions/cargo-warmup`, where it does not depend on how cargo classifies a curl code. **Why it is worth a row:** a red main that means nothing is what teaches everyone to ignore a red main that means something. **What makes it rot:** not breaking - a fourteenth job added later that quietly lacks the step, and a flake nobody can explain because the other twelve are fine. Hence a gate rather than a convention. | `tools/check-ci-retry.py`: every cargo job warms the registry, exemptions named with their reason |
| SRV-05 | A join that cannot fetch its document fails silently | Done | P2 | A join whose document fetch failed was refused with `.map_err(|_| Refusal::NotSaving)` — **the `_` was the entire defect**, discarding the origin's own explanation, which the HTTP transport had already built. Fifteen clients failed to join a URL nothing was serving and the log recorded not one line, while the neighbouring allow-list refusal logs once per attempt. **The metrics were worse than missing.** Four counters were declared, exposed on `/metrics` and documented in `65` while being incremented **nowhere**: `fetches_ok`, `fetches_failed`, `refused_capacity` and `revisions`. So the number an operator alerts on read a hard zero *through a total outage in which every join failed* — the dashboard was not absent, it was lying, which is strictly worse because it answers the question wrongly instead of not at all. The first three are now wired; `revisions` is `SRV-06`. A fetch failure logs WARN with the reason (connection refused, timeout, status code); bytes that arrive and are not a workbook — an origin answering a login page with 200 — log ERROR and move a new `opencalc_documents_unreadable_total`. **The URL is redacted at the query string**, because a host's download URL carries the credential that authorises the download and this path fires exactly when somebody is about to paste their log into an issue. Second half: `OPENCALC_ALLOWED_HOSTS` is compared with the port stripped, so `127.0.0.1:8080` is not a stricter rule but a **dead** one; the refusal now names the entry and says the port can never match, conditionally so a healthy allow-list is never blamed. **What made it invisible:** the failure sat on the server's side of a client-visible refusal, so the only symptom was an edit that never arrived — and the counter that existed to catch exactly that had never been connected, which no test asked about because the exposition format parsed cleanly. | Six tests, each seen red first — the strongest asserting on an **empty log buffer**; five mutations red, including a URL logged whole and a hint that fires unconditionally |
| SRV-06 | `opencalc_revisions_total` is a permanently-zero metric | Done | P2 | `opencalc_revisions_total` was declared, exposed and documented, and incremented nowhere, so a cluster ordering edits reported itself idle. Wired at **both** points a revision is actually ordered — the standalone commit, and the clustered leader *after* a successful log append, so a fenced leader counts as `appends_refused` rather than as throughput. Counted in **operations, not submissions**, because a revision number counts operations; a per-call increment would report a rate the revision numbers themselves contradict. **The gate is the real deliverable and it closes the class.** It reads the `AtomicU64` fields off the `Metrics` struct declaration and asserts each is both exposed and incremented by shipping code, with comments and string literals stripped first — so a comment cannot stand in for an increment, which was proved by mutating the increments into comments *describing themselves* and watching it still fail. Driven from the struct rather than a hard-coded list, because a list in a test is the same artefact as the list in the docs: right the day it is written and silently short the day somebody adds a counter, which is exactly how four of them died. Its honest limit, stated rather than discovered: it proves coverage, not reachability — a counter incremented on dead code would pass — which is why the scenario tests remain. **What made the whole class invisible:** a Prometheus exposition of zeroes parses exactly as well as one of real numbers, so every test that scraped `/metrics` passed. | `every_counter_on_metrics_is_exposed_and_incremented_somewhere`, plus a standalone and a clustered scenario; seen red by declaring an unwired counter and by commenting out an increment |
| SEC-018 | A bracketed IPv6 host can never match the allow-list | Done | P2 | **Worse than the row said, and the row was mine.** It called this a usability defect on the grounds that the parser fails closed. It does not always: `check_url` stripped the port with `rsplit_once(':')`, which cuts `[::1]` at the last colon *of the address*, and the same sloppiness **admitted** `https://[::1]:8443x/…` and `https://[host.example]/…` by misreading a malformed authority as a host on the list. Verified here by reverting the parser and watching `should refuse https://[::1]:8443x/files/1` fail — it was allowed. Replaced with a real authority parser covering `host`, `host:port`, `[literal]` and `[literal]:port`, where the bracketed form must parse as an `Ipv6Addr` and anything trailing the brackets can only be a port. Allow-list matching now compares as an `IpAddr` as well as textually, so `::1`, `0:0:0:0:0:0:0:1` and `[::1]` are one host — an entry that *is* the address being refused, spelled differently, is the same trap as the port one. **What made it invisible:** the failure presented as a refusal, and refusals look exactly like the allow-list working. | Twelve adversarial authorities refused, a bracketed literal matched by any spelling, and the port hint diagnosing `[::1]:8443`; seen red against the old parser |
| SRV-03 | Admission was a guessed count, not the resource it was protecting | Done | P1 | A node refused the thousand-and-first document and the two-hundred-and-first participant whatever it was actually using. Those numbers guessed at what a machine could hold, so they were wrong in **both directions at once**: a 64 GB node turned work away at 8 GB, and a 2 GB node accepted a thousand documents it could not hold. **Both counts now default to 0, meaning unlimited**, and admission reads the node's own memory — the **cgroup** limit rather than the host's, because a 2 GB container on a 64 GB machine is a 2 GB node, and sizing from the host is how a container is killed while `free` looks healthy. **It refuses at 85%, not at 100%, and that is the one thing not taken literally**: reaching the ceiling is an OOM kill, which takes every document on the node including everybody's unsaved work, where refusing one document costs one person a click. `VmRSS` rather than `VmHWM` — the opposite of `PERF-10`'s choice and for the opposite reason: a peak never falls, so a node that spiked once would refuse for ever, including after everything was evicted. An unmeasurable node **admits**, since a machine with no cgroup and no `/proc` must not go from unprotected to useless. Four mutations red: refusing only at the ceiling, an unmeasurable node refusing everything, cgroup v1's unlimited sentinel taken literally, and the peak read for the current size. **Two defects of my own reached main with it, and both are now pinned.** The admission check locked the document registry to count, and one caller already held that lock — so the server **deadlocked on the first join**. It did not crash and logged nothing: the socket went quiet for twenty seconds and then for ever, which is the worst way for a server to fail because nothing anywhere says what happened. Caught by `PROD-13`'s end-to-end gate, which had passed on the run immediately before. The locking and non-locking checks are now separate functions, and a test asserts with a *timeout* that the one called under a held lock answers — a deadlock is invisible to any test that waits for a result. Second: the **participant** check asked the same question, which counts *documents*, so once a node reached its document cap every arrival at an already-open document was refused. Joining costs a connection, not a document. Third, and the one to remember: `0` used to mean *no capacity* and now means *no limit* — a silent inversion of a safety setting, in the dangerous direction, pinned by its own test. Verified end to end: nine clients across three documents, 54 edits sent and 54 acknowledged, p50 15 µs and p99 3.5 ms, where before the fix it hung for ever. | A node admits until its memory is short, and never because of a count |
| SDK-008 | `WorkbookSession` escapes | Done | P1 | Checked all three the row names; only one was a hole. `apply_raw` already refuses in read-only and clears the untouched original; `config_mut` changes settings that are read at open, and toggling read-only mid-session is the host's prerogative. `workbook_mut` is the real one: it hands out the right to change anything, including into a state `validate` rejects. It cannot be supervised — the session does not see what happens through the reference, and validating per call is not affordable because `validate` walks every cell and hosts take that reference per keystroke. So the check is where being wrong stops being recoverable: `save` validates before writing. A corrupt workbook in memory is a bug; one that became a file is data loss. | `an_invalid_workbook_is_refused_rather_than_written` (`OC-MDL-0001`), `taking_a_mutable_reference_gives_up_the_untouched_original` |
| SDK-009 | npm packages ship no types | Done | P1 | `@opencalc/engine` had declarations (wasm-bindgen writes them); `@opencalc/sheet` and `@opencalc/react` had none and no `types` field, so every TypeScript host embedding this got an error and a reason to use something else. Declarations are now hand-written beside the hand-written JavaScript they describe, shipped by the build, and **compiled against**: a consumer written the way the docs tell an integrator to write one, under `strict`. A second check reads both files, because `tsc` proves the surface composes and cannot prove it is real — a `.d.ts` naming a method nobody implemented type-checks perfectly. | CI `sdk-types`: removing the tag map, an event, or adding a method that does not exist each fail it |
| SDK-010 | An SDK sample promised to integrators was never built | Done | P2 | `55` §What ships where lists five runnable samples; four existed. `sdk/examples/host-toolbar` — *our* commands on *their* buttons with all our chrome off — was never written, and **the row said it was assembly rather than design, which was wrong.** Building it found why nobody had: the element could `listCommands()` and `commands({hidden, disabled})` — **discover** them and **suppress** them — and there was no way to *run* one. A host with its own toolbar could learn our command ids, hide the ones its product has no use for, and could not put a single one on a button. The missing third of the surface. `run(id)` now exists on the element, on the editor, and in `embed.d.ts` — declared in all three because `SDK-009` established that a declaration nothing compiles against drifts. **A command *is* its control**, so running one activates that control rather than a second dispatch table beside the first; two tables drift, and the drift is a menu item that works and a host button that does nothing. An unknown or disabled id is **refused at the call**, because a host that mistypes should learn there and not from a user months later. **What made it invisible:** the sample was listed as shipping, so nobody built it and found out it could not be. | Three browser tests: a command runs by id, an unknown id is refused with the way to find the real ones, and **every id `listCommands()` returns has a control behind it** — the two halves agreeing is the property, and a list that lies is exactly what a spot check misses | `sdk/examples/host-toolbar` runs, drives the editor through `commands()` with the chrome off, and is built by whatever builds the other four |

## P2 / P3

| ID | Title | St | Sev | Mechanism | Gate |
| --- | --- | --- | --- | --- | --- |
| COL-38 | A parse failure is reported as `CannotMerge` | Done | P2 | A message the server could not read was answered `CannotMerge`, which names the **transform** — the one part that was working. The socket stayed open, the heartbeat was answered, the roster showed the participant present, and every edit went nowhere; it cost a live debugging session to find. `Refusal::Malformed` now says what happened, and a client seeing it knows not to retry — the same message will not parse the second time either. Shipped on the 5→6 bump it shared with COL-40. | `a_message_that_cannot_be_parsed_is_not_called_a_merge_failure` |
| DEP-09 | Load-aware placement never consumed | Done | P2 | `announce` publishes load and `elect()` picks the least-loaded peer, but `elect` is called only from tests. **Done.** `place()` reads the load back: least-loaded peer, **never this node** (a client that obeys a redirect to the node that just refused it never stops), **only one with a public address**, and **only one under the cap**. Ties break on id through `elect`, so two nodes refusing at the same moment send their clients to the same place rather than splitting them. A **new setting** was unavoidable: `Peer.advertise` is the *internal* address — a service name on the cluster network — which is precisely the address a browser cannot use. `OPENCALC_PUBLIC_URL` is per node and optional; unset, placement does nothing, which is right behind a single load balancer where a redirect would return through the balancer to an arbitrary node. **On the wire, a field and not a `Refusal` variant.** ADR-016 says to move `PROTOCOL_VERSION` when old and new read the same message *differently*; a new variant is worse than that — an unknown tag makes the whole message unreadable, so a client that used to show "this node is full" would show nothing. `Stopped.elsewhere` is `#[serde(default, skip_serializing_if)]`, verified against the shape an old client actually has, and absent from the JSON entirely when there is nowhere to name. The version does not move. Redis carries it in a **sibling key**, not a third tuple element: `peers` reads the body with `from_str::<(String, u32)>` and `continue`s on error, so a longer tuple would make a previous-build node *silently drop* new nodes from its peer list rather than fail loudly. The client follows **one** redirect per session — two full nodes would otherwise name each other and bounce a client forever — and `redirectTarget` refuses anything but `ws:`/`wss:` and any downgrade off TLS, since the value decides where a token gets sent. **Seen red without the fix**: dropping the filters fails 4 of 6 placement tests; making the lookup return nothing fails the end-to-end with `left: None`; removing the URL guards fails 2 of 5 client tests. **A test that could not fail, found on the way.** `a_full_node_turns_a_new_document_away_rather_than_degrading` used `connect(addr)` — which asks for `DOC` while its token names `other-document` — so the join was refused as `NotAuthorised` and the node's cap was never reached. Its assertion was `matches!(answer, Stopped { .. })` with no reason in it, so it passed on the wrong refusal for its whole life: a capacity test that never tested capacity. It only surfaced because `DEP-09` needed that exact scenario and got `NotAuthorised` back. Now asserts the reason, as do both new tests. | A full node redirects instead of refusing |
| DEP-10 | Multi-tenancy documented, not implemented | Done | P2 | One `Verifier`, one key set, and `iss` is never checked — so one tenant's minter can mint for another's document. **P0 for anyone multi-tenanting.** **Done, and not the way the row implied.** Checking `iss` against a policy would have satisfied the acceptance and fixed nothing: with one shared key set the claim is a field the minter fills in, so any tenant holding a trusted key can still sign a token naming another. The boundary has to be the *binding* — `Verifier` now holds one `Trust` per issuer, each with its own key set, and a token is verified against **the keys of the issuer it names**. `OPENCALC_ISSUERS=iss=jwks_url,…` configures it; `OPENCALC_ISSUER` pins the issuer for a single-tenant deployment, where the verified `iss` (never the unverified one used to select the key) does the refusing. The refresh loop iterates every trust, or a second tenant's keys would be frozen at boot — `SRV-02` reintroduced one tenant along. **Seen red without the fix**: routing to the first key set and dropping the binding returns `Ok(Claims { iss: "https://b.example", … })` for a token tenant A signed naming tenant B — a live session on another tenant's document. **What made it invisible**: the acceptance criterion was satisfiable by a check that does not hold. A test asserting "a token from the wrong issuer is refused" passes against a policy comparison; only a test with *two key sets* can tell the two designs apart. | A token from the wrong issuer is refused |
| DEP-11 | Secrets are env-vars only | Done | P2 | No `*_FILE` fallback, so the signing key is visible in `docker inspect` and `/proc/1/environ`. **Done.** New `casual-calc-secrets` crate (std only, no dependencies) reading `NAME_FILE` in place of `NAME`, wired to `OPENCALC_SHARED_SECRET`, `OPENCALC_ADMIN_TOKEN` and `OPENCALC_REDIS_URL` — the last because `redis://user:pass@host` carries a password. Both forms set is **refused, not ranked**: a precedence rule is how a deployment that believes it moved to files keeps running on the variable it forgot to delete. An empty file is refused too, being a mount that did not happen. `unknown_secret_files` names any `OPENCALC_*_FILE` no server reads, because the plural typo leaves a mount that is present, correct and ignored — indistinguishable from a missing secret. **Seen red without the fix**: ignoring the `_FILE` branch fails 6 of 7 tests, and the one that stays green is exactly `the_environment_variable_still_works`. **Two things this turned up.** The environment cannot be written in a test — edition 2024 makes `set_var` `unsafe` and the workspace forbids unsafe — so the rule was split into `resolve(name, inline, file)`, which a test can reach; a rule reachable only through a mutation no test may perform is a rule with no test. And `check-deploy-docs` refused the first draft: the `_FILE` names were synthesised at runtime, so no server named them and the page documented strings that did not exist. Making them literal is what made the misspelling warning possible. A third: the published-secrets test matched `OPENCALC_SHARED_SECRET` as a *prefix*, so it read the `_FILE` path `/run/secrets/…` as a published signing key. Narrowed to require a separator after the name, and **checked that narrowing it did not blunt it** — a real value planted in `.env.example` still fails. | A mounted secret file is read |
| DEP-12 | No backup/restore, and partly non-atomic writes | Done | P2 | No stated RPO; `create`, `upload` and `settings.json` use plain `fs::write`, so a crash mid-upload leaves a truncated file. **The non-atomic half is fixed; backup/restore is still open, hence Partial.** `create`, `upload`, the kept versions and `settings.json` all wrote with `fs::write`, which truncates first — so a crash, a full disk or a killed container in between left a file that exists, has the right name and is **half a document**. That is worse than no document, because a backup taken afterwards copies the truncated version over the good one. All of them now write beside and rename. Two details that carry the weight: the temporary is a **sibling**, because `rename` is only atomic within a filesystem and across one it silently degrades to copy-then-delete — reopening the very window this closes, in the place nobody would look; and a failed rename removes its temporary, because a directory filling with `.part` files is how a disk fills up for reasons nobody can explain. Document is written before metadata, so a crash between them leaves an invisible document rather than a listed one whose bytes are missing. **Honest about the gap:** whether a crash mid-write leaves the old bytes cannot be asserted without crashing, so the sibling property is what the test pins — and a mutation putting the temporary in `/tmp` reds it. **The backup half is now closed too, and the honest part is what it does not promise.** The recovery point is the backup interval and nothing shortens it — no continuous archive, no point-in-time recovery — which `65` now states before offering any procedure. The insight worth keeping: **a backup of a running store is a set of atomic files, not an atomic set of files.** Each write is atomic since the first half of this row, so no single file is ever half-written; but a document is three files written one at a time, so a live copy can hold the bytes without the metadata. Neither is corruption and both are recoverable — the defect was that **nothing detected it**, so a restore looked complete and was quietly short. The host now scans its store at startup and names what does not line up: `invisible` (bytes nothing lists), `dangling` (a listing whose bytes are gone), `unfinished` (a `.part` from a write interrupted before its rename) and `stranded` (versions of a deleted document, costing space nobody has accounted for). **Reported, never repaired**, deliberately: an invisible document may be somebody's only copy and a dangling entry may be the record of one that should be hunted down — opposite treatments, and only an operator knows which. | A crash mid-write leaves the previous file intact; a store missing a document, a listing, or carrying a leftover `.part` is named at startup — each seen red by neutralising the scan |
| PERF-06 | Range precedents scanned linearly | Done | P2 | One edge per range, scanned per popped cell: `O(dirty × ranges)`. 8.98x for 10x the sheet — headroom, not viability. **Fixed** with the row-band buckets the code's own comment had already named as step four: edges are indexed by the rows they cover, so a popped cell looks only in its own band. Ranges too tall to bucket (a whole column spans every band, so filing it everywhere costs more than reading it once) go to a `tall` list that is always scanned — bounded by how many *open* ranges a workbook has rather than how many ranges. Measured, same `dirty_total` both ways so the answer is unchanged: per-edit cost goes 4→7→13→26 µs across 200/400/800/1600 ranges before, and 7→7→9→8 µs after; the 1600-range pass drops 42.2 ms to 12.8 ms. **Below ~300 ranges the index is slightly slower** — the bookkeeping does not pay for itself until there is something to skip. **On the gate, which took three attempts to make real.** Being wrong here is silent — a missed bucket leaves a formula un-dirtied, so it keeps a stale value and nothing errors. The first test compared two copies of the lookup written in the test itself, so mutating the production loop changed nothing it read; the second called `attach` after clearing a cell to simulate churn, but `attach` returns early for a cell with no formula and never detaches, so it churned nothing. Only the third — driving `dirty_from` itself, against a linear-scan twin, plus a direct assertion of the index's own invariant — caught all of: tall edges never scanned, one band indexed instead of all, the sheet dropped from the key, the moved edge not reindexed after `swap_remove`, its old entries not removed, and stale slots left behind by a detach (which answer correctly until `ranges` shrinks past them, then panic). | Per-edit cost flat in the number of ranges |
| PIV-02 | Pivots export as cells, not as a live pivot | Partial | P2 | **The row said "blocked on validating output against Excel"; it was also unimplemented.** Export only ever *retained* an imported pivot's part — a pivot created here reached the file as whatever values its last refresh wrote, so Excel opened a block of numbers with no field list, no layout to change and nothing to refresh. Now written properly: a `pivotCacheDefinition` with `saveData="0" refreshOnLoad="1"` — the decided route, and the honest one, because this engine recomputes a pivot from its source anyway and a records part would publish a second copy of the truth that nothing keeps up to date — plus a `pivotTableDefinition` carrying the layout, the table's own `.rels`, the workbook's `<pivotCaches>` entry and relationship, and both content-type overrides. Field names come from the source's header row, which is what `source_column` indexes into; a blank header falls back to a positional name, because Excel refuses an empty one and two fields sharing a name is a different refusal. **A real bug fell out of the wiring**: a sheet whose *only* attachment is a pivot had its `.rels` part skipped altogether — the emit condition listed comments, hyperlinks, tables, retained rels and charts — so the pivot was written and then unreachable, which is the one way to be worse than not writing it. **Still Partial, and deliberately so.** The acceptance criterion is *Excel* opening it as a pivot, and no oracle here can answer that: the round-trip below goes through this project's own importer, which was written against real Excel files, so a shape it accepts is a shape Excel writes — which is evidence, not proof. Opening one generated file in Excel closes this. **A foreign reader has now opened one, which the row had been waiting for without anybody checking what was available.** Excel is not on this machine; LibreOffice is, it reads OOXML pivots, and `--validate-package` already drives it. The pivot corpus had **no pivot in it**, so the writer had never been handed to any reader but our own. Added one, and LibreOffice does not merely accept the package — it **reads the pivot as a pivot and writes back a richer one**. `name="Sales"`, `rowFields`, `dataField`, `subtotal="sum"` all survive, and it materialises a `pivotCacheRecords1.xml` of **4 records** from our `saveData="0" refreshOnLoad="1"` cache — which it could only do by resolving the source range we named. A reader that had failed to understand the definition would have dropped the parts, as it drops anything it cannot read. **Still Partial, and the remaining gap is now precise rather than total.** The row's acceptance names *Excel*, and LibreOffice's OOXML reader is not Excel's — it is tolerant where Excel is strict, and Excel's repair prompt cannot be tested without Excel. What was unknown before was whether *any* foreign reader could open it; that is now answered. What is unknown is only whether Excel's stricter reader agrees. | `--validate-package` carries a pivot through a LibreOffice re-save; opening one generated file in Excel closes the row |
| CI-014 | A signature change broke the one consumer outside the workspace | Done | P1 | `FID-28` added an argument to `transform`. Every local gate passed — `cargo build --workspace`, `cargo test --workspace`, clippy, docs, the oracle — and `fuzz-build` broke on `main`. `fuzz/` is its **own Cargo workspace**, so `--workspace` never reaches it; the local `check-rust.py` runner did not build it either. CI's authoritative gate for this is the `fuzz-build` job, which is what went red. The whole tree was green while a consumer no longer compiled, and the only thing that knew was a CI job that runs after the merge. `check-rust.py` gained a `fuzz targets` gate. `cargo check`, not `build`: the failure mode is a signature that no longer matches, which type-checking catches in seconds where a sanitizer build takes minutes. The fuzz target now derives its sheet identities from its own base workbook the way the real callers do — passing an empty slice would compile and exercise a path no client takes. | Restoring the break makes the new gate fail with `error[E0061]: this function takes 4 arguments but 3 arguments were supplied` |
| CI-015 | `cancel-in-progress` treated a push to main like a superseded branch, so main's own CI was cancelled 19 times in 40 | Done | P0 | Reported as *"main is red"*. Main was not red — **main had no result at all**, which is worse, because a missing answer reads as a failing one and neither is true. `1aca07f` merged, its `ci` run was cancelled at 47 seconds, and nothing re-ran it. `concurrency: {group: ci-${{ github.ref }}, cancel-in-progress: true}` applied one rule to every ref. **For a pull request that rule is right** — the run is superseded the instant the branch moves, and cancelling it destroys nothing anyone wanted. **For a push to main it is backwards**: there is one run per merge, nobody is queued behind it, and its result *is* the record of whether main is releasable. The config spent the record to save runner-minutes that were not scarce. Counting the last forty main runs: **19 cancelled, 18 success, 2 failure** — so for months the honest answer to "does main build?" was unavailable about half the time, and #129's merge run had died the same way at 3m26s. **What made it invisible: the failure mode of a cancellation is that it looks like a decision somebody made.** A red job invites a look; a grey one reads as housekeeping, and nineteen of them in a row never once prompted the question. Fix is the asymmetry the original expression missed — main is excluded by name, branches still cancel. The gate found a **second** instance on its first run, `pages.yml`, which is exempted on the record because it is a deploy rather than evidence: the run that cancels it publishes newer content to the same place. | `tools/check-workflow-concurrency.py`, wired into `repository-policy` and seen red — restoring `cancel-in-progress: true` gives ``ci.yml: gates main but cancel-in-progress is `true`.`` and exit 1 |
| PROD-08 | Fuzzing reaches some parsers | Done | P2 | The gate asked for every untrusted parser fuzzed and a corpus from several producers. Three were unfuzzed and all three were reachable from an upload: **`casual-calc-import`** — the *primary format*, and the largest parser in the project — **`casual-calc-io`**'s delimited reader, and **`Workbook::from_snapshot`**, which a resume, a cluster hand-off and a peer all feed. **They found three defects immediately, two of them P0.** `MODEL-01`: serde_json reads floats approximately unless told otherwise, so a tenth of every document's numbers changed across a snapshot — fixed. `IO-01`: CSV export writes `0` for anything under 5e-16 — filed with a reproducer. `IO-02`: `write_delimited` walks the bounding box rather than the populated cells, so 16 KB of input exhausted 2 GiB — filed. Corpus now spans **five producers** (Excel, LibreOffice including its OOXML *export* filter, openpyxl, XlsxWriter and this repo), read from `fixtures/` in place so seeds cannot drift from checksummed fixtures. **The snapshot hold became a proof**, as the ODS amplifier did: it used to skip drifting inputs, and now asserts they do not drift — so losing `float_roundtrip`, which is one word in a manifest that no other test would miss, fails the fuzzer by name. **What made all of it invisible:** every existing test compared this code to itself. The `xlsx` budget assertion nearly repeated that mistake — the shipped cell limit is 8 000 000 and no fuzzer input can reach it, so the check would have been an unreachable branch; each pass also imports under a tight budget, forced red to prove that path is entered. **Still uncovered, named rather than implied:** the JWKS keyset parser, and clipboard `text/html`, which is genuinely untrusted and needs a JS instrument cargo-fuzz cannot provide. | `cargo fuzz run` clean on xlsx (147k), delimited (587k) and snapshot (8.35M) executions; `tools/check-fuzz-lock.py` keeps the separate lockfile honest |
| MODEL-01 | A snapshot round trip changed a tenth of every document's numbers | Done | P0 | `serde_json` writes with `ryu` — exact and shortest — and **by default reads with a fast approximation**, so `from_snapshot(to_snapshot(w))` was not `w`. Measured independently here at **10.97%** of values at ordinary spreadsheet magnitudes coming back one ULP out, and **278 of 2001** through the real snapshot path; `12.805579049246651` is one of them. `f64::from_str` on the *same string* is correct every time, so neither the writer nor the format was ever at fault — only the reader. A snapshot crosses a **resume, a cluster hand-off and a save back to the host**, so a tenth of a document's numbers changed in the last bit the first time it took any of those paths. That breaks the first two priorities outright: never produce a wrong cell value, and identical input gives an identical model. It converges after one step rather than compounding, which is the only mercy in it. Fixed by enabling `float_roundtrip`, declared on each crate that parses rather than relied on through feature unification — unification does not apply to a crate built alone, and `cargo test -p casual-calc-model` is exactly where the drift would be tested for. **What made it invisible:** every test compared a workbook to itself *within one process*, and the defect only exists across the serialize boundary. Found by the `snapshot` fuzz target of `PROD-08`, the first thing that ever compared a workbook to itself across it. | 2 000 generated values and the known one survive a snapshot bit-for-bit; seen red without the feature at 278 of 2001 |
| IO-01 | CSV export erases any number smaller than 5e-16 | Done | P0 | `format_number` derived its decimal places from the value's magnitude and clamped them to 15, so for `abs(n) < 5e-16` the clamp refused the digits the value needed: `format!("{n:.15}")` rendered `0.000000000000000` and the cell was written **`0`**. `1e-16`, `1e-300` and `5e-324` all vanished on one save; `-1e-300` became `-0` and then `0`, so the text was not even a fixed point and the **sign** went on the second save. Rounding happens in exponential form now — `{n:.14e}` is 15 significant digits *at any magnitude* — which fixes both ends: the clamp had also been silently denying the promised 15-digit rounding to anything above `1e15`. Notation follows the exponent, positional inside `[1e-6, 1e21)` and exponential outside, so `1e300` is not written as 301 digits. **One trap found in the building:** rounding `f64::MAX` to 15 digits rounds it *out of* `f64`, and a naive version wrote `inf` — which the reader refuses to type, turning a number into text. Guarded, and the guard is fuzz-reachable. Found by the `delimited` target of `PROD-08`; its hold for this defect is gone and is now an amplification assertion instead. | A value below the old threshold survives a round trip **as text and as bits**, and a second save is a fixed point; seen red as `left "0,0,0,0.000000000000001"` against the real values |
| IO-02 | A 63 KiB CSV makes a save allocate a gigabyte | Done | P1 | `write_delimited` walked the sheet's **bounding box**: three cells at `A1`, `(0, n-1)` and `(n, 0)` needed `2n+3` bytes of input and cost `n^2` cell visits, so 16 KB exhausted 2 GiB and a 63 KiB upload made a save allocate a gigabyte — reachable from the file picker and from a WOPI host. **The reasoning matters more than the fix.** A field's position *is* its address, so the two candidate answers were "compact towards A1" and "write cells where they are". Compaction is wrong: it moves every cell and breaks the parse-write-parse fixed point the crate's whole conservative typing policy rests on. What the bounding box added on top of writing cells where they are was **trailing empty fields**, and those are the one thing provably not data — `a,,,` and `a` read back as the same row with the same cell. So dropping them is **lossless**, and needs no cap, no truncation and no compatibility report. Residual stated rather than hidden: an empty row still costs its 2 bytes, so a lone cell at the last grid row writes about 2 MiB of line breaks — linear in the address and bounded by the grid, not quadratic. | A three-cell sparse sheet saves in proportion to its cells; seen red at 4 004 004 bytes for three cells, and the fuzz target now asserts the amplification bound directly |
| WASM-02 | The editor stores an infinity the file format cannot spell | Done | P2 | Found by the worker fixing `IO-01`. `casual-calc-wasm` types a typed-in cell with `trimmed.parse::<f64>()` and **no `is_finite()` check**, unlike `casual_calc_io::type_field`, which requires finite. So typing `1e400` in the editor stores `Number(inf)`; a CSV export writes `inf`, and reopening that file reads it back as **text** — a number that left as a number and returned as something else. The fix belongs in the typing path, not the writer: there is no CSV spelling of infinity that reads back as a number, and the model should not hold one. **Unreachable by the fuzzer**, which is why it needed a person: the reader can never produce this input, so no round-trip target can generate it. | **Done.** The typing path in `casual-calc-wasm` now requires `n.is_finite()`, so `1e400` is kept as the `InlineString` `1e400` — the person sees their own text back rather than a number they never wrote. Gated by `tests/browser/editor.infinite-input.spec.mjs`, which asserts both that the cell holds no infinity and that it survives a save-and-reopen unchanged, with an ordinary `42` beside it so the test cannot pass against a build that mangles everything. **Seen red without the fix**: dropping `n.is_finite()` and rebuilding the bundle failed both tests with `Received string: "inf"`. **What made it invisible**: the defect is in the *typing* path, and every fuzz target we have starts from a reader. No reader can produce an infinity, so no round-trip target could ever generate the input — the same 'comparing our code to itself' blind spot as the ODS reader fed our own documents. A browser test, not a Rust one, because the browser is the only place the typing path runs. |
| MNT-001 | Empty catches swallow command failures | Done | P2 | 105 swallowing catches, audited and classified rather than mass-rewritten. Thirteen were real: a user-invoked command whose refusal left a control that did nothing — the Delete key, Edit ▸ Clear, autofit, comment and defined-name deletion, View ▸ Gridlines. Five were worse than silent: the UI proceeded anyway, so a refused comment delete emptied the textarea and a refused name delete removed the row while the workbook kept the name. The rest are read-only probes whose fallback *is* the answer, and were left alone. | `editor.errors.spec.mjs` — a protected sheet's refused Delete is reported *and* the cell still holds its formula |
| MNT-002 | Integration modules are review monoliths | Done | P2 | Three files, 34k lines: `editor.js` (13.2k), the WASM bridge (11.5k) and formula dispatch (9.8k). **All three are done: formula dispatch here, the WASM bridge as `MNT-004`, `editor.js` as `MNT-005`** — because they are different jobs with different gates rather than more of the same. **The seams were not chosen — they were already written down.** `functions.rs` carried 18 `// --- Statistics ---`-style headings, and the split makes the module system enforce the taxonomy those comments had only been asserting: `aggregate`, `datetime`, `engineering`, `financial`, `info`, `lookup`, `math`, `matrix`, `special`, `stats`, `text`, with `mod.rs` keeping the name catalogue and the dispatch. Nothing exceeds 1.6k lines. One heading was **not** a seam and was distributed instead: *M6-2 built-ins* named a release milestone, so `MEDIAN` sat beside `TEXTJOIN` for no reason other than shipping in the same sprint. Its eighteen members went to their families. Re-exported flat (`pub(crate) use math::*`) so every call site names a function exactly as before: the dispatch is unchanged, and the diff is a move rather than a rewrite. **Proved to be a move, not a rewrite**: all **299** functions present before and after, none lost, none added, and every body identical once whitespace and visibility are normalised — the only textual changes are `rustfmt` rewrapping signatures that grew past the line limit from `pub(crate)`. Two real finds fell out of it: the `ODD*` bond family had been filed under engineering by proximity, and its family prose was a **doc** comment butted against `OddBond`'s own with no blank line, so rustdoc rendered a paragraph about nine functions as the documentation of one struct. | 205 eval tests, all 8 Rust gates including the three oracle steps; the catalogue test now scans every module and both its guards were seen red — an unlisted module (`left: 11, right: 12`) and a submodule arm missing from the catalogue |
| MNT-004 | The WASM bridge is one 11.5k-line module | Done | P2 | The second of `MNT-002`'s three. `casual-calc-wasm/src/lib.rs` was the whole editor API — 11,519 lines, 361 items, 261 exports — in one file. Now fifteen modules beside a thin root: `axis`, `calc`, `cells`, `clipboard`, `collab`, `data`, `formula`, `history`, `io`, `objects`, `sheet`, `structural`, `style`, `view`. **This row previously said the file had "no section headings". That was asserted, not checked, and it is wrong** — it has four. Four, in 11.5k lines, is close enough to none to have made the point, but the row stated a fact about the code that the code did not support, which is the thing this project files rows about. Corrected here rather than quietly. So the seams really were read off the exports rather than enforced from comments, and doing that turned up why the file resisted review: **topics recur**. Charts are defined at 2459 *and* 4297, comments at 1478 *and* 3281, formatting in four separate stretches. The split is therefore a reorder, not a slice — which is why the equivalence proof below matters more here than it did for `MNT-002`. Re-exported flat and **`pub`, not `pub(crate)`**: every one of these was a `pub fn` at the crate root, so a narrower re-export would have quietly removed items from the public API as a side effect of tidying. The modules stay private, so the root is still the only path in. Two items were misfiled by position and moved to where they belong: the `COLLAB` thread-local had landed in `history`, and `ChartWire` in `data`. | **341 functions before and after, none lost, none added, no body differing**; and the JS contract identical — **261 exports before, 261 after, none added or removed**. All 8 Rust gates including the three oracle steps, and 131/131 browser tests against a rebuilt bundle |
| MNT-005 | `editor.js` is one 13.2k-line script | Done | P2 | The last of `MNT-002`'s three, and the only one without a compiler. **No section headings at all**, so the seams had to be derived — and the measurement is what made the job tractable: 409 functions over **118 shared mutable bindings**; 102 functions (967 lines) touch no shared state, **233 (5,155 lines) only *read* it**, and 74 (5,105 lines) write it. ES modules give a **live read-only binding**, so everything in the first two groups moves with the shared state left where it is — no body edited, no assignment site rewritten. That is the whole design: the writers and the 118 bindings stay in `editor.core.js`; 6,098 lines went to twelve topic modules. Owner chose this scope over a full `state.js`-with-setters rewrite, which had no parser available to make the rename scope-aware. **Proved a move**: 409 definitions before and after, 409 *distinct* (counted as a list, not a set — an earlier version of this check keyed by name and would have hidden duplication), none lost, none added, no body differing; and 191 top-level non-function declarations, none of which left the core. | 131/131 browser tests |
| MNT-006 | Two defects the split introduced, and what let them hide | Done | P2 | Recorded separately because both are about *verification*, not about the editor. **A module's sub-imports carry no query.** The page loads `editor.js?v=<mtime>`; the new topic modules imported `./editor.js`. Two URLs are two module instances — every top-level listener registered twice, one keystroke committed two edits, and a single undo popped a phantom and looked like it had done nothing. That is *exactly* the defect `editor.smoke.spec.mjs:126` was written for, and its comment describes the original occurrence; it caught the same thing twice. The fix keeps `editor.js` as a thin entry owning the tag and re-exporting `editor.core.js`, so the core has one URL — and the entry keeps the public **name**, because the gates find the module by `script[src*="editor.js"]` and renaming it would have meant editing the tests to fit the change. **A green probe against an unwired build.** The generator wrote the twelve modules, then threw before rewriting `editor.js` — the banner anchor `let init, wasm;` had already been rewritten to `export let init, wasm;` by an earlier pass — and stderr was going to `/dev/null`. The modules sat as dead code, nothing imported them, and the load probe passed. What caught it was a *number*: `editor.js` was still 13,206 lines when it should have been ~7,800. A third, found the same way: a top-level `const geo = {…}` between two functions was swallowed into a moved function, because the extractor scanned back to the last line starting with `}` instead of counting braces. The equivalence proof compared *functions* only and was structurally blind to a `const` leaving the file — which is why it now checks top-level declarations too. | The three checks that would have caught each: a duplicate-aware definition count, a top-level-declaration census, and a line count on the file that was supposed to shrink |
| DEP-18 | The registry overview cannot be published with an access token | Open | P3 | Found by releasing. `server-v0.0.0` built and pushed all three images — multi-arch, SBOM, provenance, `0.0.0` and `latest` — and then **reported the release as failed** because the last step, syncing the Docker Hub page from `docs/registry/*.md`, got `Forbidden`. Docker Hub's description endpoint refuses an **access token**; it wants account credentials. `DOCKER_SECRET` is a PAT, which is the right thing for it to be — a password in CI is worse than a token that can only push — so this is a real conflict rather than a misconfiguration to correct. **Made non-fatal, not hidden.** The step still runs and still fails visibly, and the run summary now says whether the page synced. The reason is that a release reporting failure when the artifact is public and correct is the more dangerous of the two wrong reports: it invites a re-run of a workflow whose work is already done. `DEP-07`'s promise of a registry overview is therefore *owed and unmet*, which is what this row records rather than the workflow quietly succeeding. Routes: a scoped Docker Hub org token if one can update descriptions, or publishing the page some other way. Not decided. | The Docker Hub page for each image matches `docs/registry/<image>.md` after a release |
| DEP-17 | A deployed build can serve a stale editor module | Done | P2 | A consequence of `MNT-005` that the dev server hid. The cache-buster stamps a `?v=` on URLs **the page names**, and a module's sub-imports carry none — so once the editor became an entry plus fourteen modules, only the entry's URL changed, while `editor.core.js` kept its own. `serve.py` was taught to stamp the entry from the newest mtime across every `editor*.js`, which restores the guarantee locally but says nothing about a deployment: there, `casual-calc-host` serves the editor with `ServeDir`, which sends `Last-Modified` and **no `Cache-Control` at all** — and with no directive a browser may apply *heuristic* caching and skip revalidation entirely. A fresh entry tag over a cached module is the exact stale-module failure the `?v=` was introduced to stop, one level down. `Cache-Control: no-cache` on the editor's static files — cache it, but ask before using it. A 304 per module, and the question is gone. The wasm blob carries its own `?b=` and is unaffected. The service is built by `editor_service`, which `main` nests and the test calls, so the assertion cannot pass against a router nothing wired up. | `editor_modules_are_served_with_revalidation`; without the layer it fails `left: None, right: Some("no-cache")` |
| MNT-003 | The TLS test fixture raced itself, and broke main | Done | P1 | `certificate_files()` guarded on `!cert_path.exists()` and wrote the certificate **before** the key, so a second test thread arriving in that window saw the certificate, concluded the pair was ready, and was handed a key file that did not exist yet. Tests in one binary share a process and run concurrently, so they share the pid-named directory. It surfaced as `a_configured_certificate_means_the_socket_speaks_tls` panicking on `tls_config(...).unwrap()` — **on one platform, sometimes**, which is the shape that gets called flaky and re-run rather than fixed. The race was latent for as long as two callers existed; adding a third (`DEP-15`'s key-error test) made it likely enough to turn main red. **Fixed** with `OnceLock`, which closes the window rather than narrowing it: there is no observable half-written state left to race with. Proved by widening the window to 300ms — which reproduces CI's failure exactly, same test and same line — and then re-running with that widening still in place after the fix. | The suite passes with a 300ms delay planted between the two writes |
| P1C-003 | Text shaping is wired but not drawn | Partial | P2 | The shaper (`harfrust` since `DEP-14`) is behind a feature and correct; `draw_glyphs` still walks per `char`. Bundled fonts cover Latin and Hebrew only — other scripts need a font decision, not a shaper. | A Hebrew run draws right-to-left |
| UX-GRID-02 | `ensureVisible` ignores zoom | Done | P2 | The viewport rect is compared against grid units without dividing by `state.zoom`, so scroll-into-view overshoots at 200%. **The code was already fixed** — on 16 Aug in `0b5e08d`, with no test and without the row being updated, so it has been an ungated fix able to regress silently ever since. The gate is the work. It has to derive the viewport itself from three raw inputs (the rectangle, the zoom, the frozen origin), because the defect lives *inside* that calculation and a hook returning `ensureVisible`'s own answer would agree with the bug and assert nothing. Three cases: a cell reached at 200% sits wholly inside the real viewport, the same at 50% where the error reverses, and reaching a cell needs more scrolling at 200% than at 100% — the last stops a fix that ignores zoom entirely from passing the first two. Seen red under the original defect and under dividing the wrong way. | Scroll-into-view lands correctly at 200%, and is gated |
| UX-P08a | Border palette polish | Done | P3 | Diagonals, composite placements and true double rendering shipped. Line-colour swatch feedback remains. **It did not** — it was implemented and ungated, like `UX-GRID-02` before it. Choosing a colour sets `--oc-x-border-swatch` on the toolbar button and marks the swatch; both are now pinned by a browser test, seen red when the tint is not set and when the marking is dropped. | The toolbar swatch shows the colour the next border will use |
| UX-P09a | Elapsed-time formats | Done | P3 | `[h]`, `[m]` and `[s]` shipped as fields. Locale-prefix polish remains. **Already done**: `numfmt.rs` handles `[$-409]` and carries month and weekday names for every language an `[$-lcid]` prefix can select, and its own comment records the bug that prompted it — a plain `contains('0')` reading the locale id's zero as a digit placeholder. Gated from the outside for the first time: `[$-409][h]:mm` must render as an elapsed time rather than literal text. | An elapsed-time format behind a locale prefix renders |
| UX-P13a | Sheet visibility | Done | P3 | Model, round-trip and hide/unhide UI shipped. Tab overflow, move-to and protection remain. **All three were already there** — the strip is `overflow-x: auto` with `min-width: 0`, `session_move_sheet` is wired to the drag reorder, and sheet protection landed with `UX-PROT-01`. Gated: twenty-four sheets added *through the editor's own button* must scroll rather than squash or push past the window, and a moved sheet must stay moved. | Many tabs scroll; a moved sheet stays moved |
| UX-CHROME-01 | Hiding the menu bar hides the roster | Done | P3 | The roster is a child of the menu bar, so `.oc-hide-menubar` takes it too. `COL-33` put presence in the menu bar precisely so it would not fold away with the page header, and then it folded away with the menu bar instead. **Fixed** by hiding the bar's *items* rather than the bar, and neutralising the bar's own box so that with nobody else present — the roster carries `hidden` until there is a session — it still collapses to nothing. No `:has()`, which has no precedent in this stylesheet and is a compatibility decision a P3 fix should not make on its own. Three gates, on computed style rather than on the class being applied: the roster survives, the menus do not, and a single-user embed is left with no empty strip. Each mutation reds its own assertion. | A host hiding the menu bar keeps the roster |
| UX-CHROME-02 | The deployed stack shows two headers stacked | Done | P1 | Reported by somebody running `docker compose up`, which is the only way it was going to surface. `casual-calc-host`'s document page renders its own header — title, participant name, Share, Download, History — and iframes `editor.html`, which renders the demo header underneath it. Its own comment claims the iframe exists "so this page can keep its own chrome without forking the editor's HTML", and **nothing implemented that**: the frame URL carried no chrome configuration at all. The mechanism already existed and was one layer away — `embed.js` toggles `oc-hide-*` classes and hides the header by default for embedders, on the stated grounds that "an embedded editor is the host's product, not ours", and `editor.css` already had the rules. A host iframing the page directly simply had no way to ask. `editor.html` now honours `?hide=` over the **same** region vocabulary rather than a second one, filtered against the known list because the value arrives on a URL and an unfiltered one is an arbitrary class on the document root. `#tb-status` stays in the DOM when the header is hidden — both this suite and the host's own readiness check wait on it, so removing it would hang the host forever. | The header is visible by default and hidden with `?hide=header`; an unknown region never becomes a class; seen red as "the header is still on screen" |
| UX-A11Y-01 | The grid was too faint to see | Done | P1 | Same report, same session. Gridlines were `#f0f1f4`, which is **1.13:1** against the canvas — measurably fainter than Google Sheets (1.32:1) and Excel (~1.45:1), and far below the 3:1 WCAG 2.1 asks for a graphical object needed to understand the content. A spreadsheet's grid is the definition of that: it is what tells you which cell you are looking at. Raised to 1.44:1 light and 1.66:1 dark, in line with the category. **Deliberately not 3:1 by default** — a hairline at 3:1 reads as a heavy table border and fights the data for attention, which is why nothing in this category ships one. The accessible answer is not a darker default for everybody but honouring the person who has asked for more, so `prefers-contrast: more` now clears 3:1 in both schemes (3.07:1 and 3.22:1). **What made it invisible:** no test can be asked how visible a hairline is, and the value was inside every theme gate because those check that a token is *honoured*, not that it can be *seen*. This one asserts the computed contrast ratio, which is mechanical. | The computed gridline-to-canvas ratio is at least the category's own level; seen red at the shipped `#f0f1f4` as "gridlines are 1.13:1" |
| TAURI-001 | Tauri desktop shell | Partial | P3 | The **backend half of [44](44-TAURI-DESKTOP-SHELL-DESIGN.md)'s diagram is built and tested**: `desktop/` is a crate that opens a workbook, lays out a viewport, edits a cell, undoes, saves, and reopens what it wrote — proved by a test that needs no window. Its own Cargo workspace, per `ADR-023`, with a `check-rust.py` gate **from the day it exists** rather than after it breaks; `CI-014` is what a separate workspace without a gate costs, and repeating that would be the same mistake twice. **Tauri itself is deliberately not a dependency yet.** Every function is already the shape a `#[tauri::command]` takes — bytes and plain values in, JSON out — so adding the attribute is an attribute, not a redesign, and until a window is needed this half is provable by `cargo test` rather than by launching an application. What remains is the webview and the three decisions that need a *running* shell to measure: the Tauri IPC transport cost, the native GPU backend, and parallel-recalc partitioning. **The webview half is larger than the design implies, and the number is now measured rather than assumed.** The browser editor makes **229 distinct engine calls** — 101 that mutate, ~129 that query. *The UI shell renders a display list* is true of drawing, which `RND-10` reduced to one call; it is not true of being an editor. Everything a toolbar, dialog, context menu and shortcut does is in that 229. Which leaves three options at very different costs — bundle WebAssembly in the webview and give up native calc, reimplement 229 commands and keep a second surface in step, or make the editor's engine binding swappable so the calls are written once and dispatched twice. The third keeps both properties and carries the risk a diagram cannot show: two implementations that must agree. Filed as `TAURI-003` rather than chosen in passing. | A desktop build that opens an `.xlsx`, edits it and saves it |
| TAURI-003 | How the desktop webview reaches the engine | Open | P3 | The decision `TAURI-001` cannot proceed past. The browser editor makes **229 distinct engine calls** (101 mutating, ~129 querying) through a single `wasm` binding — so reusing it on the desktop means answering those 229 from native Rust, and the three ways of doing that differ by more than style. **Bundle the wasm build**: the editor works unchanged and the desktop app calculates in WebAssembly, which is the property `ADR-023` and [44](44-TAURI-DESKTOP-SHELL-DESIGN.md) exist to protect. **Reimplement as Tauri commands**: native calc, and a second surface the size of `casual-calc-wasm` to keep in step — `TAURI-002` is the evidence for that cost, where *one* 113-line function in one host stopped a second host typing into a cell. **Swap the binding**: the editor already funnels everything through one `wasm` handle and a `wasmApi()` accessor, so the shape exists; what does not exist is any guarantee the two implementations answer alike, and a divergence would be invisible until a user found it. Worth noting the third has a gate that could exist: run the same call sequence through both and compare, the way `oracle-diff` compares this engine against LibreOffice. That is a real design, not a hand-wave, but it is the sort of thing to decide deliberately. **The gate that made the third option checkable now exists**, built before choosing rather than after: `engine-trace.mjs` wraps the single `wasm` handle the editor already funnels everything through, drives one scripted session, and records every call and every answer. Run the same script against a second engine and compare the traces — the shape `oracle-diff` uses against LibreOffice. A `Proxy` rather than 229 wrappers, for the reason `UX-SITE-03` made the hard way: a rule that enumerates its subjects is one omission from being wrong. Answers are *summarised* rather than hashed, so a disagreement reads as `"=A2*2"` versus `"=A1*2"` and says where to look, rather than as two hashes that say only that you are in trouble. **Proved to detect what it exists for**: a second engine that gets one call subtly wrong — a formula printed absolutely rather than at its cell's origin, which is the exact `PERF-11` mistake the desktop shell itself made — is caught, 1 divergence in 18 calls. Without that evidence the option is a hope; with it, it is a decision with a safety net. | The desktop editor and the browser editor answer the same 229 calls identically |
| TAURI-002 | Typing into a cell was a rule only one host knew | Done | P1 | **Found by building the second host, which is the only thing that could have found it.** `build_set_op` — what typed text *means*: when it is a formula, when a leading apostrophe forces text, which number format `007` implies, when an entry keeps the style already there — was `pub(crate)` in `casual-calc-wasm`. 113 lines of engine semantics living in one host. A desktop shell could not type into a cell without reimplementing all of it, and the parts it would get wrong are exactly the ones nobody remembers. Nothing about it was ever WebAssembly-specific: it takes a session and returns an `Operation`. It is now `WorkbookSession::input_edit`, with its inverse `cell_input`, and **128 lines are gone from the wasm bridge**, which calls the shared one. Two hosts, one implementation — the same fault `RND-10` fixed for drawing, caught here *before* the duplicate existed rather than after. | `desktop/` compiles against the SDK alone and its shell test passes; the browser's 151 tests pass against the moved implementation |
| SDK-011 | `blank()` returns a workbook with no sheets | Done | P2 | Found while building the desktop shell, and fixed rather than mirrored. `WorkbookSession::blank()` produces a workbook of **zero** sheets — right for building one up programmatically, wrong for every interactive host: a window whose workbook has no sheets has nothing to draw, no tab strip and no cell to put a caret in. **Both hosts had already worked that out separately.** The browser pushed a `Sheet1` in `session_new`, the desktop shell pushed one in its own constructor — a rule written down nowhere, kept in two places, and a third host would have rediscovered it or shipped a blank window. `with_sheet()` sits **beside** `blank` rather than replacing it. Thirty-six callers use `blank` and most add sheets of their own, so changing what it returns would hand them two — a name that says which one you want costs less than a subtle change to what an existing one means. Both hosts now call it and their duplicated fixes are gone. **A mutation that proved nothing, caught before it was believed.** The first check ran `cargo test -p casual-calc-sdk "a\|b"` — which is not a cargo filter, so *neither* test ran and the mutation reported "ok, 0 passed, 77 filtered out". Read quickly that looks like a pass. Running each test by name shows both failing `left: 0, right: 1`. A test count of zero is not a green result, and the alternation syntax that works in `grep` does not work here. | `a_new_session_can_be_asked_for_a_sheet_to_open_on` asserts both constructors together, so merging them would fail it; `the_opening_sheet_is_a_sheet_you_can_type_into` types a formula into the sheet a host is handed and checks its id is its own |
| DOC-029 | docs/44 describes a host-capability trait that does not exist | Done | P3 | `docs/44` described a host-capability trait the engine depends on. **No such type exists** anywhere in `crates/`, `server/` or `sdk/` — the name appears only in prose, in `08`, `19`, `44` and the changelog. What was actually built is two unrelated seams and a hole: `Environment` (a value, installed onto `Workbook::volatile_now`), `Cancel` (a real trait, in `casual-calc-model/src/cancel.rs`), and **no threading seam at all** — one `std::thread` hit in the whole workspace, in a stack-depth test. The doc now names the seams that exist, with citations, and says plainly that the trait is not built. **The promise is not lowered**: `19` invariant 7, `02` and `40` still state it, and the overdue decision is tracked as `ADR-019` rather than edited away. Two smaller fictions went with it — an `Engine` type that was never built, and a diagram claiming `casual-calc-io` opens `xlsx/ods/csv` when it depends only on `-model` and `-layout`. **What made it invisible:** `44` claims no CI gate at all, so nothing could have caught it; it was found by reading the design before building on it. | `docs/44` names only seams that exist, and each real gap is a row instead of a prose edit |
| SEC-017 | The only shipped host cannot stop a long job | Done | P1 | `SEC-012` made admission and full recalculation stoppable, and **the only shipped host took none of it**: `session_open` called plain `WorkbookSession::open`, `session_recalculate` plain `recalculate()`, and both landing-page helpers plain `import_package`. So the scenario `cancel.rs`'s own header describes — a workbook inside every limit and simply enormous holding the tab's one thread — was still what a browser user got. Fixed with a `performance.now()` deadline behind `session_set_time_budget_ms`, wired to all four, with the editor bounding an open at 10 s and F9 at 5 s. **A deadline, not a flag, and that is the design**: JS and wasm share one thread, so a `stop()` handler cannot run until the job it would stop has already returned — a settable flag would look like cancellation and never once fire. A **Keep waiting** button re-runs with no limit, because a limit the user cannot overrule is a refusal. `session_recalculate` now *returns* its outcome; it returned nothing, so a partial pass was indistinguishable from a complete one. **What made it invisible:** `SEC-012`'s tests exercised the SDK, where the seam is, not the bridge, where it went unused — and a seam that is cancellable looks taken. **Still open:** `collab_receive` recalculates uncancellably; left deliberately, because what an acknowledged-but-stale revision means to a peer is ADR-012's territory, and no test could be made to fail without deciding it first. | Three browser tests; seen red with the budget token neutralised — "a stopped open reported success", "F9 reported a finished calculation on a pass that was stopped part-way" |
| COL-43 | A relayed edit recalculates uncancellably | Done | P2 | The third instance of `SEC-017`'s class, and the one that could not be fixed with it. `collab_receive` calls plain `session.recalculate()`, so a peer's edit that triggers an expensive pass holds the tab exactly as an oversized open used to. Three lines to change and deliberately not changed: with no budget set the cancellable call is bit-identical, so **no test could be made to fail**, and making it meaningfully cancellable means deciding what an acknowledged-but-stale revision means to a peer — which is ADR-012's territory. A convergence question, not a plumbing one. Filed rather than fixed because it needs the decision first, which is the only reason this session accepts for filing. **Done, to the owner's decision: apply the edit, mark values stale.** `collab_receive` uses `recalculate_cancellable(&budget_token())` and returns the outcome. Convergence is defined on the **model**: the operation is applied and acknowledged before the recalculation starts, so the document converges on cell content whatever happens next, and only derived values are left behind — the session is already marked stale by the SDK, and the host is told so it can finish rather than present a half-fresh sheet as final. The budget is **per batch, not per operation**: twenty relayed edits at 50 ms each is a second of frozen tab, which is the thing being prevented rather than a smaller version of it. The editor passes the same budget a local F9 gets — a peer's edit is not entitled to more of this tab than the person sitting at it. **Two test-design traps, both of which produced a green test against unfixed code.** First, `CANCEL_CHECK_INTERVAL` is **4096**: the engine asks the cancel token every few thousand evaluations, not every cell, so the first draft's 400 formulas finished without ever asking and a zero budget changed nothing. The workload is 6000 formulas now, pasted as **one** operation because 6000 separate edits would be 6000 recalculations of a growing sheet. Second, the client guarded the budget on `recalcBudgetMs > 0`, which silently turned the test's zero budget into an unbounded one — the engine's own convention is that **negative** means no limit and `0` is a real budget of no time. Inventing a second convention next to an existing one is how a test stops testing. **Seen red without the fix**: restoring the plain `recalculate()` fails with *"no relayed batch reported stale values, so the recalculation was never cancellable"*, while the convergence half stays green — which is the point, since convergence was never the thing at risk. | A peer's edit that exceeds the budget leaves the tab responsive and the document convergent, asserted with two browser clients |
| DOC-032 | DOC-025's own gate is a gate CI does not run | Done | P1 | `DOC-025`'s acceptance is *No doc claims a gate CI does not run*, and `67` states its gate as *a documentation consistency check rejects the known contradictory phrases and validates generated facts*. **No such check exists.** CI runs exactly six `tools/check-*.py` scripts — tracker-ids, deploy-docs, site-nav, env-example, theme-tokens and ci-retry — and no doc-consistency check. The row that exists to catch unbacked gate claims is itself one. Worse, its headline number is unbacked too: **the "142 stale claims" has no list, no tool and no artifact**. The string appears exactly once in the repository, and `git log -S` shows it was introduced by the consolidation commit itself rather than by any audit; the pre-consolidation row named ten documents and no count. A backlog nobody can enumerate cannot be worked down, only carried. **Built.** `tools/check-doc-claims.py` runs in CI over 55 documents and 19 job names, asserting two mechanical things: every `tools/check-*.py` a document names exists *and* is run by a workflow, and every name a document calls a CI job is one. It caught its own row on the first run — `DOC-032` named the script before CI ran it. What it deliberately does **not** check is prose with no identifier in it: "this is covered by a test" cannot be verified by grep, and a gate that pretended to would be the very thing it exists to prevent. The unbacked "142 stale claims" figure is untouched by this and remains `DOC-025`'s problem: the string appears once in the repository and `git log -S` shows the consolidation commit introduced it, so there is no audit behind it. | `tools/check-doc-claims.py` runs in CI and fails on a doc naming a gate absent from the workflows |
| ADR-019 | The dual-host capability trait is three phases past its deadline | Done | P2 | `08` lists the capability-trait ADR as Pending, *to be Accepted at Phase 0*. Phases 0-1E, 2 and 3 have all shipped without it, and what got built instead is two unrelated seams (`Environment`, `Cancel`) and no threading seam. Meanwhile `19` invariant 7, `02` and `40` each state the trait in the **present tense**, so four documents describe as existing a thing that was never decided. The decision to make is whether it is one trait or the shape the code actually grew into. `TAURI-001` is the first work that cannot start without it, which is why this surfaces now. Two sibling ADRs are also overdue on the same register. **Decided by the owner: the single trait is rejected.** What grew instead — a value for the clock, a predicate for cancellation, and no threading seam because nothing is parallel — is accepted as the design, on the grounds that the three share only the property that the engine cannot supply them, which is not enough to make them one interface. Boundary invariant 7's wording becomes "no engine crate names a platform", and `02`, `19` and `40` now say that instead of promising a type nobody wrote; they were left promising it until the decision was made, because editing them first is how a decision gets made by nobody. `tools/check-host-seams.py` enforces it. `TAURI-001` is unblocked: a desktop shell supplies values and has nothing to implement. | The ADR register has no Pending entry whose stated deadline has already passed |
| DOC-031 | ADR-018 says an unshaped build reports it, and nothing can be asked | Done | P2 | `64` promises a build without shaping "reports that it lacks it rather than silently producing wrong output — a caller rendering a thumbnail can then decide, rather than discovering it from a customer", and **nothing could be asked**. `casual-calc-render` exposed the render entry points, the data-bar style and its constants; a caller rendering Arabic on a WebAssembly build got the glyphs in the wrong order and no indication of it. Now `shaping_available()` — `const`, so it costs nothing to branch on — and `needs_shaping()`, which is a *script* question answerable from the string alone, so a host can ask before rendering anything. Neither decides anything alone: an unshaped build is fine until the document contains Arabic, and Arabic is fine on a build that shapes; the pair is what identifies a thumbnail that will be silently wrong. **The gate was inert when first written, and that is the part worth keeping.** Asserting `shaping_available() == cfg!(feature = "shaping")` passes on the default build even when the function is hardcoded `true`, because both sides are then true — the mutation did not bite. It is only meaningful where the feature is **off**, which is the browser profile, and CI never built it. The `wasm` job now runs `cargo test -p casual-calc-render --no-default-features`, and the mutation reds two tests there. **What made the original invisible:** the promise was about the build nobody tested. | `shaping_available()` matches the feature, and CI runs the unshaped profile; seen red there with the answer hardcoded, and with Arabic removed from the script ranges |

## The Excel parity backlog

[73](73-EXCEL-UX-PARITY-AUDIT.md) holds roughly thirty-five findings that have no
row here yet — the largest untracked surface in the repository. Its
`[verified]`/`[unverified]` split is the point of it: three verified P1s became
`FID-23`, `UX-NAV-01` and `UX-NAV-02` and are closed, and the rest must be
reproduced before they are scheduled. Rows arrive here as they are verified;
the audit is worked down, not merged in wholesale.

## Where a finding goes

One question, in order:

1. **Does the code do something wrong?** A row here.
2. **Does a document describe code that does not exist?** Fix the document. No
   row — a doc edit is not tracked work.
3. **Does a document state a contract the code does not keep?** A row here, and
   **the document keeps its text.** Never resolve this by deleting the promise:
   that is how a security bound disappears without anyone deciding to drop it.
   [23](23-CELL-STORE-REPRESENTATION.md) says it plainly — *a documented limit
   nothing in the code enforces is not a limit*.

A new tracking document is never the answer. That is how there came to be
thirteen.
