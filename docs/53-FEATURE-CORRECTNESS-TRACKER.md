# 53 — Feature Correctness Tracker

_Every feature the app exposes, and whether it is implemented **through the
model** or patched around it. Rows are generated from the code by
[`tools/feature-audit/inventory.py`](../tools/feature-audit/inventory.py), not
written from memory._

The fidelity phase ([51](51-FIDELITY-GAP-AUDIT.md),
[52](52-FIDELITY-TRACKER.md)) took the file layer to 95.9% structural and 0
destructive gaps. It answered *"does a workbook survive a round trip"*. This
tracker answers a different question the first one cannot:

> **Does the app do the right thing, through the model, for every feature it
> already claims to have?**

## The five questions

A feature can pass any one of these and fail the rest. "It works" in a demo
usually means only the first.

| | Question | Failure looks like |
| --- | --- | --- |
| **UI** | Is it reachable — a command, button, dialog or panel? | The construct is supported but nobody can use it |
| **MODEL** | Does it change the workbook model rather than editor-local state? | Works until reload, then gone |
| **UNDO** | Does the change go through `Operation`, so undo reverses it? | Ctrl+Z silently does nothing, or undoes the *wrong* thing |
| **ROUND-TRIP** | Does the change survive a save and reopen? | Silent data loss — the class 51 was built to find |
| **RENDER** | Does anything draw it? | The file is right and the screen is wrong |

## Current state

Measured 2026-08-09. `FNS` is the exported wasm surface for the area; `MUT` how
many mutate; `UNDO` how many are reversible.

| Area | FNS | MUT | UNDO | RT | Render | Verdict |
| --- | --- | --- | --- | --- | --- | --- |
| Cell values and formulas | 3 | 3 | 3 | ✅ | ✅ | correct |
| Number formats | 2 | — | — | ✅ | ✅ | correct (styling path) |
| Fonts and text style | 9 | — | — | ✅ | ✅ | correct (styling path) |
| Fill and colours | 3 | — | — | ✅ | ✅ | correct (styling path) |
| Borders | 3 | 1 | 1 | ✅ | ✅ | correct |
| Alignment, wrap, indent, rotation | 5 | — | — | ✅ | ✅ | correct (styling path) |
| Merge | 5 | 3 | 3 | ✅ | ✅ | correct |
| Freeze panes | 1 | 1 | 1 | ✅ | ✅ | correct |
| Rows/columns: size, hide, outline | 17 | 12 | 12 | ✅ | n/a | correct |
| Insert/delete rows, columns, cells | 5 | 5 | 5 | ✅ | n/a | correct |
| Sheets: add, rename, reorder, tab colour | 7 | 5 | 5 | ✅ | n/a | correct |
| Autofilter | 7 | 5 | 5 | ✅ | n/a | correct |
| Sort | 2 | 1 | 1 | ✅ | n/a | correct |
| Defined names | 2 | 1 | 1 | ✅ | n/a | correct |
| Clipboard and paste special | 5 | 2 | 2 | n/a | n/a | correct |
| Fill handle and series | 2 | 1 | 1 | n/a | n/a | correct |
| Find and replace | 4 | 2 | 2 | n/a | n/a | correct |
| Conditional formatting | 6 | 5 | 5 | ✅ | ✅ | correct |
| Comments and threads | 6 | 3 | 3 | ✅ | ✅ | correct |
| Data validation | 5 | 3 | 3 | ✅ | ❌ | undo correct; FC-16 render |
| Named cell styles | 2 | 1 | 0* | ✅ | ❌ | correct (*definition registration only) |
| Sheet visibility | 2 | 1 | 1 | ✅ | n/a | correct |
| Sheet protection | 2 | 1 | 1 | ✅ | ❌ | correct |
| Hyperlinks | 3 | 1 | 1 | ✅ | ✅ | correct |
| Tables (ListObjects) | 5 | 3 | 3 | ✅ | ✅ | correct |
| **Print setup** | **0** | 0 | 0 | ✅ | ❌ | **FC-09** |
| **Charts, drawings, images** | **0** | 0 | 0 | ✅ | ❌ | **FC-10** |
| Rich text runs | — | — | — | ✅ | ✅ | correct (render) |

## Rows

Status: 🔴 Todo · 🟡 Partial-in-progress · ✅ Done.

### Undo is broken, not missing — the worst kind

These fourteen mutations write straight to `workbook_mut()` and never construct
an `Operation`. That is not "undo is unimplemented": undo *appears* to work,
because the button is enabled and the history has entries. Pressing it after
editing a comment reverses **the last cell edit instead**, which is worse than
doing nothing — the user loses work they did not ask to lose, in a place they
were not looking.

| ID | Feature | Sev | Status | Note |
| --- | --- | --- | --- | --- |
| FC-01 | Conditional formatting: `add_cf`, `clear_cf`, `delete_cf_rule`, `reorder_cf_rule`, `set_cf_stop` | P0 | ✅ | Fixed by widening the mechanism, not by patching the call sites: `SheetMetadata` — which already had a proven inverse for the positional state — now also carries validations, conditional formats, comments, visibility and protection, so all thirteen mutations route through the existing `edit_sheet_metadata` and become reversible at once. Adding five new `Operation` variants would have meant five new inverses to get right. `session_set_sheet_visibility` keeps its "at least one visible sheet" check *outside* the closure, since an operation closure has nowhere to report an error to. **The regression test was verified by reintroducing the bug**: with the bypass restored it fails with the real symptom — `undo after a comment edit destroyed the preceding cell edit` — and passes once fixed. A guard never seen to fail is a comment. |
| FC-02 | Comments and threads: `set_comment`, `reply_comment`, `resolve_comment` | P0 | ✅ | Fixed by widening the mechanism, not by patching the call sites: `SheetMetadata` — which already had a proven inverse for the positional state — now also carries validations, conditional formats, comments, visibility and protection, so all thirteen mutations route through the existing `edit_sheet_metadata` and become reversible at once. Adding five new `Operation` variants would have meant five new inverses to get right. `session_set_sheet_visibility` keeps its "at least one visible sheet" check *outside* the closure, since an operation closure has nowhere to report an error to. **The regression test was verified by reintroducing the bug**: with the bypass restored it fails with the real symptom — `undo after a comment edit destroyed the preceding cell edit` — and passes once fixed. A guard never seen to fail is a comment. |
| FC-03 | Data validation: `set_validation`, `set_list_validation`, `clear_validation` | P0 | ✅ | Fixed by widening the mechanism, not by patching the call sites: `SheetMetadata` — which already had a proven inverse for the positional state — now also carries validations, conditional formats, comments, visibility and protection, so all thirteen mutations route through the existing `edit_sheet_metadata` and become reversible at once. Adding five new `Operation` variants would have meant five new inverses to get right. `session_set_sheet_visibility` keeps its "at least one visible sheet" check *outside* the closure, since an operation closure has nowhere to report an error to. **The regression test was verified by reintroducing the bug**: with the bypass restored it fails with the real symptom — `undo after a comment edit destroyed the preceding cell edit` — and passes once fixed. A guard never seen to fail is a comment. |
| FC-04 | Named cell styles: `apply_cell_style` | P0 | ✅ | Already correct in its user-visible effect: the styling goes through `apply_style_range`, which builds an `EditOperation::Batch`. What escapes the log is the *registration* of a built-in style definition in `Workbook::cell_styles` — inert, idempotent, and re-registered on the next use. Left alone deliberately rather than contorted into an operation for a number's sake; the audit tool still counts it, which is the honest reading. |
| FC-05 | Sheet visibility: `set_sheet_visibility` | P0 | ✅ | Fixed by widening the mechanism, not by patching the call sites: `SheetMetadata` — which already had a proven inverse for the positional state — now also carries validations, conditional formats, comments, visibility and protection, so all thirteen mutations route through the existing `edit_sheet_metadata` and become reversible at once. Adding five new `Operation` variants would have meant five new inverses to get right. `session_set_sheet_visibility` keeps its "at least one visible sheet" check *outside* the closure, since an operation closure has nowhere to report an error to. **The regression test was verified by reintroducing the bug**: with the bypass restored it fails with the real symptom — `undo after a comment edit destroyed the preceding cell edit` — and passes once fixed. A guard never seen to fail is a comment. |
| FC-06 | Sheet protection: `set_sheet_protected` | P0 | ✅ | Fixed by widening the mechanism, not by patching the call sites: `SheetMetadata` — which already had a proven inverse for the positional state — now also carries validations, conditional formats, comments, visibility and protection, so all thirteen mutations route through the existing `edit_sheet_metadata` and become reversible at once. Adding five new `Operation` variants would have meant five new inverses to get right. `session_set_sheet_visibility` keeps its "at least one visible sheet" check *outside* the closure, since an operation closure has nowhere to report an error to. **The regression test was verified by reintroducing the bug**: with the bypass restored it fails with the real symptom — `undo after a comment edit destroyed the preceding cell edit` — and passes once fixed. A guard never seen to fail is a comment. |

The fix is the same in every case: extend `Operation` so the change is
expressible, or route it through the existing `SetSheetMetadata` capture/install
pair that sheet-level state already uses. Not a per-call patch.

### Supported in the file, unreachable in the app

Constructs that round-trip correctly and have **no way to create or edit them**.
Opening a workbook preserves them; the user cannot make one.

| ID | Feature | Sev | Status | Note |
| --- | --- | --- | --- | --- |
| FC-07 | Hyperlinks: insert, edit, follow, remove | P1 | ✅ | Insert/edit dialog, underline + accent cue on the grid, hover tooltip, click to follow, and remove — all through `SheetMetadata`, so the whole thing is undoable. **The dialog offers both destinations rather than a mode switch**, because the schema treats `r:id` and `location` as independent and a link may carry both ("open that document at this anchor"); making the user pick one would model the feature more narrowly than the format does. Following is guarded three ways so it cannot hijack ordinary work: not while picking a formula reference, not with a modifier held (Ctrl-click selects a linked cell without leaving), and not on a cell that is already selected — that is a drag or a rename starting, not a request to navigate. External targets open with `noopener`, or the opened page can reach back through `window.opener` and navigate this one. Internal anchors resolve through `parseNameRange`, the same parser the name box uses, so a link accepts exactly the references a user can type. Verified in-browser end to end including undo removing the link and redo restoring it. |
| FC-08 | Tables: Ctrl+T, banded rows, header filter buttons, totals row | P1 | ✅ | Ctrl+T and a context-menu command create a table; header shading and banded rows are painted; "Convert to range" removes one. All through `SheetMetadata`, so creating or removing a table is a single undo step. **The header question is asked, not guessed**: whether the first row is a header decides the column *names*, and a structured reference resolves by name — a wrong guess leaves every `Table[Column]` pointing at the wrong column while still resolving, which is silent. Empty headers become `Column1…`, duplicates get a suffix, and the table name is made unique across the workbook, all for the same reason: a duplicate name resolves to whichever came first and reads the wrong data. A single-cell selection expands to the surrounding block via `session_block_bounds` rather than asking the user to select the table first. Bands count from the first *data* row, so a header does not shift the stripe pattern by one. Band colours are theme-derived and deliberately faint — a band strong enough to compete with a cell's own fill makes the user's formatting harder to see, not easier. Verified in-browser: Ctrl+T on one cell, then `=SUM(Table[Amount])` resolving to 450 against the table the UI had just made. **Completed further**: header filter buttons (the existing autofilter button renderer generalised to take a list of header regions rather than the sheet's single filter, so tables and the sheet filter share one implementation), a totals-row toggle that moves the table's bottom edge — leaving the range alone would make the last data row read as the totals row — and **auto-expand**, so typing directly below or beside a table takes the cell in. That last one is what makes a table worth having: the range, the banding and every structured reference follow the data instead of needing to be re-pointed by hand. Expansion is limited to exactly one row below or one column right; anything further would swallow unrelated data. A new column gets a generated name, or a structured reference to it has nothing to resolve against. **Still missing**: the table style gallery and calculated columns. |
| FC-09 | Print setup: margins, orientation, headers/footers, breaks | P2 | 🔴 | carried verbatim; a UI means modelling it properly first |
| FC-10 | Charts, drawings, images | P2 | 🔴 | retained byte for byte; not drawn, not editable |

### Modelled and round-tripping, but invisible

The file is right and the screen is wrong. From
[`depth.py`](../tools/fidelity-audit/depth.py): 70 fields are ROUND-TRIP only.

| ID | Feature | Sev | Status | Note |
| --- | --- | --- | --- | --- |
| FC-11 | Rich text runs — per-character formatting inside a cell | P1 | ✅ | The canvas draws per-run formatting: bold, italic, strike, underline, colour, size, typeface and super/subscript. Runs travel in the existing cell payload and only when the string actually has them — a `runs` key on every cell would bloat a screenful for the overwhelming majority that are plain. **A run inherits rather than replaces**: `<rPr>` carries only what differs, so treating an absent property as a reset would drop the cell's own font on every partially-formatted string. **Measurement is per run too** — each has its own font, so measuring the concatenation with the cell's font gives a width that is wrong wherever they differ, and the overflow scan then borrows the wrong number of neighbouring columns; alignment drifts with it. Super/subscript is drawn smaller and offset by a fraction of the cell size, so it tracks a resized font. Verified by importing a real `.xlsx` with three differing runs rather than by injecting state. LIVE depth 58.7% → 61.3%; fidelity unchanged at 95.9% / P0 0. |
| FC-12 | Gradient and pattern fills | P1 | ✅ | Gradients and patterns paint. **A pattern's *background* fills the cell and its foreground draws the motif on top** — painting only the foreground as a solid is what made every patterned cell a flat block of the wrong colour. The motifs are approximated by density rather than matched hatch for hatch: the alternative is eighteen bitmaps for something almost no sheet uses, and a wrong-density hatch still reads as a hatch, whereas a solid block reads as a fill the user did not choose. A gradient replaces the pattern rather than joining it, matching `<fill>`, which holds one or the other. |
| FC-13 | Superscript / subscript, underline variants, legacy font effects | P2 | ✅ | Super/subscript and the underline variants now draw on cell fonts, as they already did on runs. The vertical shift is applied **before measuring**, so the spill scan and alignment use the size actually drawn rather than the nominal one — measuring first and shrinking after would centre the text as though it were full size. A double or accounting underline is a second rule below the first; drawing one line for all four kinds is what made a ledger's accounting underline read as an ordinary one. The payload carries the *kind* beside the existing boolean rather than replacing it, so every current reader of `u` keeps working. `sup` is a separate key from `va`, which is already vertical alignment — two different properties with confusingly similar names in the format. Verified by importing a workbook with all four. |
| FC-14 | Shrink-to-fit, reading order, justify-last-line, relative indent | P2 | 🟡 | Shrink-to-fit scales the font down until the text fits its own cell. **It also had to be excluded from the overflow scan**, alongside `clip`: both mean "stay inside this cell", so borrowing a neighbour first and then shrinking would leave the text scaled down *and* overhanging — worse than either behaviour alone. Reading order, justify-last-line and relative indent remain modelled but unrendered; they need bidi layout rather than a paint change. |
| FC-15 | `quotePrefix` indicator in the grid | P2 | 🔴 | stored; no visual cue that a value is forced text |
| FC-16 | A table's filter is the table's, not the sheet's | P0 | ✅ | `Table` carried only `auto_filter_ref: Option<String>` — the range, with nowhere to put a rule. Every filter operation read `sheet.auto_filter`, so clicking a table's header button found no filter covering that column and returned an empty checklist: **the reported "filter icon on row 1 shows no data"**. The editor was honest about it in a comment (*"a table's own filter state is not tracked separately yet"*) and drew every table column as unfiltered. Fixed in the model: `Table::auto_filter: Option<AutoFilter>`, so a table's rules round-trip like the sheet's. Import and export now share one `build_auto_filter` / `write_auto_filter` rather than two partial implementations that had already drifted — the table's only ever read the `ref` and dropped every `<filterColumn>` on load, silently. Every wasm filter entry point resolves a `FilterSite` from the column instead of assuming the sheet, and `recompute_filter_hidden` unions across all filters so clearing one does not release rows another hides. New `session_filter_regions` reports every region with its active columns, so table buttons draw filtered state truthfully. |
| FC-17 | Ctrl+A selects the block you are standing in | P1 | ✅ | It selected the used region from A1 on the first press, which on a loaded sheet is "everything" — **the reported "Cmd+A selected all columns and rows"**. Excel's progression is current region → used region → whole sheet, and the first step is the useful one. `session_block_bounds` already computed the contiguous block for Ctrl+T; Ctrl+A now uses it. A block that already *is* the used region skips the redundant middle step rather than appearing to do nothing. |
| FC-18 | Subscript / superscript have a control | P1 | ✅ | Modelled, imported, exported and drawn since FC-13 — with no way to apply it. `session_toggle_vert_align` goes through `apply_style_range`, so it is one undo step like every other character toggle, and the two are mutually exclusive because OOXML has one `vertAlign` per font. `session_cell_format` reports it as `vt` so the menu can tick what is applied. No keyboard shortcut is advertised: Excel has no default for either, and `Ctrl+Shift+Plus` is already Insert Cells. |
| FC-20 | A filter covers the table, not what the selection literally says | P1 | ✅ | The reported repro is the ordinary one: **click the row 1 header, press the toolbar filter**. Taken literally that asks for one row spanning all 16384 columns — a filter with no rows beneath its header, so every checklist was empty, and a button on every column of the sheet including the blank ones. Both reported symptoms, one cause. `session_filter_range_for` decides in the engine: a genuinely two-dimensional selection is taken as given, anything thinner (one cell, one row, one column, the whole sheet) grows to the contiguous block around the first populated cell in it — Excel's rule. It seeds by scanning the populated cells rather than the box, because a whole-row selection is 16384 columns wide and all but a handful are empty. Verified on a real 6 MB / 168k-cell workbook with exactly those steps. |
| FC-21 | A table's style is resolved, not approximated by two constants | P1 | ✅ | Creating a table changed almost nothing: no banding, no header colour, no name. Three separate faults. **(1)** `showRowStripes` went out as a bare JSON `1` while the host compared it to the string `"1"`, so banding never painted on any table — and the two payloads that carry a table were formatted separately, which is how they drifted; they now share one `table_json`. **(2)** The two colours were hardcoded canvas constants, so every table looked the same and a file whose author chose green opened blue. `casual-calc-layout::table_style` resolves `TableStyle{Light,Medium,Dark}{n}` against the workbook theme — the accents cycle in groups of seven, which is why `Medium2` is the familiar blue — and the host paints what the engine returns, swatches included, so preview and grid cannot disagree. Excel keeps its real definitions in its own resources rather than in the file, so the tints are a documented approximation; a green style looking green beats one grey for everything. **(3)** The fills were painted at the end of `draw()`, after the cell text, under a comment claiming the opposite — true only because the old colours were 16%-alpha washes. Opaque fills covered every value in the table. A table also paints its **body**, not just its bands: a table is a light block in Excel whatever the application theme is, and banding alone left light stripes across a dark grid with unreadable text on them. A manual cell fill still wins over the style, as in Excel. |
| FC-22 | Table configuration is a panel, not a modal | P1 | ✅ | Name, style, header row, totals row and the four banding switches, in the existing right-hand tool panel, applied the moment you change them. Each control is one `session_*` call and therefore one undo step; the panel holds no state, so it cannot disagree with the workbook. Creation asks nothing and opens the panel — a modal that asked four questions before showing you anything was slower *and* blind. New engine APIs: `session_rename_table` (rejects a clash rather than silently suffixing — a structured reference resolves by name alone), `session_set_table_style` (style name plus a banding bitmask, one undo step), `session_set_table_headers`, `session_table_style_preview`. |
| FC-23 | A totals row that totals something | P1 | ✅ | `totalsRowFunction` was stored and round-tripped but never evaluated, so a totals row was blank here and in every other reader. Excel writes **both**: the attribute on the column and a real `SUBTOTAL(109, Table[Column])` in the cell. `session_set_totals_function` writes the pair in one `Batch`, or an undo would leave them disagreeing. The 10x codes are deliberate — they skip filtered-out rows, which is the point of a table total. The reference is structured, not A1, so inserting a row widens the total. Turning the row on also writes Excel's "Total" label, and turning it off clears the cells the range gave back — otherwise a stale "Total" sits under the table looking like data. Pickers are per column in the table panel, labelled from the model's own column names. |
| FC-24 | Data validation says what it wants, and `errorStyle` is obeyed | P0 | ✅ | Six modelled fields with no way to reach them, and one of them was a correctness bug: **every rule hard-blocked**, because `errorStyle` was carried through saves and ignored. Only `stop` refuses; `warning` and `information` let the value through. An opened file's advisory rule became a wall with no way past it. Now honoured — with the caveat that Excel *asks* on a warning and this reports instead, because the commit path is synchronous; the outcome matches answering yes, and the edit is undoable. `showDropDown` — which, as the schema defines it, *hides* the list — was also ignored, so a typed-only rule still grew a chevron. The input hint (`promptTitle`/`promptText`) now shows in a tooltip under the cell while it is selected: a constraint explained only after you type something wrong is explained too late. Panel gains the alert style, both titles, and the dropdown flag, and loads the existing wording so Apply no longer blanks it. **Also fixed here:** the rejection message was interpolated into `status.innerHTML`, and that wording comes from the file — a workbook could inject nodes into the page. All three status writes go through a `textContent` helper now. |
| FC-25 | Sheet view switches: show formulas, show zeros | P1 | ✅ | `showFormulas` and `showZeros` were imported, exported and never shown, so a file saved with "show formulas" on opened displaying results and saved back claiming otherwise. Both now change what the grid renders, applied in the payload rather than in `display_text` — they are properties of the *view*, so a copy or an export must still see the value. `showZeros="0"` blanks a zero result, it does not change it: the cell still holds 0 and still totals as 0. Under `showFormulas` a numeric cell stops being flagged numeric, or the host filled the column with `#######` — the rule for a number too wide to show safely, not for text that can spill. In the View menu, with Excel's Ctrl+` on the first. **`rightToLeft` deliberately has no switch**: it is modelled and round-trips, but the canvas lays columns left to right, so a control for it would change the file and nothing on screen. |
| FC-26 | Sheet protection is enforced, and cells can be locked | P0 | ✅ | Protection was stored, round-tripped and toggleable — and **never enforced**, so a workbook whose author locked its formulas opened fully editable and saved back still claiming to be protected. The default is the trap: OOXML locks a cell *unless* it says otherwise, so `<protection locked="0">` marks the input cells and an absent flag means locked; reading it the other way round unlocks the whole sheet. Guarded on the user-edit entry points — set cell, paste, the three clears, insert/delete rows and columns — and not in the transaction layer, because undo and redo must still replay edits made before the sheet was protected. A block containing one locked cell is refused whole, as in Excel. `<protection hidden="1">` now keeps the formula out of the formula bar, and only while the sheet is protected. Format → Protection carries both switches plus the sheet toggle, since neither cell flag does anything without it. The refusal is testable because the decision is a separate predicate: a `JsError` cannot be constructed off-wasm, so a test that went through the guard could only ever panic. |
| FC-27 | Submenu checkmarks | P2 | ✅ | Found while adding Protection: `openMenu` refreshed ticks inside the dropdown, but a submenu is appended to `document.body`, so **no nested item's checkmark was ever set** — Alignment, Number, Zoom and Text overflow all showed nothing whatever the cell had. Refreshed per submenu on open. Also: `JsError` stringifies as "Error: …", so 39 call sites read "error: Error: this sheet is protected"; they share one helper now, and a refused edit keeps the cursor on its cell instead of advancing past the failure. |
| FC-28 | Page setup, and something that actually prints | P1 | ✅ | `PrintSetup` was five verbatim attribute maps: read, written, and unchangeable. A sheet imported as landscape could only ever be saved as landscape. `SheetMetadata` gains `print`, so a page-setup change is one undo step like any other; a panel exposes orientation, paper, scale/fit-to-page, the four margins, printed gridlines and headings, centring, and header/footer text, flattened to `group.attribute` keys that map straight onto the OOXML elements. An empty value *removes* the attribute rather than writing `""` — OOXML's defaults are meaningful, and `orientation=""` is not `orientation` absent. Scale and fit-to-page clear each other, because a file asking for both leaves the reader to choose. **Printing is real, not just recorded**: `session_print_html` renders the sheet as a document honouring the settings — `@page size/orientation`, the margins, gridline and heading borders, and the header/footer with its field codes stripped (printing `&L&"Calibri"Sales` literally is worse than dropping the codes; `&&` survives as a literal ampersand). A separate window rather than a print stylesheet, because the grid is a canvas and a stylesheet has nothing to lay out across pages. Verified the generated document, not the browser's print dialog — invoking it would block the automation. **Not yet:** `Print_Area` and `Print_Titles` defined names, and page breaks, are still carried verbatim. |
| FC-29 | A defined name the parser cannot read is kept, not dropped | P0 | ✅ | Found while building print titles. `import` parsed each `definedName`'s `refersTo` and, on failure, **discarded the whole name** (`RetentionOutcome::NotRetained`). The commonest casualty is `Print_Titles`, whose value is a whole-row reference (`Sheet1!$1:$2`) — a form this formula parser does not support — so **every workbook with repeating print titles silently lost them on save**, as did any name using `$A:$A`. New `Expr::Raw` keeps the text exactly as written: it prints back verbatim, evaluates to `#NAME?` rather than inventing a value, is left alone by the reference rewriter (guessing at references inside text you cannot parse would corrupt what was preserved), and is treated as a name by the dependency graph so recalculation stays conservative rather than stale. Round-trip tested with `$1:$2` and `$A:$A` alongside a readable name. **The underlying gap remains**: whole-row and whole-column references do not parse, so `=SUM(A:A)` is still `#NAME?`. That needs a change to `CellReference`, the printer, the rewriter and ~15 evaluator sites that iterate `a.row..=b.row` — a million-row loop if it is done naively — so it is its own unit of work, not a rider on this one. |
| FC-30 | Print area and repeating rows | P1 | ✅ | Both are ordinary sheet-scoped defined names with reserved names — that is the entire mechanism — and both were carried verbatim with no way to set one and no effect on anything printed. `Print_Area` narrows what `session_print_html` renders; `Print_Titles` rows go into `<thead>`, which is exactly what a browser repeats at the top of each printed page, and are skipped in the body so the first page does not show them twice. Set-from-selection and Clear in the page panel. The title reference is written through `Expr::Raw`, so the file gets precisely the `Sheet1!$1:$2` Excel expects rather than something reworded to suit the parser. |
| FC-31 | Whole-row and whole-column references | P0 | ✅ | `=SUM(A:A)` was `#NAME?`: the parser had no notion of a reference that names only one axis, which is also why `Print_Titles` (`Sheet1!$1:$2`) could not be read and was being dropped (FC-29). **Design:** `CellReference` gains `row_implicit` / `col_implicit`. The unnamed side still carries a usable bound — 0 at a range's start, the sheet limit at its end — so every consumer that reads `.row`/`.col` without knowing about the flags keeps working and simply sees the widest span; only the printer and the clamp look at them. That is what kept the change to four files instead of forty. **Parsing:** an axis token is a reference *only* when a colon follows it, or every one-letter defined name would become a column. **Printing:** the unnamed side is omitted, so `A:A` does not come back as `A1:A1048576` — a different range. **Cost:** the literal bounds are a million rows, so every site that walks a range now goes through `ranges::range_bounds`, which narrows an unnamed axis to the populated extent (the store's last row is O(log n); a whole-row reference asks only its own rows for their widest column). 200 open-range formulas recalculate in milliseconds, and there is a test that fails if the clamp is ever bypassed. **Dependencies:** an open range is treated as name-dependent rather than given a span, because a span frozen at today's extent goes stale the moment a cell appears below it — tested by adding a cell 50 rows down and checking the total follows. **Rewriting:** an unnamed axis is not shifted by copy/fill or by row/column inserts; `A:A` copied down is still `A:A`. Not supported: the unanchored `1:2` form, which the lexer sees as numbers. |
| FC-32 | The depth audit was under-reporting, then briefly over-reporting | P1 | ✅ | `depth.py` decides a field is LIVE by grepping consumer code for `.field`. It therefore missed every field reached through a setter — `session_set_font_color(hex, slot, tint)` uses `font_theme` and `tint_micro` as surely as an assignment does — and every field set by struct-literal shorthand, which is how the DV panel's `allow_blank` reaches the model. Same class of mistake as `inventory.py`'s, made by the other instrument. Now it also follows one level into model methods. **The first version of the fix over-reported**, jumping to 80% because a constructor touches every field and `new(` appears in consumer code constantly — that is a worse failure than the one it fixed, because an audit that says "nothing left" stops you looking. Constrained to methods that touch ≤3 fields and carry a distinctive multi-word name, after `tint(` matched an unrelated local helper in the layout crate. LIVE 69.1% → 74.3%, **none of it from a code change** — the work was already done and the instrument could not see it. |
| FC-33 | Sorting leaves a record; a file opens where it was left | P2 | ✅ | `sortState` was round-trip only, so a range sorted in this app opened in Excel claiming it had never been sorted — its sort dialog reopens from that element. Now written as part of the same undo step as the move, with `descending` carried by omission for ascending, as the schema defines. `tabSelected` likewise: every file opened on sheet 1, ignoring the tab its author left active — a summary sheet at the end is put there deliberately. A hidden sheet is skipped, because a tab that cannot be shown cannot be the one to open on. |
| FC-34 | Manual page breaks | P2 | ✅ | The last carried-only piece of the print model. `<brk id>` is **one-based**, matching the row number a user sees, while everything else here is zero-based — the conversion happens at the wasm boundary rather than being repeated by each caller, and there is a test that fails if it drifts by one. `man="1"` is written with every break: without it the break is one Excel *computed*, and it is discarded on the next repagination. A break before the first line is refused, because that is the page edge already. The printed page gets `break-before:page` on the row the break sits before, and the grid draws Excel's dashed rules — a break only visible once you print is one you set by accident and never find. |
| FC-19 | Table and hyperlink are reachable from the menu | P2 | ✅ | Both existed behind Ctrl+T / Ctrl+K and the context menu — neither is somewhere you look for a feature you have not met. Now in Insert, with their shortcuts shown. |

## Working rules

Carried from [52](52-FIDELITY-TRACKER.md), because they are what made it
converge:

1. **One row at a time, to completion.** No part-done rows.
2. **Fix the mechanism, not the call site.** Six separate patches to make six
   commands undoable is the same mistake at larger scale. Extend `Operation`
   once.
3. **Verify before claiming.** Re-run `inventory.py`; the number in the note is
   the measured one.
4. **Record what was actually wrong**, not what changed. The diff shows the
   what; the note is the only place the why survives.
5. **A trap found is a test written.** With its reason in the assertion message.

## A note on this tracker's own tooling

The first version of `inventory.py` read each exported function's body and
reported every toolbar styling command — bold, fill, alignment, number format —
as un-undoable. They are all fine: they delegate to `apply_style_range`, which
builds an `EditOperation::Batch`. The tool could not see one level of
indirection and produced a confident, wrong answer.

It now follows delegation into private helpers. Worth recording because it is
the same failure this tracker exists to catch, committed by the instrument
rather than the code — and because a measurement nobody checks is just an
assertion with a number attached.
